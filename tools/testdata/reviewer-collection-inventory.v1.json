{
  "schemaVersion": 1,
  "kind": "reviewer-collection-inventory",
  "description": "Every collection-bearing field on a stage contract or shared PowerShell boundary between capture and the delivery decision. Curated by static reading of the producing and consuming code; each row records where the value is built, where it is consumed, and the shape it collapses to when the boundary is not forced.",
  "stageOrder": [
    "capture",
    "source",
    "snapshot",
    "corpus",
    "blindResults",
    "candidateUnion",
    "fingerprints",
    "specialistPlan",
    "verifierAssignment",
    "verdict",
    "reconciliation",
    "deliveryDecision"
  ],
  "fieldCount": 236,
  "fields": [
    {
      "id": "blindResults/ExpandConstructIds (internal PS from Expand-ReviewerConventionSpecialistConstructIds)/$expanded.Ids",
      "stage": "blindResults",
      "contract": "ExpandConstructIds (internal PS from Expand-ReviewerConventionSpecialistConstructIds)",
      "field": "$expanded.Ids",
      "kind": "psCollectionReturn",
      "required": "False",
      "producer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~453 (@{ Ok=$true; Ids=@($ids.ToArray()); Duplicated=@($duplicated.ToArray()) })",
      "consumer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~1389 ($partition[$field] = @($expanded.Ids))",
      "collapseRisk": "@($ids.ToArray()) double-wrap. 0-element when empty string input. KNOWN ESCAPE SHAPE: empty string input → 0-item List → @() → partition entry is @(). The partition cover-check then reports every sealed anchor as 'missing a verdict' — the consequence of an empty Ids list is a fully-degraded row.",
      "knownEscapeShape": true
    },
    {
      "id": "blindResults/ExpandConstructIds (internal PS)/$expanded.Duplicated",
      "stage": "blindResults",
      "contract": "ExpandConstructIds (internal PS)",
      "field": "$expanded.Duplicated",
      "kind": "psCollectionReturn",
      "required": "False",
      "producer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~453 (@($duplicated.ToArray()))",
      "consumer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~1386 (foreach ($dup in @($expanded.Duplicated)))",
      "collapseRisk": "0 duplicates is the normal case. @() wrap at consumer.",
      "knownEscapeShape": false
    },
    {
      "id": "blindResults/ReviewFacts/CloudTestData/$claims = @(Get-ReviewerFactValue $data 'Claims' @())",
      "stage": "blindResults",
      "contract": "ReviewFacts/CloudTestData",
      "field": "$claims = @(Get-ReviewerFactValue $data 'Claims' @())",
      "kind": "psCollectionReturn",
      "required": "False",
      "producer": "src/Agents/reviewer/ReviewFacts.ps1 ~490 (CloudTest envelope data)",
      "consumer": "src/Agents/reviewer/ReviewFacts.ps1 ~564-572 (orderedClaims.Add; Select-Object -First maxClaims)",
      "collapseRisk": "@() wrap. 0-element → no claim processing; domainApplicability fact is emitted at line ~667 only when testFiles, projects, claims, and manifests are ALL 0.",
      "knownEscapeShape": false
    },
    {
      "id": "blindResults/ReviewFacts/CloudTestData/$files = @(Get-ReviewerFactValue $data 'ChangedFiles' @())",
      "stage": "blindResults",
      "contract": "ReviewFacts/CloudTestData",
      "field": "$files = @(Get-ReviewerFactValue $data 'ChangedFiles' @())",
      "kind": "psCollectionReturn",
      "required": "False",
      "producer": "src/Agents/reviewer/ReviewFacts.ps1 ~488 (wrapper-observed from CloudTest envelope data)",
      "consumer": "src/Agents/reviewer/ReviewFacts.ps1 ~492-499 (Where-Object filtering for testFiles and projects)",
      "collapseRisk": "@() default fallback. 0-element valid (no changed files). 1-element from ConvertFrom-Json → PSCustomObject — Where-Object on PSCustomObject as array works in PS7 due to @() wrap.",
      "knownEscapeShape": false
    },
    {
      "id": "blindResults/ReviewFacts/CloudTestData/$manifests = @(Get-ReviewerFactValue $data 'Manifests' @())",
      "stage": "blindResults",
      "contract": "ReviewFacts/CloudTestData",
      "field": "$manifests = @(Get-ReviewerFactValue $data 'Manifests' @())",
      "kind": "psCollectionReturn",
      "required": "False",
      "producer": "src/Agents/reviewer/ReviewFacts.ps1 ~489 (CloudTest envelope data)",
      "consumer": "src/Agents/reviewer/ReviewFacts.ps1 ~507 (foreach ($manifest in $manifests))",
      "collapseRisk": "@() wrap. 0-element → no manifest processing. 1-element singleton guarded by @().",
      "knownEscapeShape": false
    },
    {
      "id": "blindResults/ReviewFacts/CloudTestExecution/$.execution.categories",
      "stage": "blindResults",
      "contract": "ReviewFacts/CloudTestExecution",
      "field": "$.execution.categories",
      "kind": "jsonArray",
      "required": "False",
      "producer": "src/Agents/reviewer/ReviewFacts.ps1 ~435 (@($categories | ForEach-Object { [string]$_ })) in ConvertFrom-ReviewerFactCloudTestManifest (JSON branch); XML branch: @($category -split '\\s*[,;]\\s*')",
      "consumer": "src/Agents/reviewer/ReviewFacts.ps1 ~524-527 ($categoryObservations = @($execution.categories | ForEach-Object { … })); ~625 (@($required | Where-Object { … }))",
      "collapseRisk": "KNOWN ESCAPE SHAPE: 0 categories is valid (assembly-level claim, no category filter). The XML branch returns @() for an empty Category attribute. The JSON branch: if the manifest has 'categories': [] → @() after @($rawCategories). The consumer at ~625 evaluates @($required).Count -eq 0 → $included=$true for assembly-level claims — safe. BUT: if JSON manifest has 'categories': 'SingleCat' (a string, not an array), li…",
      "knownEscapeShape": true
    },
    {
      "id": "blindResults/RuleRequest (internal PS from Get-ReviewerConventionSpecialistRuleRequest)/$request.Requested",
      "stage": "blindResults",
      "contract": "RuleRequest (internal PS from Get-ReviewerConventionSpecialistRuleRequest)",
      "field": "$request.Requested",
      "kind": "psCollectionReturn",
      "required": "False",
      "producer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~1231 (@{ Requested = $requested.ToArray(); Unrequested = @($unrequested.ToArray()) })",
      "consumer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~1269 (foreach ($entry in @($request.Requested)))",
      "collapseRisk": "0 requested when ResolvedSources is empty (should not happen — specialist requires ≥1 source). @() wrap at consumer.",
      "knownEscapeShape": false
    },
    {
      "id": "blindResults/RuleRequest (internal PS)/$request.Unrequested",
      "stage": "blindResults",
      "contract": "RuleRequest (internal PS)",
      "field": "$request.Unrequested",
      "kind": "psCollectionReturn",
      "required": "False",
      "producer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~1231 (@($unrequested.ToArray()))",
      "consumer": "Used for diagnostics only",
      "collapseRisk": "0-element when all sources fit within MaxRows=10. @() wrap.",
      "knownEscapeShape": false
    },
    {
      "id": "blindResults/SpecialistResultMarker/$.candidates",
      "stage": "blindResults",
      "contract": "SpecialistResultMarker",
      "field": "$.candidates",
      "kind": "jsonArray",
      "required": "True",
      "producer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~694 (marker schema: Type='objectArray'; MaxItems=8); written by model in stdout; parsed by ConvertFrom-ReviewerConventionSpecialistResultMarkerOutcome ~963",
      "consumer": "src/Agents/reviewer/ConventionSpecialist.ps1 Invoke-ReviewerConventionSpecialist ~2021 (foreach candidate in @($Marker.candidates)); also CrossVerification.ps1 ~935 (foreach ($candidate in @($ConventionCandidates)))",
      "collapseRisk": "KNOWN ESCAPE SHAPE (explicitly handled): ConvertFrom-Json on a 1-item candidates array returns a bare PSCustomObject. The normalizer at line ~940 checks $candidatesProperty.Value -is [System.Object[]] and skips non-arrays. Every downstream consumer uses @(...) wrap. 0-item produces [] which is $null risk on older PS but PS7 returns @().",
      "knownEscapeShape": true
    },
    {
      "id": "blindResults/SpecialistResultMarker/$.candidates[*].factIds",
      "stage": "blindResults",
      "contract": "SpecialistResultMarker",
      "field": "$.candidates[*].factIds",
      "kind": "jsonArray",
      "required": "False",
      "producer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~728-731 (schema: Type='string', comma-separated, Pattern rf1:…; NOT a JSON array — stored as a delimited string)",
      "consumer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~275 ($changedFactIds = @($changedFactText -split ',' | Where-Object { $_ }))",
      "collapseRisk": "Stored as a single delimited string, so no JSON array collapse risk. However the split-result @($changedFactIds) could be @('') when factIds is empty string — the Where-Object {$_} filter guards against this.",
      "knownEscapeShape": false
    },
    {
      "id": "blindResults/SpecialistResultMarker/$.residualRisks",
      "stage": "blindResults",
      "contract": "SpecialistResultMarker",
      "field": "$.residualRisks",
      "kind": "jsonArray",
      "required": "True",
      "producer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~918-924 (schema: Type='objectArray'; MaxItems=12); written by model",
      "consumer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~2194 (ResidualRisks = @($Marker.residualRisks)); RunReconciliation.ps1",
      "collapseRisk": "0-element legal (no residual risks identified). @() wrap at consumer. 1-element collapses without @().",
      "knownEscapeShape": false
    },
    {
      "id": "blindResults/SpecialistResultMarker/$.ruleCoverage",
      "stage": "blindResults",
      "contract": "SpecialistResultMarker",
      "field": "$.ruleCoverage",
      "kind": "jsonArray",
      "required": "True",
      "producer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~808-917 (schema: Type='objectArray'; MaxItems=10); written by model; normalized marker",
      "consumer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~2098 ($coverageRows = @($Marker.ruleCoverage)); Resolve-ReviewerConventionSpecialistRuleCoverage ~1248 (foreach ($row in @($Rows)))",
      "collapseRisk": "0 rows legal (no rule coverage reported) → empty array. 1-row result collapses to scalar on ConvertFrom-Json; consumer guards with @(). The marker schema validation rejects this field if over MaxItems=10 (the whole marker fails), so very large arrays are caught before consumption.",
      "knownEscapeShape": false
    },
    {
      "id": "blindResults/SpecialistResultMarker/$.withheld",
      "stage": "blindResults",
      "contract": "SpecialistResultMarker",
      "field": "$.withheld",
      "kind": "jsonArray",
      "required": "True",
      "producer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~776-785 (schema: Type='objectArray'; MaxItems=24); wrapper-populated during Invoke-ReviewerConventionSpecialist ~2094",
      "consumer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~2094 (foreach ($item in @($Marker.withheld))); RunReconciliation.ps1 ~419 (@(Get-ReviewerRunReconciliationValue $VerificationManifest 'withheld' @()))",
      "collapseRisk": "0-element is expected (and legal) when the specialist produces fully valid candidates. Empty array [] default is @() in PS7; @() wrap at every consumer. 1-element withheld produces singleton PSCustomObject on ConvertFrom-Json; all consumers use @().",
      "knownEscapeShape": false
    },
    {
      "id": "blindResults/SpecialistResultMarker/changedCodeFix/$.candidates[*].changedCodeFix.evidenceFactIds",
      "stage": "blindResults",
      "contract": "SpecialistResultMarker/changedCodeFix",
      "field": "$.candidates[*].changedCodeFix.evidenceFactIds",
      "kind": "jsonArray",
      "required": "False",
      "producer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~747-750 (schema: Type='string', AllowEmpty=$true, Pattern; but model sometimes emits [] as JSON array)",
      "consumer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~246 ([string](Get-ReviewerConventionSpecialistValue $changedFix 'evidenceFactIds' '')); ~275 (split by comma)",
      "collapseRisk": "KNOWN ESCAPE SHAPE — EXPLICITLY HANDLED: The normalizer at ConventionSpecialist.ps1 ~948-958 detects when ConvertFrom-Json deserializes this field as a 0-element System.Object[] (the model emitting [] instead of '') and normalizes it to empty string. Without this normalizer, @() silently becomes the string type check fails. The normalizer fires only for the 0-element case; a non-empty array would pass schema validat…",
      "knownEscapeShape": true
    },
    {
      "id": "blindResults/SpecialistResultMarker/RuleCoverageRow/$.ruleCoverage[*].compliantConstructs",
      "stage": "blindResults",
      "contract": "SpecialistResultMarker/RuleCoverageRow",
      "field": "$.ruleCoverage[*].compliantConstructs",
      "kind": "jsonArray",
      "required": "True",
      "producer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~854 (same string schema)",
      "consumer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~1392 ($compliantIds = @($partition['compliantConstructs']))",
      "collapseRisk": "Same as violatingConstructs — delimited string expanded to array. 0-element when empty string, safe with @().",
      "knownEscapeShape": false
    },
    {
      "id": "blindResults/SpecialistResultMarker/RuleCoverageRow/$.ruleCoverage[*].notInReachConstructs",
      "stage": "blindResults",
      "contract": "SpecialistResultMarker/RuleCoverageRow",
      "field": "$.ruleCoverage[*].notInReachConstructs",
      "kind": "jsonArray",
      "required": "True",
      "producer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~865-868 (string schema)",
      "consumer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~1393 ($notInReach = @($partition['notInReachConstructs'])); ~1549 (@($notInReach) | Where-Object { $required -ccontains $_ })",
      "collapseRisk": "Same delimited-string pattern. A large notInReach set that reaches MaxLength=370 chars gets truncated in the marker and the expansion then produces a shorter-than-intended list — partial truncation is a data-loss risk disguised as a count mismatch.",
      "knownEscapeShape": false
    },
    {
      "id": "blindResults/SpecialistResultMarker/RuleCoverageRow/$.ruleCoverage[*].unknownConstructs",
      "stage": "blindResults",
      "contract": "SpecialistResultMarker/RuleCoverageRow",
      "field": "$.ruleCoverage[*].unknownConstructs",
      "kind": "jsonArray",
      "required": "True",
      "producer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~870-874 (string schema)",
      "consumer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~1394 ($unknownIds = @($partition['unknownConstructs'])); drives derived status = 'unknown'",
      "collapseRisk": "Same delimited-string pattern. Even a single unknownConstructs id forces the row to status='unknown'.",
      "knownEscapeShape": false
    },
    {
      "id": "blindResults/SpecialistResultMarker/RuleCoverageRow/$.ruleCoverage[*].violatingChangedFileTargets",
      "stage": "blindResults",
      "contract": "SpecialistResultMarker/RuleCoverageRow",
      "field": "$.ruleCoverage[*].violatingChangedFileTargets",
      "kind": "jsonArray",
      "required": "True",
      "producer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~876-881 (string schema, comma-separated cf<n>:<line>)",
      "consumer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~1395-1396 ($violatingChangedLines = @(… -split ',' | Where-Object { $_ })); ~1401 (foreach $target)",
      "collapseRisk": "Delimited string, same pattern. 0 items when empty string — safe with @()|Where-Object. Each target is validated against the changedLineAnchors dictionary; invalid ones go to $invalidLineTargets.",
      "knownEscapeShape": false
    },
    {
      "id": "blindResults/SpecialistResultMarker/RuleCoverageRow/$.ruleCoverage[*].violatingConstructs",
      "stage": "blindResults",
      "contract": "SpecialistResultMarker/RuleCoverageRow",
      "field": "$.ruleCoverage[*].violatingConstructs",
      "kind": "jsonArray",
      "required": "True",
      "producer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~850-853 (schema: Type='string'; AllowEmpty=$true; stored as comma/range string NOT a JSON array)",
      "consumer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~1382 (Expand-ReviewerConventionSpecialistConstructIds -Text …); ~1391 ($violating = @($partition['violatingConstructs']))",
      "collapseRisk": "Stored as a delimited string. Expand-ReviewerConventionSpecialistConstructIds splits on comma, returns @{ Ok=; Ids=@(…); Duplicated=@(…) }. The Ids array can be 0-element when empty string is passed — safe with @() wrap.",
      "knownEscapeShape": false
    },
    {
      "id": "blindResults/SpecialistRuleCoverageResult (internal PS return from Resolve-ReviewerConventionSpecialistRuleCoverage)/$coverage.Rows",
      "stage": "blindResults",
      "contract": "SpecialistRuleCoverageResult (internal PS return from Resolve-ReviewerConventionSpecialistRuleCoverage)",
      "field": "$coverage.Rows",
      "kind": "list",
      "required": "False",
      "producer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~1700+ ($normalized.ToArray() built inside Resolve-…)",
      "consumer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~2126 (@($coverage.Rows | Where-Object {…}))",
      "collapseRisk": "In-memory List→ToArray. Where-Object pipeline on a 0-element result is $null without @() wrap; caller uses @() guard.",
      "knownEscapeShape": false
    },
    {
      "id": "blindResults/SpecialistRuleCoverageResult (internal PS)/$coverage.UnemittedViolations",
      "stage": "blindResults",
      "contract": "SpecialistRuleCoverageResult (internal PS)",
      "field": "$coverage.UnemittedViolations",
      "kind": "list",
      "required": "False",
      "producer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~1700+ ($unemitted List populated for rows that claim violation but no candidate was emitted)",
      "consumer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~2180 (foreach ($unemitted in @($coverage.UnemittedViolations)))",
      "collapseRisk": "0 items normal. @() wrap at consumer. KNOWN ESCAPE SHAPE: an empty List returned as .ToArray() is @() in memory but if the function returns it through PS pipeline without comma-unary it unwraps to nothing (zero outputs), and the caller's @() capture would still be @() — safe.",
      "knownEscapeShape": true
    },
    {
      "id": "blindResults/TargetResolution (internal PS from Resolve-ReviewerConventionSpecialistTargets)/$resolvedTargets.Targets",
      "stage": "blindResults",
      "contract": "TargetResolution (internal PS from Resolve-ReviewerConventionSpecialistTargets)",
      "field": "$resolvedTargets.Targets",
      "kind": "psCollectionReturn",
      "required": "False",
      "producer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~206 (@{ … Targets = @($ordered); … })",
      "consumer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~259 (foreach ($targetError in @($resolvedTargets.Errors))); ~266 (@($resolvedTargets.Targets).Count -eq 0)",
      "collapseRisk": "@($ordered) wrap guarantees array. 0-element triggers error 'must target a sealed changed line'.",
      "knownEscapeShape": false
    },
    {
      "id": "blindResults/TargetResolution (internal PS)/$resolvedTargets.Errors",
      "stage": "blindResults",
      "contract": "TargetResolution (internal PS)",
      "field": "$resolvedTargets.Errors",
      "kind": "list",
      "required": "False",
      "producer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~205 ([string[]]$errors.ToArray())",
      "consumer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~259 (foreach ($targetError in @($resolvedTargets.Errors)))",
      "collapseRisk": "0 errors is the happy path. [string[]] cast preserves array type even for 0/1 elements. @() wrap at consumer.",
      "knownEscapeShape": false
    },
    {
      "id": "candidateUnion/Adjudication/adjudication.matchedIssueIds (evaluation/v1/adjudication.schema.json)/$.verdicts[*].adjudication.matchedIssueIds",
      "stage": "candidateUnion",
      "contract": "Adjudication/adjudication.matchedIssueIds (evaluation/v1/adjudication.schema.json)",
      "field": "$.verdicts[*].adjudication.matchedIssueIds",
      "kind": "jsonArray",
      "required": "True",
      "producer": "src/Agents/reviewer/evaluation/v1/adjudication.schema.json ~57",
      "consumer": "Adjudication pipeline",
      "collapseRisk": "Same as label.matchedIssueIds. adjudication object can be null (not yet adjudicated), so consumer must null-guard before accessing this field.",
      "knownEscapeShape": false
    },
    {
      "id": "candidateUnion/Adjudication/label.matchedIssueIds (evaluation/v1/adjudication.schema.json)/$.verdicts[*].labels[*].matchedIssueIds",
      "stage": "candidateUnion",
      "contract": "Adjudication/label.matchedIssueIds (evaluation/v1/adjudication.schema.json)",
      "field": "$.verdicts[*].labels[*].matchedIssueIds",
      "kind": "jsonArray",
      "required": "True",
      "producer": "src/Agents/reviewer/evaluation/v1/adjudication.schema.json ~44",
      "consumer": "Adjudication pipeline",
      "collapseRisk": "0-element valid (abstain verdict needs no issue ids). 1-element collapses to string on ConvertFrom-Json.",
      "knownEscapeShape": false
    },
    {
      "id": "candidateUnion/Adjudication/verdict.labels (evaluation/v1/adjudication.schema.json)/$.verdicts[*].labels",
      "stage": "candidateUnion",
      "contract": "Adjudication/verdict.labels (evaluation/v1/adjudication.schema.json)",
      "field": "$.verdicts[*].labels",
      "kind": "jsonArray",
      "required": "True",
      "producer": "src/Agents/reviewer/evaluation/v1/adjudication.schema.json ~34 (required, minItems:2)",
      "consumer": "Adjudication reconciliation",
      "collapseRisk": "Schema requires ≥2 items, so never singleton. No collapse risk from schema enforcement.",
      "knownEscapeShape": false
    },
    {
      "id": "candidateUnion/Adjudication/verdicts (evaluation/v1/adjudication.schema.json)/$.verdicts",
      "stage": "candidateUnion",
      "contract": "Adjudication/verdicts (evaluation/v1/adjudication.schema.json)",
      "field": "$.verdicts",
      "kind": "jsonArray",
      "required": "True",
      "producer": "src/Agents/reviewer/evaluation/v1/adjudication.schema.json ~24 (required field, array)",
      "consumer": "Adjudication/report pipeline",
      "collapseRisk": "0-element = no adjudicated claims yet. 1-verdict collapses to PSCustomObject.",
      "knownEscapeShape": false
    },
    {
      "id": "candidateUnion/DiscoveryCandidate (tools/testdata/reviewer-acquisition-discovery-candidate.json)/$.candidates",
      "stage": "candidateUnion",
      "contract": "DiscoveryCandidate (tools/testdata/reviewer-acquisition-discovery-candidate.json)",
      "field": "$.candidates",
      "kind": "jsonArray",
      "required": "True",
      "producer": "tools/testdata/reviewer-acquisition-discovery-candidate.json ~15 (static test fixture array with 1 candidate)",
      "consumer": "Acquisition harness tool projection logic",
      "collapseRisk": "1-element array in test fixture — ConvertFrom-Json without @() would collapse to scalar PSCustomObject. Test fixtures consumed by harness tools that should use @() wrapping.",
      "knownEscapeShape": false
    },
    {
      "id": "candidateUnion/EligibilityResult/$.decisions",
      "stage": "candidateUnion",
      "contract": "EligibilityResult",
      "field": "$.decisions",
      "kind": "jsonArray",
      "required": "False",
      "producer": "src/Agents/reviewer/CrossVerification.ps1 ~3182 ($decisions.ToArray())",
      "consumer": "Downstream gate evaluation",
      "collapseRisk": "0-element valid. @() wrap.",
      "knownEscapeShape": false
    },
    {
      "id": "candidateUnion/EligibilityResult/$.withheld (post-verification)",
      "stage": "candidateUnion",
      "contract": "EligibilityResult",
      "field": "$.withheld (post-verification)",
      "kind": "jsonArray",
      "required": "False",
      "producer": "src/Agents/reviewer/CrossVerification.ps1 ~3181 ($finalWithheld.ToArray())",
      "consumer": "src/Agents/reviewer/CrossVerification.ps1 ~3275; RunReconciliation.ps1 ~419",
      "collapseRisk": "0-element normal when all candidates are eligible. KNOWN ESCAPE SHAPE: final withheld deduplication uses two HashSet[string] ($eligibleIds, $withheldIds) — a candidateId that appears in both eligible and withheld lists is dropped from withheld (line ~3160-3164). An empty candidateId ('') in a withheld entry is also dropped (line ~3159). This could silently lose accountedNotEmitted entries that carry candidateId=''.",
      "knownEscapeShape": true
    },
    {
      "id": "candidateUnion/EligibilityResult (from Resolve-ReviewerVerificationEligibilityResult)/$.eligible",
      "stage": "candidateUnion",
      "contract": "EligibilityResult (from Resolve-ReviewerVerificationEligibilityResult)",
      "field": "$.eligible",
      "kind": "jsonArray",
      "required": "False",
      "producer": "src/Agents/reviewer/CrossVerification.ps1 ~3180 ($eligible.ToArray())",
      "consumer": "src/Agents/reviewer/CrossVerification.ps1 ~3275 (withheld = @($resolved.withheld)); consumed downstream in delivery pipeline",
      "collapseRisk": "0-element when all candidates are withheld. @() wrap.",
      "knownEscapeShape": false
    },
    {
      "id": "candidateUnion/EligibleCandidate/$.eligible[*].origins",
      "stage": "candidateUnion",
      "contract": "EligibleCandidate",
      "field": "$.eligible[*].origins",
      "kind": "jsonArray",
      "required": "False",
      "producer": "src/Agents/reviewer/CrossVerification.ps1 ~3126 (@((Get-ReviewerVerificationValue $cluster 'origins' @())))",
      "consumer": "Downstream artifact serialization",
      "collapseRisk": "1-model-origin cluster → 1-element origins list → collapses to string on ConvertFrom-Json. @() wrap at capture.",
      "knownEscapeShape": false
    },
    {
      "id": "candidateUnion/EvaluationRun/derivation.models.generalists (evaluation/v1/run.schema.json)/$.derivation.models.generalists",
      "stage": "candidateUnion",
      "contract": "EvaluationRun/derivation.models.generalists (evaluation/v1/run.schema.json)",
      "field": "$.derivation.models.generalists",
      "kind": "jsonArray",
      "required": "True",
      "producer": "src/Agents/reviewer/evaluation/v1/run.schema.json ~76-79 (schema: minItems:2, maxItems:2)",
      "consumer": "Evaluation harness (Evaluation.ps1)",
      "collapseRisk": "Fixed 2-item array (never singleton or empty by schema constraint). No collapse risk.",
      "knownEscapeShape": false
    },
    {
      "id": "candidateUnion/EvaluationRun/derivation.results (evaluation/v1/run.schema.json)/$.derivation.results",
      "stage": "candidateUnion",
      "contract": "EvaluationRun/derivation.results (evaluation/v1/run.schema.json)",
      "field": "$.derivation.results",
      "kind": "jsonArray",
      "required": "True",
      "producer": "src/Agents/reviewer/evaluation/v1/run.schema.json ~85-139 (schema: array of result objects)",
      "consumer": "Evaluation harness and adjudication pipeline",
      "collapseRisk": "0-element valid for an empty corpus run. 1-element corpus → singleton collapse on ConvertFrom-Json.",
      "knownEscapeShape": false
    },
    {
      "id": "candidateUnion/EvaluationRun/result.claims (evaluation/v1/run.schema.json)/$.derivation.results[*].claims",
      "stage": "candidateUnion",
      "contract": "EvaluationRun/result.claims (evaluation/v1/run.schema.json)",
      "field": "$.derivation.results[*].claims",
      "kind": "jsonArray",
      "required": "True",
      "producer": "src/Agents/reviewer/evaluation/v1/run.schema.json ~108-138 (array of claim objects)",
      "consumer": "Adjudication pipeline (blind claim key matching)",
      "collapseRisk": "0-element (approve vote, no findings). 1-claim result collapses to PSCustomObject.",
      "knownEscapeShape": false
    },
    {
      "id": "candidateUnion/GeneralistDiscovery/Findings (from marker)/$.findings (in generalist marker)",
      "stage": "candidateUnion",
      "contract": "GeneralistDiscovery/Findings (from marker)",
      "field": "$.findings (in generalist marker)",
      "kind": "jsonArray",
      "required": "False",
      "producer": "Generalist model stdout (generalist result marker); consumed in ConvertTo-ReviewerVerificationCandidates ~919",
      "consumer": "src/Agents/reviewer/CrossVerification.ps1 ~919 (foreach ($finding in @(Get-ReviewerVerificationValue $marker 'findings' @())))",
      "collapseRisk": "KNOWN ESCAPE SHAPE: 0 findings → @() → no candidates contributed from this pass. 1 finding → singleton PSCustomObject on ConvertFrom-Json; @() wrap at ~919 prevents collapse. Default fallback @() guards $null marker case.",
      "knownEscapeShape": true
    },
    {
      "id": "candidateUnion/VerificationCandidatePlan/$.candidates (bounded candidate list)",
      "stage": "candidateUnion",
      "contract": "VerificationCandidatePlan",
      "field": "$.candidates (bounded candidate list)",
      "kind": "jsonArray",
      "required": "False",
      "producer": "src/Agents/reviewer/CrossVerification.ps1 ~1127 ($bounded.ToArray()) in Get-ReviewerVerificationCandidatePlan",
      "consumer": "src/Agents/reviewer/CrossVerification.ps1 ~3245 ($clusters = @(Get-ReviewerVerificationClusters -Candidates $candidates …))",
      "collapseRisk": "0-element when all candidates exceed cap. MaxCandidates=64 (CrossVerification.ps1 line ~9). @() wrap at consumer.",
      "knownEscapeShape": false
    },
    {
      "id": "candidateUnion/VerificationCandidatePlan/$.withheld (cap-exceeded candidates)",
      "stage": "candidateUnion",
      "contract": "VerificationCandidatePlan",
      "field": "$.withheld (cap-exceeded candidates)",
      "kind": "jsonArray",
      "required": "False",
      "producer": "src/Agents/reviewer/CrossVerification.ps1 ~1128 ($withheld.ToArray()) in Get-ReviewerVerificationCandidatePlan",
      "consumer": "src/Agents/reviewer/CrossVerification.ps1 ~3267 (-PreVerificationWithheld $preVerificationWithheld); RunReconciliation.ps1 ~419",
      "collapseRisk": "0-element when count ≤ cap. @() wrap at all consumers.",
      "knownEscapeShape": false
    },
    {
      "id": "candidateUnion/VerificationCandidatePlan (from Get-ReviewerVerificationCandidatePlan)/$allCandidates",
      "stage": "candidateUnion",
      "contract": "VerificationCandidatePlan (from Get-ReviewerVerificationCandidatePlan)",
      "field": "$allCandidates",
      "kind": "psCollectionReturn",
      "required": "False",
      "producer": "src/Agents/reviewer/CrossVerification.ps1 ~1094 (@(ConvertTo-ReviewerVerificationCandidates …))",
      "consumer": "src/Agents/reviewer/CrossVerification.ps1 ~1101-1107 (@($allCandidates | Where-Object {…}) — split into two priority groups for selection order)",
      "collapseRisk": "@() wrap at capture. 0-element valid (no generalist passes, no convention candidates). The union of generalist findings and convention candidates: a generalist with 0 findings AND specialist with 0 candidates produces @().",
      "knownEscapeShape": false
    },
    {
      "id": "candidateUnion/VerificationCluster/$.clusters[*].memberHashes",
      "stage": "candidateUnion",
      "contract": "VerificationCluster",
      "field": "$.clusters[*].memberHashes",
      "kind": "jsonArray",
      "required": "True",
      "producer": "src/Agents/reviewer/CrossVerification.ps1 ~1278-1280 (@($members | ForEach-Object { … }))",
      "consumer": "src/Agents/reviewer/CrossVerification.ps1 (cluster hash identity computation)",
      "collapseRisk": "1-member cluster gives 1-element array — collapses to string on ConvertFrom-Json. The hash identity depends on this being an array; a scalar string passed to Get-ReviewerVerificationObjectSha256 would produce a different hash than a 1-element array.",
      "knownEscapeShape": false
    },
    {
      "id": "candidateUnion/VerificationCluster/$.clusters[*].origins",
      "stage": "candidateUnion",
      "contract": "VerificationCluster",
      "field": "$.clusters[*].origins",
      "kind": "jsonArray",
      "required": "False",
      "producer": "src/Agents/reviewer/CrossVerification.ps1 ~1304 ($originList.ToArray()) — deduplicated model names for cluster members",
      "consumer": "src/Agents/reviewer/CrossVerification.ps1 ~3282 (@($_.origins))",
      "collapseRisk": "1-origin cluster (single-model discovery) collapses to string on ConvertFrom-Json. Consumer wraps with @().",
      "knownEscapeShape": false
    },
    {
      "id": "candidateUnion/VerificationCluster (from Get-ReviewerVerificationClusters)/$.clusters[*].members",
      "stage": "candidateUnion",
      "contract": "VerificationCluster (from Get-ReviewerVerificationClusters)",
      "field": "$.clusters[*].members",
      "kind": "jsonArray",
      "required": "True",
      "producer": "src/Agents/reviewer/CrossVerification.ps1 ~1305 (members = $members.ToArray())",
      "consumer": "src/Agents/reviewer/CrossVerification.ps1 ~3167 (foreach ($candidate in @(Get-ReviewerVerificationValue $cluster 'members' @()))); ~1334 (foreach ($member in @(Get-ReviewerVerificationValue $cluster 'members' @())))",
      "collapseRisk": "Every cluster has ≥1 member by construction (groups start from a single seed). 1-member cluster → singleton PSCustomObject on ConvertFrom-Json. All consumers use @() wrap via Get-ReviewerVerificationValue fallback @().",
      "knownEscapeShape": false
    },
    {
      "id": "candidateUnion/VerificationManifest/PreVerificationWithheld/$.preVerificationWithheld",
      "stage": "candidateUnion",
      "contract": "VerificationManifest/PreVerificationWithheld",
      "field": "$.preVerificationWithheld",
      "kind": "jsonArray",
      "required": "False",
      "producer": "src/Agents/reviewer/CrossVerification.ps1 ~3255 ($preVerificationWithheld = @((Get-ReviewerVerificationValue $InputManifest 'preVerificationWithheld' @())))",
      "consumer": "src/Agents/reviewer/CrossVerification.ps1 ~3267 (-PreVerificationWithheld $preVerificationWithheld); ~3260 ($candidates.Count + $preVerificationWithheld.Count)",
      "collapseRisk": "@() wrap. 0-element expected for first-pass reviews. Count check at ~3260 validates sealed total candidate accounting.",
      "knownEscapeShape": false
    },
    {
      "id": "capture/discovery-candidate-input.v1/$.candidates[*]",
      "stage": "capture",
      "contract": "discovery-candidate-input.v1",
      "field": "$.candidates[*]",
      "kind": "jsonArray",
      "required": "required: yes; empty array: NO (minItems:1)",
      "producer": "src/Agents/reviewer/acquisition/v1/discovery-candidate.schema.json:38-58 (minItems:1, maxItems:64, required)",
      "consumer": "Verifier acquisition: the candidate set the verifier compares its output against; membership test and hash computation",
      "collapseRisk": "A single-candidate set (common when a specialist found exactly one finding) collapses on ConvertFrom-Json to a PSCustomObject. Consumer must use @($candidates) before any foreach or indexing. Collapse to scalar would cause the hash computation to include .Name strings, not candidate entry fields.",
      "knownEscapeShape": false
    },
    {
      "id": "capture/legacy-benchmark-projection.v1/$.resources[*]",
      "stage": "capture",
      "contract": "legacy-benchmark-projection.v1",
      "field": "$.resources[*]",
      "kind": "jsonArray",
      "required": "required: yes; empty array: NO (minItems:1)",
      "producer": "src/Agents/reviewer/acquisition/v1/legacy-benchmark-projection.schema.json:39-55 (minItems:1, maxItems:4096, required)",
      "consumer": "Legacy benchmark adapter: each entry maps a mediaRole to a sealed payload path for prompt injection",
      "collapseRisk": "Same single-element collapse as other resources arrays. maxItems:4096 bound prevents runaway, but minItems:1 means empty never legally occurs — code must still @()-wrap on deserialization.",
      "knownEscapeShape": false
    },
    {
      "id": "capture/role-input-capture-request.v1/$.resources[*]",
      "stage": "capture",
      "contract": "role-input-capture-request.v1",
      "field": "$.resources[*]",
      "kind": "jsonArray",
      "required": "required: yes; empty array: NO (minItems:1)",
      "producer": "src/Agents/reviewer/acquisition/v1/role-input-capture-request.schema.json:41-56 (minItems:1, maxItems:1024, required)",
      "consumer": "Acquisition runner: each resource entry names a sealed media payload to inject into the capture",
      "collapseRisk": "Single-resource request collapses to PSCustomObject from ConvertFrom-Json. Consumer must wrap with @().",
      "knownEscapeShape": false
    },
    {
      "id": "capture/role-input-capture.v1/$.files[*]",
      "stage": "capture",
      "contract": "role-input-capture.v1",
      "field": "$.files[*]",
      "kind": "jsonArray",
      "required": "required: yes; empty array: NO (minItems:4)",
      "producer": "src/Agents/reviewer/acquisition/v1/role-input-capture.schema.json:160-173 (minItems:4, required)",
      "consumer": "Integrity cross-check: each entry binds a captured file's path, sha256, byteLength",
      "collapseRisk": "minItems:4 guarantees multiple elements in valid data, so PSCustomObject collapse from ConvertFrom-Json is unlikely for real captures, but a synthetic test document with exactly 4 items that happen to fail and leave 1 would still hit the single-element collapse pattern.",
      "knownEscapeShape": false
    },
    {
      "id": "capture/role-input-capture.v1/$.launch.allowTools",
      "stage": "capture",
      "contract": "role-input-capture.v1",
      "field": "$.launch.allowTools",
      "kind": "jsonArray",
      "required": "required: yes; empty array: YES",
      "producer": "src/Agents/reviewer/acquisition/v1/role-input-capture.schema.json:91 — $defs/toolList; required",
      "consumer": "Same as availableTools — startup allow-list enforcement",
      "collapseRisk": "Same as availableTools; a 0-element toolList is legal (deny everything) and must not collapse to $null on round-trip.",
      "knownEscapeShape": false
    },
    {
      "id": "capture/role-input-capture.v1/$.launch.arguments",
      "stage": "capture",
      "contract": "role-input-capture.v1",
      "field": "$.launch.arguments",
      "kind": "jsonArray",
      "required": "required: yes; empty array: NO (minItems:1)",
      "producer": "src/Agents/reviewer/acquisition/v1/role-input-capture.schema.json:93-96 (minItems:1, items: string); required",
      "consumer": "Process launch: argv reconstructed from this array for replay qualification and audit",
      "collapseRisk": "A 1-argument array becomes a bare string after ConvertFrom-Json; wrapped with @() it is safe for argv reconstruction. Missing @() would pass a scalar string to a param expecting [string[]], silently producing a 1-character-per-element iteration in some cases.",
      "knownEscapeShape": false
    },
    {
      "id": "capture/role-input-capture.v1/$.launch.availableTools",
      "stage": "capture",
      "contract": "role-input-capture.v1",
      "field": "$.launch.availableTools",
      "kind": "jsonArray",
      "required": "required: yes; empty array: YES (no minItems)",
      "producer": "src/Agents/reviewer/acquisition/v1/role-input-capture.schema.json:90 — $defs/toolList: type array, uniqueItems:true; required in launch object",
      "consumer": "Acquisition startup validation (Start-ReviewerAgent.ps1 — not in scope) reads and iterates this list to confirm the tool set before launch",
      "collapseRisk": "A toolList with 1 element may serialize as a bare string on ConvertTo-Json without -AsHashtable. The uniqueItems constraint is not enforced by PS serialization. If later deserialized and compared as a string, membership tests would work by accident; $null would not.",
      "knownEscapeShape": false
    },
    {
      "id": "capture/role-input-capture.v1/$.launch.denySet",
      "stage": "capture",
      "contract": "role-input-capture.v1",
      "field": "$.launch.denySet",
      "kind": "jsonArray",
      "required": "required: yes; empty array: YES",
      "producer": "src/Agents/reviewer/acquisition/v1/role-input-capture.schema.json:92 — $defs/toolList; required",
      "consumer": "Tool-call interception layer; membership test at each model tool invocation",
      "collapseRisk": "Empty denySet [] is correct for a permissive capture. If collapsed to $null, -ccontains membership test evaluates to $false for every tool name, silently disabling denial enforcement.",
      "knownEscapeShape": false
    },
    {
      "id": "capture/role-input-capture.v1/$.snapshot.resources[*]",
      "stage": "capture",
      "contract": "role-input-capture.v1",
      "field": "$.snapshot.resources[*]",
      "kind": "jsonArray",
      "required": "required: yes; empty array: NO (minItems:1)",
      "producer": "src/Agents/reviewer/acquisition/v1/role-input-capture.schema.json:109-124 (minItems:1, required in snapshot object)",
      "consumer": "Capture audit: each resource entry records what was read during the role run",
      "collapseRisk": "A single-resource snapshot gives PSCustomObject from ConvertFrom-Json. Iterating without @() would yield property name strings. The field is minItems:1 so 0-element collapse does not arise from valid data.",
      "knownEscapeShape": false
    },
    {
      "id": "capture/transcript-package.v1/$.attempts[*]",
      "stage": "capture",
      "contract": "transcript-package.v1",
      "field": "$.attempts[*]",
      "kind": "jsonArray",
      "required": "required: yes; empty array: NO (minItems:1)",
      "producer": "src/Agents/reviewer/acquisition/v1/transcript-package.schema.json:120-155 (minItems:1, maxItems:8, required)",
      "consumer": "src/Agents/reviewer/AcquisitionPackage.ps1:261 — [string]@($core.attempts)[-1].markerStatus membership test for RequireCaptured validation; indexing the last element",
      "collapseRisk": "If $core.attempts contains one entry, ConvertFrom-Json returns a PSCustomObject rather than [object[]]. Without @() wrap, [-1] indexing on a PSCustomObject does not yield the last array element — it yields $null or throws. The @() at line 261 guards this. A missing wrapper in any other consumer would silently yield $null.",
      "knownEscapeShape": false
    },
    {
      "id": "capture/transcript-package.v1/$.directories[*]",
      "stage": "capture",
      "contract": "transcript-package.v1",
      "field": "$.directories[*]",
      "kind": "jsonArray",
      "required": "required: no; empty array: YES",
      "producer": "src/Agents/reviewer/acquisition/v1/transcript-package.schema.json:62-71 (no minItems, not required)",
      "consumer": "src/Agents/reviewer/AcquisitionPackage.ps1:186 — foreach ($entry in @($manifest.directories)) to populate $boundDirectories hashtable; line 194 — Get-ChildItem cross-check to detect undeclared directories",
      "collapseRisk": "Not in schema required list; if omitted from JSON, ConvertFrom-Json gives $null for .directories. @() wrap at line 186 is the only guard — without it, foreach on $null iterates once with $null, poisoning $boundDirectories with a null key. Empty JSON array [] is safe.",
      "knownEscapeShape": false
    },
    {
      "id": "capture/transcript-package.v1/$.files[*]",
      "stage": "capture",
      "contract": "transcript-package.v1",
      "field": "$.files[*]",
      "kind": "jsonArray",
      "required": "required: yes; empty array: NO (minItems:1)",
      "producer": "src/Agents/reviewer/acquisition/v1/transcript-package.schema.json:48-61 (minItems:1, required)",
      "consumer": "src/Agents/reviewer/AcquisitionPackage.ps1:141 — foreach ($entry in @($manifest.files)) iterates to verify bound-file SHA/bytes; AcquisitionPackage.ps1:171 — cross-checked against Get-ChildItem for unbound-file detection",
      "collapseRisk": "ConvertFrom-Json of a 1-entry JSON array produces a PSCustomObject, not an [object[]]. The @() wrap at line 141 guards the foreach; absent @(), a single-file package would iterate the PSCustomObject's properties, not the file entry. Every downstream path must use @($manifest.files).",
      "knownEscapeShape": false
    },
    {
      "id": "corpus/evaluation-corpus.v1/$.corrections[*]",
      "stage": "corpus",
      "contract": "evaluation-corpus.v1",
      "field": "$.corrections[*]",
      "kind": "jsonArray",
      "required": "required: yes; empty: YES — common for a freshly frozen corpus",
      "producer": "src/Agents/reviewer/evaluation/v1/corpus.schema.json:58-86 (type:array, no minItems, required)",
      "consumer": "Evaluation.ps1 — reads corrections to apply to examples' ground truth",
      "collapseRisk": "Empty [] is common — @() on ConvertFrom-Json result is required. A 1-correction array collapses to PSCustomObject.",
      "knownEscapeShape": false
    },
    {
      "id": "corpus/evaluation-corpus.v1/$.examples[*]",
      "stage": "corpus",
      "contract": "evaluation-corpus.v1",
      "field": "$.examples[*]",
      "kind": "jsonArray",
      "required": "required: yes; empty: NO (minItems:1)",
      "producer": "src/Agents/reviewer/evaluation/v1/corpus.schema.json:53-56 (type:array, minItems:1, required)",
      "consumer": "src/Agents/reviewer/evaluation/Evaluation.ps1 (not read in this session) — iterates examples for evaluation scoring and adjudication",
      "collapseRisk": "A 1-example corpus collapses to a single PSCustomObject on ConvertFrom-Json. Any consumer iterating without @() would iterate the PSCustomObject's properties, not the example fields. minItems:1 makes 0-element illegal, but the 1-element collapse is real.",
      "knownEscapeShape": false
    },
    {
      "id": "corpus/evaluation-corpus.v1/$.examples[*].adjudication.issueIds",
      "stage": "corpus",
      "contract": "evaluation-corpus.v1",
      "field": "$.examples[*].adjudication.issueIds",
      "kind": "jsonArray",
      "required": "required: yes (in adjudication when present); empty: YES",
      "producer": "src/Agents/reviewer/evaluation/v1/corpus.schema.json:208 (type:array, items:string; no minItems)",
      "consumer": "Ground truth resolution — adjudicator's issue ID list overrides label disagreements",
      "collapseRisk": "adjudication is type ['object','null'] at line 201 — the whole field may be null. When present, issueIds can be empty or 1-element (collapse risk same as labels.issueIds).",
      "knownEscapeShape": false
    },
    {
      "id": "corpus/evaluation-corpus.v1/$.examples[*].groundTruth.issueIds",
      "stage": "corpus",
      "contract": "evaluation-corpus.v1",
      "field": "$.examples[*].groundTruth.issueIds",
      "kind": "jsonArray",
      "required": "required: yes; empty: YES — concordant decision of 'approve' has no issueIds",
      "producer": "src/Agents/reviewer/evaluation/v1/corpus.schema.json:218 (type:array, items:string; no minItems; required in groundTruth)",
      "consumer": "Evaluation scoring — the canonical ground-truth issue set each model output is measured against",
      "collapseRisk": "Empty [] for approve decisions — must NOT collapse to $null. A single groundTruth issueId collapses on ConvertFrom-Json. Evaluation loop must use @($groundTruth.issueIds) for correct membership test.",
      "knownEscapeShape": false
    },
    {
      "id": "corpus/evaluation-corpus.v1/$.examples[*].inventory[*]",
      "stage": "corpus",
      "contract": "evaluation-corpus.v1",
      "field": "$.examples[*].inventory[*]",
      "kind": "jsonArray",
      "required": "required: yes (in example required); empty: YES — valid for examples where no issues were found",
      "producer": "src/Agents/reviewer/evaluation/v1/corpus.schema.json:167-182 (type:array, items:issueId/class/severity/convention/correctness/path; no minItems)",
      "consumer": "Evaluation scoring — each item describes one labeled issue for the example",
      "collapseRisk": "Empty inventory is common (PR with no real issues). A 1-issue inventory collapses to PSCustomObject. Consumer must use @().",
      "knownEscapeShape": false
    },
    {
      "id": "corpus/evaluation-corpus.v1/$.examples[*].labels[*]",
      "stage": "corpus",
      "contract": "evaluation-corpus.v1",
      "field": "$.examples[*].labels[*]",
      "kind": "jsonArray",
      "required": "required: yes; empty: NO (minItems:2)",
      "producer": "src/Agents/reviewer/evaluation/v1/corpus.schema.json:183-198 (type:array, minItems:2, required)",
      "consumer": "Adjudication: at least 2 labeler decisions required per example; consumers compare labels to derive concordance",
      "collapseRisk": "minItems:2 guarantees at least 2 elements — collapse to scalar is not possible for valid data. However, a malformed seed file could have 1 label; the schema would catch it but deserialization happens before validation.",
      "knownEscapeShape": false
    },
    {
      "id": "corpus/evaluation-corpus.v1/$.examples[*].labels[*].issueIds",
      "stage": "corpus",
      "contract": "evaluation-corpus.v1",
      "field": "$.examples[*].labels[*].issueIds",
      "kind": "jsonArray",
      "required": "required: yes (in label object); empty: YES — labeler abstained",
      "producer": "src/Agents/reviewer/evaluation/v1/corpus.schema.json:195 (type:array, items:string; no minItems)",
      "consumer": "Adjudication: issueIds list for each labeler's claim — compared against inventory.issueId",
      "collapseRisk": "Empty [] means labeler found nothing. A 1-issueId list collapses to bare string on ConvertFrom-Json. -ccontains membership test fails silently if consumer doesn't @()-wrap.",
      "knownEscapeShape": false
    },
    {
      "id": "corpus/evaluation-corpus.v1/$.strata",
      "stage": "corpus",
      "contract": "evaluation-corpus.v1",
      "field": "$.strata",
      "kind": "jsonArray",
      "required": "required: yes (in corpus required array); empty: YES",
      "producer": "src/Agents/reviewer/evaluation/v1/corpus.schema.json:49-51 (type:array, items:$ref stratum enum; not required, no minItems)",
      "consumer": "src/Agents/reviewer/evaluation/v1/corpus.schema.json — defines the valid strata values; Evaluation.ps1 (not in session scope) reads strata for partition/stratification",
      "collapseRisk": "An empty strata array is legal (no stratification). A 1-stratum corpus collapses to a bare string on ConvertFrom-Json — any consumer must use @() wrap.",
      "knownEscapeShape": false
    },
    {
      "id": "deliveryDecision/evaluation-policy.v1 (evaluation/v1/policy.schema.json)//qualifiableScopes",
      "stage": "deliveryDecision",
      "contract": "evaluation-policy.v1 (evaluation/v1/policy.schema.json)",
      "field": "/qualifiableScopes",
      "kind": "jsonArray",
      "required": "yes; minItems:1 (schema enforced)",
      "producer": "evaluation policy file (out-of-repo)",
      "consumer": "evaluation tool uses to determine which pack+severity scopes are qualified",
      "collapseRisk": "Cannot be empty by schema (minItems:1). Each element {pack, severity} is required.",
      "knownEscapeShape": false
    },
    {
      "id": "deliveryDecision/evaluation-report.v1 (evaluation/v1/report.schema.json)//arms",
      "stage": "deliveryDecision",
      "contract": "evaluation-report.v1 (evaluation/v1/report.schema.json)",
      "field": "/arms",
      "kind": "jsonArray",
      "required": "yes (schema required); no minItems but practically ≥1",
      "producer": "evaluation tool (tools/)",
      "consumer": "evaluation tool reads back; qualification logic iterates arms",
      "collapseRisk": "Each arm has ~50 required sub-fields. If serialized via ConvertTo-Json without -Depth ≥4, nested sub-objects would be truncated. Evaluation tool must use -Depth ≥5.",
      "knownEscapeShape": false
    },
    {
      "id": "deliveryDecision/evaluation-report.v1 /arms[*]//arms[*]/issueClassRecall",
      "stage": "deliveryDecision",
      "contract": "evaluation-report.v1 /arms[*]",
      "field": "/arms[*]/issueClassRecall",
      "kind": "jsonArray",
      "required": "yes; LEGALLY EMPTY (no issue-class breakdown)",
      "producer": "evaluation tool",
      "consumer": "evaluation tool; schema required in each arm",
      "collapseRisk": "Each element has required sub-fields {issueClass,matched,total,recall}. ConvertTo-Json depth matters here too.",
      "knownEscapeShape": false
    },
    {
      "id": "deliveryDecision/evaluation-report.v1 /corpusPopulation//corpusPopulation/byStratum",
      "stage": "deliveryDecision",
      "contract": "evaluation-report.v1 /corpusPopulation",
      "field": "/corpusPopulation/byStratum",
      "kind": "jsonArray",
      "required": "yes; LEGALLY EMPTY when corpus not stratified",
      "producer": "evaluation tool",
      "consumer": "evaluation tool; schema required",
      "collapseRisk": "@() is legal. 0 strata → minPerStratumExamples check trivially satisfied.",
      "knownEscapeShape": false
    },
    {
      "id": "deliveryDecision/evaluation-report.v1 /corpusPopulation//corpusPopulation/deficits",
      "stage": "deliveryDecision",
      "contract": "evaluation-report.v1 /corpusPopulation",
      "field": "/corpusPopulation/deficits",
      "kind": "jsonArray",
      "required": "yes; LEGALLY EMPTY (empty deficits = corpus meets all thresholds)",
      "producer": "evaluation tool",
      "consumer": "evaluation tool; schema required; items are deficit description strings",
      "collapseRisk": "ESCAPE SHAPE: 0-element deficits means qualification thresholds are satisfied. Non-empty strings describe specific shortfalls.",
      "knownEscapeShape": false
    },
    {
      "id": "deliveryDecision/evaluation-report.v1 /qualification//qualification/globalVetoes",
      "stage": "deliveryDecision",
      "contract": "evaluation-report.v1 /qualification",
      "field": "/qualification/globalVetoes",
      "kind": "jsonArray",
      "required": "yes; LEGALLY EMPTY (no global vetoes = anyQualified can be true)",
      "producer": "evaluation tool",
      "consumer": "evaluation tool; schema required (items: string, minLength:1)",
      "collapseRisk": "ESCAPE SHAPE: 0-element = no global block on qualification. Non-empty strings each represent a veto reason preventing all scopes from qualifying.",
      "knownEscapeShape": false
    },
    {
      "id": "deliveryDecision/evaluation-report.v1 /qualification//qualification/requirements",
      "stage": "deliveryDecision",
      "contract": "evaluation-report.v1 /qualification",
      "field": "/qualification/requirements",
      "kind": "jsonArray",
      "required": "yes; no minItems",
      "producer": "evaluation tool",
      "consumer": "evaluation tool; schema required",
      "collapseRisk": "Each requirement has {id, ok, reasonCodes, observed}. Nested reasonCodes array inside each requirement.",
      "knownEscapeShape": false
    },
    {
      "id": "deliveryDecision/evaluation-report.v1 /qualification/requirements[*]//qualification/requirements[*]/reasonCodes",
      "stage": "deliveryDecision",
      "contract": "evaluation-report.v1 /qualification/requirements[*]",
      "field": "/qualification/requirements[*]/reasonCodes",
      "kind": "jsonArray",
      "required": "yes; LEGALLY EMPTY when requirement ok=true",
      "producer": "evaluation tool",
      "consumer": "evaluation tool; schema required (items: string, minLength:1)",
      "collapseRisk": "ESCAPE SHAPE: 0-element = requirement passed. Non-empty = specific reason codes for the failure.",
      "knownEscapeShape": false
    },
    {
      "id": "deliveryDecision/evaluation-report.v1 /transcriptionInput/comment//transcriptionInput/comment/scopes",
      "stage": "deliveryDecision",
      "contract": "evaluation-report.v1 /transcriptionInput/comment",
      "field": "/transcriptionInput/comment/scopes",
      "kind": "jsonArray",
      "required": "yes (schema required); no minItems",
      "producer": "evaluation tool (mirrors qualification shape for operator transcription)",
      "consumer": "operator reads to hand-author qualification artifact",
      "collapseRisk": "Mirrors /comment/scopes in qualification. If nonQualifying=true, operator can skip transcription.",
      "knownEscapeShape": false
    },
    {
      "id": "deliveryDecision/gate-decision /candidates[*]//candidates[*]/humanPromotedCommentReasons",
      "stage": "deliveryDecision",
      "contract": "gate-decision /candidates[*]",
      "field": "/candidates[*]/humanPromotedCommentReasons",
      "kind": "jsonArray",
      "required": "yes; always ≥1 element by same logic",
      "producer": "DeliveryGates.ps1:992 `@(if ([bool]$humanPromoted.Ok) { @(\"ok\") } else { @($humanPromoted.ReasonCodes) })`",
      "consumer": "(wrapper)",
      "collapseRisk": "Same as unattendedCommentReasons. @() wrapper ensures array.",
      "knownEscapeShape": false
    },
    {
      "id": "deliveryDecision/gate-decision /candidates[*]//candidates[*]/unattendedCommentReasons",
      "stage": "deliveryDecision",
      "contract": "gate-decision /candidates[*]",
      "field": "/candidates[*]/unattendedCommentReasons",
      "kind": "jsonArray",
      "required": "yes; always ≥1 element (either @(\"ok\") or reason codes)",
      "producer": "DeliveryGates.ps1:990 `@(if ($unattendedOk) { @(\"ok\") } else { $unattendedReasons })`",
      "consumer": "(wrapper reads to decide posting eligibility)",
      "collapseRisk": "Singleton @(\"ok\") is the eligible path. If $unattendedReasons is @() from Get-ReviewerGateUniqueReasonCodes despite !Ok (unreachable in practice), would get empty. In practice if !Ok, reasons always has ≥1 entry.",
      "knownEscapeShape": false
    },
    {
      "id": "deliveryDecision/gate-decision artifact//candidates",
      "stage": "deliveryDecision",
      "contract": "gate-decision artifact",
      "field": "/candidates",
      "kind": "jsonArray",
      "required": "yes; LEGALLY EMPTY when no verified eligible candidates",
      "producer": "DeliveryGates.ps1:1043 `candidates = $candidateEntries.ToArray()`",
      "consumer": "(wrapper, Start-ReviewerAgent.ps1 reads for posting logic)",
      "collapseRisk": "$candidateEntries.ToArray() on empty List returns empty typed object[] → stored in [ordered]@{} → serialized as [] by canonical JSON. Safe.",
      "knownEscapeShape": false
    },
    {
      "id": "deliveryDecision/gate-decision artifact//gateImportantOrHigherKeys",
      "stage": "deliveryDecision",
      "contract": "gate-decision artifact",
      "field": "/gateImportantOrHigherKeys",
      "kind": "jsonArray",
      "required": "yes; LEGALLY EMPTY (no important+ findings)",
      "producer": "DeliveryGates.ps1:1004 `$importantOrHigherKeys = @($importantOrHigher | ForEach-Object { [string]$_.gateKey } | Sort-Object -Unique)`",
      "consumer": "(wrapper, monotonicity check and approval coverage key)",
      "collapseRisk": "@() wrapper. 0-element = no important/higher findings → approval eligible for that criterion.",
      "knownEscapeShape": false
    },
    {
      "id": "deliveryDecision/gate-decision artifact//humanPromotableComments",
      "stage": "deliveryDecision",
      "contract": "gate-decision artifact",
      "field": "/humanPromotableComments",
      "kind": "jsonArray",
      "required": "yes; LEGALLY EMPTY",
      "producer": "DeliveryGates.ps1:1046 `humanPromotableComments = $humanPromotable.ToArray()`",
      "consumer": "(wrapper for human-promote mode display)",
      "collapseRisk": "$humanPromotable.ToArray() on empty List → empty object[] → serialized as []. Safe via canonical JSON path.",
      "knownEscapeShape": false
    },
    {
      "id": "deliveryDecision/gate-decision artifact//runReasonCodes",
      "stage": "deliveryDecision",
      "contract": "gate-decision artifact",
      "field": "/runReasonCodes",
      "kind": "jsonArray",
      "required": "yes; LEGALLY EMPTY when RunAccounting.Ok=true",
      "producer": "DeliveryGates.ps1:1008 `if ([bool]$RunAccounting.Ok) { @() } else { Get-ReviewerGateUniqueReasonCodes -Reasons $RunAccounting.ReasonCodes }`",
      "consumer": "(wrapper reads to determine whether run was clean; checked by Test-ReviewerVerifiedMultiPassPreconditions)",
      "collapseRisk": "ESCAPE SHAPE: @() when Ok=true — a consumer checking .Count=0 as \"run clean\" is correct by design.",
      "knownEscapeShape": false
    },
    {
      "id": "deliveryDecision/gate-decision artifact//unattendedComments",
      "stage": "deliveryDecision",
      "contract": "gate-decision artifact",
      "field": "/unattendedComments",
      "kind": "jsonArray",
      "required": "yes; LEGALLY EMPTY when no eligible comments or cap=0",
      "producer": "DeliveryGates.ps1:999 `$cappedComments = @(Get-ReviewerGateCappedSubset -Entries … -MaxCount ([int]$EffectivePolicy.maxCommentsPerRun))`",
      "consumer": "(wrapper posts these comments)",
      "collapseRisk": "ESCAPE SHAPE: Get-ReviewerGateCappedSubset returns `@()` when MaxCount≤0 (line 874 `if ($MaxCount -le 0) { return @() }`). Policy ships maxCommentsPerRun=3 but policy can set it to 0 → silently empty.",
      "knownEscapeShape": false
    },
    {
      "id": "deliveryDecision/gate-decision artifact//unattendedSuggestions",
      "stage": "deliveryDecision",
      "contract": "gate-decision artifact",
      "field": "/unattendedSuggestions",
      "kind": "jsonArray",
      "required": "yes; LEGALLY EMPTY",
      "producer": "DeliveryGates.ps1:1001 `$cappedSuggestions = @(Get-ReviewerGateCappedSubset -Entries … -MaxCount ([int]$EffectivePolicy.maxSuggestionsPerRun))`",
      "consumer": "(wrapper posts suggestions)",
      "collapseRisk": "ESCAPE SHAPE: policy.json ships maxSuggestionsPerRun=0 → Get-ReviewerGateCappedSubset immediately returns @(). Default is always empty suggestions.",
      "knownEscapeShape": false
    },
    {
      "id": "deliveryDecision/gates.policy.v1 (gate-policy.json + policy.schema.json)//packs",
      "stage": "deliveryDecision",
      "contract": "gates.policy.v1 (gate-policy.json + policy.schema.json)",
      "field": "/packs",
      "kind": "jsonArray",
      "required": "yes (policy.schema.json required); LEGALLY EMPTY (no packs declared → all candidates get packUnknown → nothing eligible)",
      "producer": "gates/v1/policy.json:22 `\"packs\": [...]`; effective policy built at DeliveryGates.ps1:299 `$packsRaw = @(Get-ReviewerVerificationValue $Policy \"packs\" @())`",
      "consumer": "DeliveryGates.ps1:305 `foreach ($pack in $packsRaw)` → $effectivePacks; 719 `@($EffectivePolicy.packs | Where-Object { [string]$_.name -ceq $pack })` in Test-ReviewerGateCandidateEligible",
      "collapseRisk": "ESCAPE SHAPE: empty packs array → all candidates fail packUnknown. Code cap: MaxPacks=32. `@()` default prevents null collapse.",
      "knownEscapeShape": false
    },
    {
      "id": "deliveryDecision/gates.policy.v1 /approval//approval/requiredCheckNames",
      "stage": "deliveryDecision",
      "contract": "gates.policy.v1 /approval",
      "field": "/approval/requiredCheckNames",
      "kind": "jsonArray",
      "required": "yes (schema required field); LEGALLY EMPTY (empty = no specific checks required)",
      "producer": "gates/v1/policy.json:29 `\"requiredCheckNames\": []`; DeliveryGates.ps1:326 `$requiredCheckNames = @(Get-ReviewerVerificationValue $approvalRaw \"requiredCheckNames\" @())`",
      "consumer": "DeliveryGates.ps1:330-334 (validates entries are non-empty strings); stored in effective policy; (wrapper checks live CI status against this list)",
      "collapseRisk": "ESCAPE SHAPE: empty is the default. Policy.json ships with `[]`. Code cap: MaxRequiredCheckNames=64. @() default prevents collapse.",
      "knownEscapeShape": false
    },
    {
      "id": "deliveryDecision/gates.qualification.v1 (qualification.schema.json)//comment/scopes",
      "stage": "deliveryDecision",
      "contract": "gates.qualification.v1 (qualification.schema.json)",
      "field": "/comment/scopes",
      "kind": "jsonArray",
      "required": "yes (qualification schema required); no minItems → LEGALLY EMPTY but 0 scopes means no qualification for any comment",
      "producer": "qualification artifact (New-ReviewerGateQualification tool)",
      "consumer": "DeliveryGates.ps1:603-611 `$scopes = @(Get-ReviewerVerificationValue $commentNode \"scopes\" @())` — filters to matching pack+severity; if .Count ≠ 1 → qualificationScopeMissing",
      "collapseRisk": "ESCAPE SHAPE: 0-element scopes makes EVERY comment-eligibility check return qualificationScopeMissing (fail-closed). Wrapped in @().",
      "knownEscapeShape": false
    },
    {
      "id": "deliveryDecision/gates.qualification.v1 /agentBinding//agentBinding/generalistModels",
      "stage": "deliveryDecision",
      "contract": "gates.qualification.v1 /agentBinding",
      "field": "/agentBinding/generalistModels",
      "kind": "jsonArray",
      "required": "yes; minItems:2, maxItems:2 (qualification.schema.json)",
      "producer": "qualification artifact",
      "consumer": "DeliveryGates.ps1:564 `$qualModels = @(@(Get-ReviewerVerificationValue $agentBinding \"generalistModels\" @()) | ForEach-Object { [string]$_ } | Sort-Object)`; compared to $liveModels at line 566",
      "collapseRisk": "Exactly 2 required by schema. If 1 element → qualificationBindingMismatch. `@()` and Sort-Object keep it an array. `($qualModels -join \"|\")` comparison is string-based → safe for 2 elements.",
      "knownEscapeShape": false
    },
    {
      "id": "deliveryDecision/Get-ReviewerGateUniqueReasonCodes return/$seen.ToArray() (with leading `, ` prefix)",
      "stage": "deliveryDecision",
      "contract": "Get-ReviewerGateUniqueReasonCodes return",
      "field": "$seen.ToArray() (with leading `, ` prefix)",
      "kind": "psCollectionReturn",
      "required": "no; LEGALLY EMPTY (no reasons = Ok=true path)",
      "producer": "DeliveryGates.ps1:195 `return , ($seen.ToArray())`",
      "consumer": "DeliveryGates.ps1:582 `.Count -eq 0` test; 636 `.Count -eq 0`; 783 `.Count -eq 0`; 825 `.Count -eq 0`; 1167 `.Count -eq 0`",
      "collapseRisk": "The `, ` prefix forces array-wrapping: even an empty HashSet.ToArray() is returned as a 0-element array, not $null. All callers either test .Count or store in @(…). Safe.",
      "knownEscapeShape": false
    },
    {
      "id": "deliveryDecision/Select-ReviewerGateSubset return/$kept.ToArray() (with leading `, ` prefix)",
      "stage": "deliveryDecision",
      "contract": "Select-ReviewerGateSubset return",
      "field": "$kept.ToArray() (with leading `, ` prefix)",
      "kind": "psCollectionReturn",
      "required": "no; LEGALLY EMPTY when no allowed keys match",
      "producer": "DeliveryGates.ps1:860 `return , ($kept.ToArray())`",
      "consumer": "(wrapper, approved-subset filtering)",
      "collapseRisk": "ESCAPE SHAPE: `, ` prefix forces array. 0-element subset = nothing approved for posting. Safe.",
      "knownEscapeShape": false
    },
    {
      "id": "deliveryDecision/Test-ReviewerGateCandidateEligible in-memory/$changedNormalized (HashSet[string] — changed-path duplicate suppression)",
      "stage": "deliveryDecision",
      "contract": "Test-ReviewerGateCandidateEligible in-memory",
      "field": "$changedNormalized (HashSet[string] — changed-path duplicate suppression)",
      "kind": "hashSet",
      "required": "internal; built from $ChangedPaths param",
      "producer": "DeliveryGates.ps1:760 `$changedNormalized = [System.Collections.Generic.HashSet[string]]::new([StringComparer]::Ordinal)`",
      "consumer": "DeliveryGates.ps1:765 `$changedNormalized.Contains($path)` — membership test for changed-file eligibility",
      "collapseRisk": "ESCAPE SHAPE: empty HashSet (no changed paths) → Contains always false → anchorNotInChangeSet for every changedFile candidate (fail-closed).",
      "knownEscapeShape": false
    },
    {
      "id": "deliveryDecision/Test-ReviewerGateCandidateEligible parameters/$ChangedPaths (string[] parameter)",
      "stage": "deliveryDecision",
      "contract": "Test-ReviewerGateCandidateEligible parameters",
      "field": "$ChangedPaths (string[] parameter)",
      "kind": "jsonArray",
      "required": "no; LEGALLY EMPTY when PR has no changed files",
      "producer": "wrapper / Start-ReviewerAgent.ps1 (builds from live change-set read)",
      "consumer": "DeliveryGates.ps1:761 `foreach ($changedPath in @($ChangedPaths))`",
      "collapseRisk": "ESCAPE SHAPE: empty $ChangedPaths → changedNormalized HashSet empty → all changedFile candidates get anchorNotInChangeSet. PR-level (prMetadata) candidates not affected.",
      "knownEscapeShape": false
    },
    {
      "id": "deliveryDecision/Test-ReviewerGateCandidateEligible parameters/$ThreadFacts (object[] parameter)",
      "stage": "deliveryDecision",
      "contract": "Test-ReviewerGateCandidateEligible parameters",
      "field": "$ThreadFacts (object[] parameter)",
      "kind": "jsonArray",
      "required": "no; LEGALLY EMPTY (no prior review threads)",
      "producer": "wrapper / Start-ReviewerAgent.ps1 (from Get-ReviewerVerificationThreadFacts)",
      "consumer": "DeliveryGates.ps1:770 `foreach ($thread in @($ThreadFacts))`",
      "collapseRisk": "Empty $ThreadFacts → no thread-status checks → no threadStatusUnknown or blockingHumanThreadOpen reasons added. Fail-open on thread state: if threads exist but were not supplied, the gate cannot detect blocking threads.",
      "knownEscapeShape": false
    },
    {
      "id": "deliveryDecision/Test-ReviewerGateVerificationComplete in-memory/$ids (HashSet[string] — accounting duplicate detection)",
      "stage": "deliveryDecision",
      "contract": "Test-ReviewerGateVerificationComplete in-memory",
      "field": "$ids (HashSet[string] — accounting duplicate detection)",
      "kind": "hashSet",
      "required": "internal; starts empty; must end with exactly TotalCandidateCount distinct entries",
      "producer": "DeliveryGates.ps1:807 `$ids = [System.Collections.Generic.HashSet[string]]::new([StringComparer]::Ordinal)`",
      "consumer": "DeliveryGates.ps1:810-815 `.Add($id)` returns false for duplicate → $duplicateOrMissing=true; 822 `$ids.Count -ne $TotalCandidateCount` → candidateAccountingMismatch",
      "collapseRisk": "If $Eligible and $Withheld are both empty but TotalCandidateCount>0 → candidateAccountingMismatch (correct fail-closed). If TotalCandidateCount=0 and both empty → HashSet stays empty, 0=0, OK.",
      "knownEscapeShape": false
    },
    {
      "id": "fingerprints/ExistingFingerprints/$freshFingerprints (from Get-ReviewerExistingFingerprints after posting)",
      "stage": "fingerprints",
      "contract": "ExistingFingerprints",
      "field": "$freshFingerprints (from Get-ReviewerExistingFingerprints after posting)",
      "kind": "hashSet",
      "required": "False",
      "producer": "src/Agents/reviewer/Start-ReviewerAgent.ps1 ~12221 ($freshFingerprints = Get-ReviewerExistingFingerprints -Threads $freshThreads)",
      "consumer": "src/Agents/reviewer/Start-ReviewerAgent.ps1 ~12224 ($freshFingerprints.Contains((Get-ReviewerFindingFingerprint …)))",
      "collapseRisk": "Same Write-Output -NoEnumerate risk. If $freshThreads is 0-length, returns empty HashSet — .Contains() returns $false for all findings → confirmed=0, which triggers a warning but does not throw.",
      "knownEscapeShape": false
    },
    {
      "id": "fingerprints/ExistingFingerprints — threads.comments/$(thread).comments",
      "stage": "fingerprints",
      "contract": "ExistingFingerprints — threads.comments",
      "field": "$(thread).comments",
      "kind": "jsonArray",
      "required": "False",
      "producer": "ADO API response / PR thread data, consumed by Get-ReviewerExistingFingerprints ~4733",
      "consumer": "src/Agents/reviewer/Start-ReviewerAgent.ps1 ~4733 (foreach ($c in @(Get-ReviewerHashValue -Container $t -Key 'comments' -Default @())))",
      "collapseRisk": "KNOWN ESCAPE SHAPE: if the ADO API returns a thread with a single comment, ConvertFrom-Json may give a bare PSCustomObject not an array. The @() wrap at line ~4733 guards this correctly. An empty comments array → @() → 0 iterations → no fingerprints added for that thread (correct for deleted threads).",
      "knownEscapeShape": true
    },
    {
      "id": "fingerprints/ExistingFingerprints (HashSet[string] from Get-ReviewerExistingFingerprints)/$Bound.ExistingFingerprints",
      "stage": "fingerprints",
      "contract": "ExistingFingerprints (HashSet[string] from Get-ReviewerExistingFingerprints)",
      "field": "$Bound.ExistingFingerprints",
      "kind": "hashSet",
      "required": "True",
      "producer": "src/Agents/reviewer/Start-ReviewerAgent.ps1 ~4720-4739 (Get-ReviewerExistingFingerprints; returns via Write-Output -NoEnumerate $set)",
      "consumer": "src/Agents/reviewer/Start-ReviewerAgent.ps1 ~12198 ($ExistingFingerprints.Contains($fingerprint)); ~12212 ([void]$ExistingFingerprints.Add($fingerprint)); ~12221 ($freshFingerprints = Get-ReviewerExistingFingerprints -Threads $freshThreads)",
      "collapseRisk": "KNOWN ESCAPE SHAPE: Get-ReviewerExistingFingerprints uses Write-Output -NoEnumerate $set, which preserves the HashSet object. WITHOUT -NoEnumerate the HashSet would enumerate to individual strings and the call-site assignment would get either $null (0 fingerprints), a single string (1 fingerprint), or an array of strings — all of which would fail .Contains() and .Add() calls. The -NoEnumerate guard is present and co…",
      "knownEscapeShape": true
    },
    {
      "id": "fingerprints/GateApprovalCoverageKey — confirmedImportantOrHigherKeys/$.confirmedImportantOrHigherKeys",
      "stage": "fingerprints",
      "contract": "GateApprovalCoverageKey — confirmedImportantOrHigherKeys",
      "field": "$.confirmedImportantOrHigherKeys",
      "kind": "jsonArray",
      "required": "False",
      "producer": "src/Agents/reviewer/DeliveryGates.ps1 ~1213-1214 (@($ConfirmedImportantOrHigherKeys) | ForEach-Object { [string]$_ } | Sort-Object -Unique)",
      "consumer": "src/Agents/reviewer/DeliveryGates.ps1 ~1205 (canonical SHA input)",
      "collapseRisk": "@($ConfirmedImportantOrHigherKeys) wraps the string[] param. If $null is passed, @($null) = 1-element @($null) → canonicalized as [null] not [] → wrong hash.",
      "knownEscapeShape": false
    },
    {
      "id": "fingerprints/GateApprovalCoverageKey — gateImportantOrHigherKeys/$.gateImportantOrHigherKeys",
      "stage": "fingerprints",
      "contract": "GateApprovalCoverageKey — gateImportantOrHigherKeys",
      "field": "$.gateImportantOrHigherKeys",
      "kind": "jsonArray",
      "required": "False",
      "producer": "src/Agents/reviewer/DeliveryGates.ps1 ~1210-1212 (@(Get-ReviewerVerificationValue $Decision 'gateImportantOrHigherKeys' @()) | ForEach-Object { [string]$_ } | Sort-Object -Unique)",
      "consumer": "src/Agents/reviewer/DeliveryGates.ps1 ~1205 (passed to Get-ReviewerVerificationObjectSha256 as part of ordered hashtable)",
      "collapseRisk": "@() wrap at read. 0-element = no important findings. Sort-Object -Unique on $null (empty array from Where-Object) returns $null — the ForEach-Object chain prevents this by using @() default.",
      "knownEscapeShape": false
    },
    {
      "id": "fingerprints/GateEligibilityFingerprint (from Get-ReviewerGateEligibilityFingerprint)/$PriorRunFingerprintMatches (scalar bool from fingerprint comparison)",
      "stage": "fingerprints",
      "contract": "GateEligibilityFingerprint (from Get-ReviewerGateEligibilityFingerprint)",
      "field": "$PriorRunFingerprintMatches (scalar bool from fingerprint comparison)",
      "kind": "hashSet",
      "required": "False",
      "producer": "src/Agents/reviewer/DeliveryGates.ps1 ~1172 (Get-ReviewerGateEligibilityFingerprint → sha256 string)",
      "consumer": "src/Agents/reviewer/DeliveryGates.ps1 ~1151 (-PriorRunFingerprintMatches: compared gate fingerprint to persisted record)",
      "collapseRisk": "Scalar SHA-256 string comparison, no collection. No collapse risk.",
      "knownEscapeShape": false
    },
    {
      "id": "fingerprints/IntendedFingerprints / ConfirmedFingerprints/$ConfirmedFingerprints",
      "stage": "fingerprints",
      "contract": "IntendedFingerprints / ConfirmedFingerprints",
      "field": "$ConfirmedFingerprints",
      "kind": "hashSet",
      "required": "False",
      "producer": "src/Agents/reviewer/DeliveryGates.ps1 ~1492 (HashSet[string] parameter)",
      "consumer": "src/Agents/reviewer/DeliveryGates.ps1 ~1494 ($ConfirmedFingerprints.Contains($_))",
      "collapseRisk": "KNOWN ESCAPE SHAPE: empty HashSet is a valid object, not $null. If the caller passes $null (no confirmation round done), .Contains() throws NullReferenceException. The param type [System.Collections.Generic.HashSet[string]] does NOT prevent $null in PS without [ValidateNotNull()].",
      "knownEscapeShape": true
    },
    {
      "id": "fingerprints/IntendedFingerprints / ConfirmedFingerprints (from Test-ReviewerGateConfirmedFingerprintCoverage)/$IntendedFingerprints",
      "stage": "fingerprints",
      "contract": "IntendedFingerprints / ConfirmedFingerprints (from Test-ReviewerGateConfirmedFingerprintCoverage)",
      "field": "$IntendedFingerprints",
      "kind": "list",
      "required": "False",
      "producer": "src/Agents/reviewer/DeliveryGates.ps1 ~1491 ([AllowEmptyCollection()][string[]]$IntendedFingerprints param)",
      "consumer": "src/Agents/reviewer/DeliveryGates.ps1 ~1494 (@(@($IntendedFingerprints) | Where-Object { $ConfirmedFingerprints.Contains($_) }).Count); ~1497 (@($IntendedFingerprints).Count)",
      "collapseRisk": "@() wrap on both references. 0-element = nothing to confirm → Complete=$true (vacuous coverage). KNOWN ESCAPE SHAPE: the function evaluates Complete = (posted -eq Intended) — if $IntendedFingerprints is $null (caller passes $null not @()), @($null) gives a 1-element array containing $null, making Count=1 rather than 0, and Complete is false rather than true.",
      "knownEscapeShape": true
    },
    {
      "id": "reconciliation/anchorOutcome (v1 + v2 schema)//rows[*]/anchors[*]/perRunVerdicts",
      "stage": "reconciliation",
      "contract": "anchorOutcome (v1 + v2 schema)",
      "field": "/rows[*]/anchors[*]/perRunVerdicts",
      "kind": "jsonArray",
      "required": "yes; one entry per run → ≥1",
      "producer": "RunReconciliation.ps1:1219 `perRunVerdicts = @($perRun.ToArray())`",
      "consumer": "RunReconciliation.ps1:1547 `(@($anchor.perRunVerdicts)) -join ' / '` (report format); schema: required",
      "collapseRisk": "Safe; 1-element array for single-run case. Serializes correctly. Contains \"unaccounted\" for runs that never mentioned this anchor.",
      "knownEscapeShape": false
    },
    {
      "id": "reconciliation/candidateOutcome v2//candidates[*]/absentInRuns",
      "stage": "reconciliation",
      "contract": "candidateOutcome v2",
      "field": "/candidates[*]/absentInRuns",
      "kind": "jsonArray",
      "required": "yes; LEGALLY EMPTY — when inEveryRun=true (the semanticAgreementTextWithheld path)",
      "producer": "RunReconciliation.ps1:1384 `absentInRuns = @($sortedAbsent)`",
      "consumer": "schema: required (v2)",
      "collapseRisk": "ESCAPE SHAPE: 0-element when candidate present in all runs. A consumer reading this as indicating presence should check inEveryRun=true first.",
      "knownEscapeShape": false
    },
    {
      "id": "reconciliation/candidateOutcome v2//candidates[*]/identityErrors",
      "stage": "reconciliation",
      "contract": "candidateOutcome v2",
      "field": "/candidates[*]/identityErrors",
      "kind": "jsonArray",
      "required": "yes; LEGALLY EMPTY when identity is valid",
      "producer": "RunReconciliation.ps1:1378 `@((Get-ReviewerRunReconciliationValue $identity \"errors\" @()))`; errors list built at ~559-696",
      "consumer": "schema: required; schema allOf: maxItems:0 when disposition=semanticAgreementTextWithheld; minItems:1 when disposition=withheldMalformedSemanticIdentity",
      "collapseRisk": "ESCAPE SHAPE: 0-element is the success path (valid identity). 1+ elements indicate exactly why identity is invalid. The schema constraint makes this a semantic boolean: empty = good, non-empty = bad.",
      "knownEscapeShape": false
    },
    {
      "id": "reconciliation/candidateOutcome v2//candidates[*]/perRunCounts",
      "stage": "reconciliation",
      "contract": "candidateOutcome v2",
      "field": "/candidates[*]/perRunCounts",
      "kind": "jsonArray",
      "required": "yes; one entry per run → ≥1",
      "producer": "RunReconciliation.ps1:1385 `perRunCounts = @($sortedCountRows)` where each row is {replayNonce,count}",
      "consumer": "schema: required (v2)",
      "collapseRisk": "Safe; one entry per run. 0 count = absent. Serializes as array of objects.",
      "knownEscapeShape": false
    },
    {
      "id": "reconciliation/candidateOutcome v2//candidates[*]/presentationVariants",
      "stage": "reconciliation",
      "contract": "candidateOutcome v2",
      "field": "/candidates[*]/presentationVariants",
      "kind": "jsonArray",
      "required": "yes; LEGALLY EMPTY when no run had this candidate (all absent)",
      "producer": "RunReconciliation.ps1:1387 `@($sortedPresentations)` where each entry is {sha256, replayNonce, candidateId, text}",
      "consumer": "schema: required (v2)",
      "collapseRisk": "@() wrapper. 0 variants with disposition=withheldRunDisagreement means some runs had it but the absent runs produced no presentation — safe because runs present in presentInRuns still contributed variants.",
      "knownEscapeShape": false
    },
    {
      "id": "reconciliation/candidateOutcome v2 (reviewer.run-reconciliation.v2)//candidates[*]/presentInRuns",
      "stage": "reconciliation",
      "contract": "candidateOutcome v2 (reviewer.run-reconciliation.v2)",
      "field": "/candidates[*]/presentInRuns",
      "kind": "jsonArray",
      "required": "yes; LEGALLY EMPTY — when candidate absent from every run (withheld case)",
      "producer": "RunReconciliation.ps1:1383 `presentInRuns = @($sortedPresent)`",
      "consumer": "schema: required (v2); report format",
      "collapseRisk": "ESCAPE SHAPE: 0-element when all runs lack this candidate. Combined with absentInRuns having all nonces. @() wrapper safe.",
      "knownEscapeShape": false
    },
    {
      "id": "reconciliation/empty-run manifest (reviewer.run-reconciliation.empty-run)//emptyRunAccounting/withheld",
      "stage": "reconciliation",
      "contract": "empty-run manifest (reviewer.run-reconciliation.empty-run)",
      "field": "/emptyRunAccounting/withheld",
      "kind": "jsonArray",
      "required": "yes; totalCandidateCount must equal withheld.Count (validated at line 435)",
      "producer": "RunReconciliation.ps1:448 `$rows = @($accountedRows.ToArray())`; 458 `withheld = @($rows)`",
      "consumer": "RunReconciliation.ps1:419-435 (reads back $withheldRows to validate accounting)",
      "collapseRisk": "ESCAPE SHAPE: LEGALLY EMPTY when totalCandidateCount=0 (no candidates at all). @() wrapper → always array.",
      "knownEscapeShape": false
    },
    {
      "id": "reconciliation/empty-run manifest (ruleCoverage sub-fields)//ruleCoverage/changedConstructs  /ruleCoverage/changedFileAnchors  /ruleCoverage/constructFiles  /ruleCoverage/remediationFacts",
      "stage": "reconciliation",
      "contract": "empty-run manifest (ruleCoverage sub-fields)",
      "field": "/ruleCoverage/changedConstructs  /ruleCoverage/changedFileAnchors  /ruleCoverage/constructFiles  /ruleCoverage/remediationFacts",
      "kind": "jsonArray",
      "required": "yes in empty-run; all explicitly @()",
      "producer": "RunReconciliation.ps1:492-498 all set to @()",
      "consumer": "RunReconciliation.ps1:335,626,627,761-767 — read via Get-ReviewerRunReconciliationValue with @() default",
      "collapseRisk": "ESCAPE SHAPE: all are ALWAYS @() in empty runs. Reconciler interprets empty changedConstructs as no constructs reviewed = empty binding. An anchor-binding comparison against a non-empty run will show a binding difference.",
      "knownEscapeShape": false
    },
    {
      "id": "reconciliation/Resolve-ReviewerRunReconciliation in-memory (Get-ReviewerRunReconciliationDifference return)/return.OnlyLeft / return.OnlyRight",
      "stage": "reconciliation",
      "contract": "Resolve-ReviewerRunReconciliation in-memory (Get-ReviewerRunReconciliationDifference return)",
      "field": "return.OnlyLeft / return.OnlyRight",
      "kind": "psCollectionReturn",
      "required": "no; LEGALLY EMPTY (no difference = sets equal)",
      "producer": "RunReconciliation.ps1:861 `@{ OnlyLeft = @($onlyLeft.ToArray()); OnlyRight = @($onlyRight.ToArray()) }`",
      "consumer": "(internal; callers check .Count)",
      "collapseRisk": "Both wrapped in @(). 0-element = agreement. Safe.",
      "knownEscapeShape": false
    },
    {
      "id": "reconciliation/Resolve-ReviewerRunReconciliation in-memory HashSet (nonce dedup)/$nonces (HashSet[string] — nonce duplicate detection)",
      "stage": "reconciliation",
      "contract": "Resolve-ReviewerRunReconciliation in-memory HashSet (nonce dedup)",
      "field": "$nonces (HashSet[string] — nonce duplicate detection)",
      "kind": "hashSet",
      "required": "internal; starts empty, should stay a set (all distinct) across runs",
      "producer": "RunReconciliation.ps1:901 `$nonces = [System.Collections.Generic.HashSet[string]]::new([StringComparer]::Ordinal)`",
      "consumer": "RunReconciliation.ps1:920 `$nonces.Add($nonce)` — Add returns false if already present → problem added",
      "collapseRisk": "In-memory only. If $nonces is empty (first run), Add always returns true. If Add returns false (nonce reused), a nonceReused problem is recorded, preventing false reconciliation.",
      "knownEscapeShape": false
    },
    {
      "id": "reconciliation/reviewer.run-reconciliation-set (v1 + v2 schema)//expectedRunSha256",
      "stage": "reconciliation",
      "contract": "reviewer.run-reconciliation-set (v1 + v2 schema)",
      "field": "/expectedRunSha256",
      "kind": "jsonArray",
      "required": "not required; no minItems in schema — schema says \"Empty means the declaration fixes only how many runs will count, which is the weaker form\"",
      "producer": "New-ReviewerGateQualification tool (out of scope)",
      "consumer": "tools/Compare-ReviewerReplayRuns.ps1:226 and :323 (@(Get-ReviewerConventionSpecialistValue $runSet \"expectedRunSha256\" @())); re-emitted at :369",
      "collapseRisk": "ESCAPE SHAPE: empty array is EXPLICITLY legal per schema description. A declaration with expectedRunSha256=[] still seals plannedRunCount but does not bind specific artifacts — weaker integrity guarantee.",
      "knownEscapeShape": false
    },
    {
      "id": "reconciliation/reviewer.run-reconciliation.v1 / v2//candidates",
      "stage": "reconciliation",
      "contract": "reviewer.run-reconciliation.v1 / v2",
      "field": "/candidates",
      "kind": "jsonArray",
      "required": "yes; LEGALLY EMPTY (no candidates found across runs)",
      "producer": "RunReconciliation.ps1:1447 `@($candidates.ToArray())`",
      "consumer": "schema: required; report format reads .Count",
      "collapseRisk": "@() wrapper → array. 0-element = agreedCandidateCount=0 and no convention comment eligible.",
      "knownEscapeShape": false
    },
    {
      "id": "reconciliation/reviewer.run-reconciliation.v1 / v2//problems",
      "stage": "reconciliation",
      "contract": "reviewer.run-reconciliation.v1 / v2",
      "field": "/problems",
      "kind": "jsonArray",
      "required": "yes (schema required); LEGALLY EMPTY (no problems = clean comparison)",
      "producer": "RunReconciliation.ps1:1441 `[string[]](Get-ReviewerRunReconciliationProblemText -Problems $problems)` which returns `, [string[]]@(...)`",
      "consumer": "RunReconciliation.ps1:1518 `if (@($Reconciliation.problems).Count -gt 0)` (report format loop); schema: required",
      "collapseRisk": "Get-ReviewerRunReconciliationProblemText returns `, [string[]]@(...)` — the `, ` prefix forces array wrapping so an empty list returns @() not $null. Safe.",
      "knownEscapeShape": false
    },
    {
      "id": "reconciliation/reviewer.run-reconciliation.v1 / v2//rows",
      "stage": "reconciliation",
      "contract": "reviewer.run-reconciliation.v1 / v2",
      "field": "/rows",
      "kind": "jsonArray",
      "required": "yes; LEGALLY EMPTY (no rules transported = empty rows array)",
      "producer": "RunReconciliation.ps1:1445 `@($rows.ToArray())`",
      "consumer": "RunReconciliation.ps1:1526 `foreach ($row in @($Reconciliation.rows))`; schema: required",
      "collapseRisk": "@() wrapper → always an array. 0-element rows means no rule coverage — reconciliation still valid but all counts are 0.",
      "knownEscapeShape": false
    },
    {
      "id": "reconciliation/reviewer.run-reconciliation.v1 / v2//runs",
      "stage": "reconciliation",
      "contract": "reviewer.run-reconciliation.v1 / v2",
      "field": "/runs",
      "kind": "jsonArray",
      "required": "yes; minimum 1 run (throws if 0)",
      "producer": "RunReconciliation.ps1:1442 `@($runSummaries.ToArray())`",
      "consumer": "RunReconciliation.ps1:1535 report format; schema: required",
      "collapseRisk": "Always ≥1 entry. Wrapped in @(). Safe.",
      "knownEscapeShape": false
    },
    {
      "id": "reconciliation/ruleCoverage (specialist/empty-run manifests)//ruleCoverage/{missing,duplicates,unknown,unaccountedCandidates}",
      "stage": "reconciliation",
      "contract": "ruleCoverage (specialist/empty-run manifests)",
      "field": "/ruleCoverage/{missing,duplicates,unknown,unaccountedCandidates}",
      "kind": "jsonArray",
      "required": "yes in empty-run; LEGALLY EMPTY (no holes = clean accounting)",
      "producer": "specialist manifest / RunReconciliation.ps1:493-499 (all set to @() in empty-run)",
      "consumer": "RunReconciliation.ps1:945 `$ids = @(Get-ReviewerRunReconciliationValue $coverage $hole.Field @())` — .Count > 0 triggers a problem",
      "collapseRisk": "@() default prevents collapse. Non-empty triggers reconciliation problem. All four can be empty legitimately.",
      "knownEscapeShape": false
    },
    {
      "id": "reconciliation/ruleCoverage (specialist/empty-run manifests)//ruleCoverage/rows",
      "stage": "reconciliation",
      "contract": "ruleCoverage (specialist/empty-run manifests)",
      "field": "/ruleCoverage/rows",
      "kind": "jsonArray",
      "required": "yes in empty-run (explicitly @()); no minItems otherwise",
      "producer": "specialist manifest / RunReconciliation.ps1:490 `rows = @()` for empty-run",
      "consumer": "RunReconciliation.ps1:1021 `foreach ($row in @(Get-ReviewerRunReconciliationValue $coverage \"rows\" @()))`; 674 `@(@(Get-ReviewerRunReconciliationValue $coverage \"rows\" @()) | Where-Object {…})`",
      "collapseRisk": "ESCAPE SHAPE: always @() in empty-run. Reconciler matches rows by ruleRef across runs — missing row = absent = disagreement.",
      "knownEscapeShape": false
    },
    {
      "id": "reconciliation/ruleOutcome (v1 + v2 schema)//rows[*]/anchors",
      "stage": "reconciliation",
      "contract": "ruleOutcome (v1 + v2 schema)",
      "field": "/rows[*]/anchors",
      "kind": "jsonArray",
      "required": "yes; LEGALLY EMPTY (rule accounted for with no construct anchors)",
      "producer": "RunReconciliation.ps1:1253 `@($anchorRows.ToArray())`",
      "consumer": "RunReconciliation.ps1:1534 `@($row.anchors).Count`; 1546 `foreach ($anchor in @($unsettled | Select-Object -First 24))`; schema: required",
      "collapseRisk": "@() wrapper. 0 anchors → no settled violations → reconciledStatus may be notApplicable or unknown. Schema allows empty.",
      "knownEscapeShape": false
    },
    {
      "id": "reconciliation/ruleOutcome (v1 + v2 schema)//rows[*]/disagreements",
      "stage": "reconciliation",
      "contract": "ruleOutcome (v1 + v2 schema)",
      "field": "/rows[*]/disagreements",
      "kind": "jsonArray",
      "required": "yes; LEGALLY EMPTY (no disagreements = stable row)",
      "producer": "RunReconciliation.ps1:1251 `@($disagreements.ToArray())`",
      "consumer": "RunReconciliation.ps1:1547 `foreach ($disagreement in @($row.disagreements))`; schema: required",
      "collapseRisk": "@() wrapper → array. 0-element = stable status. Safe.",
      "knownEscapeShape": false
    },
    {
      "id": "reconciliation/ruleOutcome (v1 + v2 schema)//rows[*]/notInReachConstructs",
      "stage": "reconciliation",
      "contract": "ruleOutcome (v1 + v2 schema)",
      "field": "/rows[*]/notInReachConstructs",
      "kind": "jsonArray",
      "required": "yes; LEGALLY EMPTY when no not-in-reach anchors",
      "producer": "RunReconciliation.ps1:1258 `@($(if ($stable) { $normalizedNotInReach.ToArray() } else { @() }))`",
      "consumer": "schema: required; report format",
      "collapseRisk": "ESCAPE SHAPE: @() when unstable. Same pattern as violatingConstructs.",
      "knownEscapeShape": false
    },
    {
      "id": "reconciliation/ruleOutcome (v1 + v2 schema)//rows[*]/rawStatuses",
      "stage": "reconciliation",
      "contract": "ruleOutcome (v1 + v2 schema)",
      "field": "/rows[*]/rawStatuses",
      "kind": "jsonArray",
      "required": "yes; one element per run → ≥1",
      "producer": "RunReconciliation.ps1:1250 `@($rawStatuses.ToArray())`",
      "consumer": "RunReconciliation.ps1:1530 `(@($row.rawStatuses)) -join ','` (report format); schema: required",
      "collapseRisk": "Safe; one entry added per run in the loop. Single-run case: 1-element array serialized correctly.",
      "knownEscapeShape": false
    },
    {
      "id": "reconciliation/ruleOutcome (v1 + v2 schema)//rows[*]/readings",
      "stage": "reconciliation",
      "contract": "ruleOutcome (v1 + v2 schema)",
      "field": "/rows[*]/readings",
      "kind": "jsonArray",
      "required": "yes; one entry per run → ≥1",
      "producer": "RunReconciliation.ps1:1252 `@($readings.ToArray())`",
      "consumer": "schema: required (type array, no items constraint); report format",
      "collapseRisk": "Safe; one reading added per run.",
      "knownEscapeShape": false
    },
    {
      "id": "reconciliation/ruleOutcome (v1 + v2 schema)//rows[*]/violatingConstructs",
      "stage": "reconciliation",
      "contract": "ruleOutcome (v1 + v2 schema)",
      "field": "/rows[*]/violatingConstructs",
      "kind": "jsonArray",
      "required": "yes; LEGALLY EMPTY — schema doc: \"Empty unless the row is stable. An unstable row never carries forward the anchors of whichever run found something.\"",
      "producer": "RunReconciliation.ps1:1254 `@($(if ($stable) { $normalizedViolating.ToArray() } else { @() }))`; set to @() explicitly on unreconciled reset at line ~1410",
      "consumer": "RunReconciliation.ps1:1531 `if (@($row.violatingConstructs).Count -gt 0)`; schema: required",
      "collapseRisk": "ESCAPE SHAPE: explicitly @() when unstable OR when all anchors are compliant/notInReach. A consumer that reads this as a non-empty set and acts on violation constructs without checking `.stable` first is unsafe.",
      "knownEscapeShape": false
    },
    {
      "id": "reconciliation/ruleOutcome (v2 schema only)//rows[*]/violatingChangedFileTargets",
      "stage": "reconciliation",
      "contract": "ruleOutcome (v2 schema only)",
      "field": "/rows[*]/violatingChangedFileTargets",
      "kind": "jsonArray",
      "required": "yes in v2; LEGALLY EMPTY — same rule as violatingConstructs",
      "producer": "RunReconciliation.ps1:1255-1258 `@($(if ($stable) { $normalizedViolatingChangedFileTargets.ToArray() } else { @() }))`",
      "consumer": "RunReconciliation.ps1:1136-1148 (anchorVerdicts population using cf* ids); schema v2: required",
      "collapseRisk": "ESCAPE SHAPE: explicitly @() when unstable. v1 schema does not have this field — cross-version consumers must tolerate its absence.",
      "knownEscapeShape": false
    },
    {
      "id": "reconciliation/semantic-candidate identity v2//evidence/factIds",
      "stage": "reconciliation",
      "contract": "semantic-candidate identity v2",
      "field": "/evidence/factIds",
      "kind": "jsonArray",
      "required": "yes field; LEGALLY EMPTY for changedFile anchor; required ≥1 for prMetadata",
      "producer": "RunReconciliation.ps1:788 `factIds = @($factIds)` where $factIds comes from sorted unique strings parsed from comma-separated field",
      "consumer": "v2 schema: minItems:1 for prMetadata anchor (allOf conditional); validated at RunReconciliation.ps1:713",
      "collapseRisk": "ESCAPE SHAPE: changedFile → @(); prMetadata → must have ≥1. A consumer treating factIds as optional for prMetadata would miss the required evidence.",
      "knownEscapeShape": false
    },
    {
      "id": "reconciliation/semantic-candidate identity v2//evidence/violations",
      "stage": "reconciliation",
      "contract": "semantic-candidate identity v2",
      "field": "/evidence/violations",
      "kind": "jsonArray",
      "required": "yes field; LEGALLY EMPTY for changedFile+sealed RawSpan and required empty for prMetadata",
      "producer": "RunReconciliation.ps1:789 `violations = @($violationSet.ToArray())`",
      "consumer": "v2 schema: maxItems:0 for prMetadata (empty required); LEGALLY EMPTY for changedFile+sealed RawSpan (allOf note)",
      "collapseRisk": "ESCAPE SHAPE: 0-element is valid in two distinct structural cases (changedFile+RawSpan, prMetadata). Consumers distinguishing these must check anchorKind.",
      "knownEscapeShape": false
    },
    {
      "id": "reconciliation/semantic-candidate identity v2//manifestations",
      "stage": "reconciliation",
      "contract": "semantic-candidate identity v2",
      "field": "/manifestations",
      "kind": "jsonArray",
      "required": "yes; minItems:1 (schema enforced; also code-validated at line 643 `if (@($primaryTarget.Targets).Count -ne 1 -or @($manifestations.Targets).Count -lt 1)`)",
      "producer": "RunReconciliation.ps1:784 `manifestations = @($manifestations.Targets)`",
      "consumer": "v2 schema: minItems:1, maxItems:32; validated at RunReconciliation.ps1:643",
      "collapseRisk": "Cannot be empty if validation passes. Singleton array (1 element) is the minimum for changedFile anchor.",
      "knownEscapeShape": false
    },
    {
      "id": "reconciliation/semantic-candidate identity v2//remediation/changedCodeFix/evidenceFactIds",
      "stage": "reconciliation",
      "contract": "semantic-candidate identity v2",
      "field": "/remediation/changedCodeFix/evidenceFactIds",
      "kind": "jsonArray",
      "required": "yes field; LEGALLY EMPTY when valueSource=authoritativeRule (validated at line 607: `($valueSource -ceq \"authoritativeRule\" -and $changedEvidenceText)` → error…",
      "producer": "RunReconciliation.ps1:795 `evidenceFactIds = @($changedEvidenceFactIds)` where $changedEvidenceFactIds parsed from comma-separated string",
      "consumer": "v2 schema: maxItems:8; validated at RunReconciliation.ps1:613-616",
      "collapseRisk": "ESCAPE SHAPE: @() when valueSource=authoritativeRule. Non-empty only when valueSource=deterministicFact.",
      "knownEscapeShape": false
    },
    {
      "id": "reconciliation/semantic-candidate identity v2//remediation/changedCodeFix/targets",
      "stage": "reconciliation",
      "contract": "semantic-candidate identity v2",
      "field": "/remediation/changedCodeFix/targets",
      "kind": "jsonArray",
      "required": "yes; minItems:1",
      "producer": "RunReconciliation.ps1:793 `targets = @($remediation.Targets)`",
      "consumer": "v2 schema: minItems:1; for prMetadata: exactly 1 element with target=\"prMetadata\"",
      "collapseRisk": "Validated at runtime; cannot be empty if identity is valid.",
      "knownEscapeShape": false
    },
    {
      "id": "reconciliation/semantic-candidate identity v2 (reviewer.semantic-candidate)//anchor/constructs",
      "stage": "reconciliation",
      "contract": "semantic-candidate identity v2 (reviewer.semantic-candidate)",
      "field": "/anchor/constructs",
      "kind": "jsonArray",
      "required": "yes field; LEGALLY EMPTY for changedFile+sealed-RawSpan anchor",
      "producer": "RunReconciliation.ps1:783 `constructs = @($anchorConstructs.ToArray())`",
      "consumer": "v2 schema: /definitions/semanticCandidateIdentity/anchor/constructs — array, no minItems globally but minItems implicitly forced by changedFile allOf (\"Empty only when the exact line is bound by a sealed changed-file RawSpan\")",
      "collapseRisk": "ESCAPE SHAPE: 0-element is valid for sealed RawSpan case. A consumer checking constructs.Count > 0 as a proxy for 'violation confirmed' would be incorrect for this path.",
      "knownEscapeShape": false
    },
    {
      "id": "snapshot/corpus-index-internal/$payloads (PS array from corpus-index.json payloads field)",
      "stage": "snapshot",
      "contract": "corpus-index-internal",
      "field": "$payloads (PS array from corpus-index.json payloads field)",
      "kind": "psCollectionReturn",
      "required": "required: yes (in corpus index); empty: NO (payloadCount ≥ 1 from schema)",
      "producer": "src/Agents/reviewer/CorpusSeal.ps1:535 — $payloads = @(Get-ReviewerCorpusSealProperty -Object $index -Name 'payloads' -Type array)",
      "consumer": "src/Agents/reviewer/CorpusSeal.ps1:536 — if ($payloads.Count -ne $declaredCount) count check; line 541 — foreach ($entry in $payloads) to build $byPath Dictionary",
      "collapseRisk": "@(Get-ReviewerCorpusSealProperty ... -Type array) — Get-ReviewerCorpusSealProperty at line 476 already enforces [System.Object[]] type and returns @($value). So $payloads is already [object[]] before @() wrapping. A 1-payload corpus: ConvertFrom-Json at line 526 gives PSCustomObject for the payloads field, Get-ReviewerCorpusSealProperty -Type array at line 477 throws 'must be an array'. This is the same single-eleme…",
      "knownEscapeShape": false
    },
    {
      "id": "snapshot/corpus-index-internal/Import-ReviewerCorpusIndex return value — .Payloads (Dictionary[string,object])",
      "stage": "snapshot",
      "contract": "corpus-index-internal",
      "field": "Import-ReviewerCorpusIndex return value — .Payloads (Dictionary[string,object])",
      "kind": "jsonObjectMap",
      "required": "required: n/a; empty is legal if payloadCount=0 (schema says Min:1 so actually impossible)",
      "producer": "src/Agents/reviewer/CorpusSeal.ps1:539 — $byPath = [Dictionary[string,object]] Ordinal; line 554-558 — byPath[$path] = pscustomobject with path/sha256/length; line 577-589 — return @{ Payloads = $byPath; ... }",
      "consumer": "src/Agents/reviewer/CorpusSeal.ps1:608 — $Index.Payloads.ContainsKey($Path) membership; line 611 — $entry = $Index.Payloads[$Path]",
      "collapseRisk": "Dictionary — no collapse. ContainsKey and indexing are type-safe. The key risk is case-sensitivity: OrdinalIgnoreCase folding is separately tracked by $foldedSeen at line 540 to prevent case-variant path aliasing.",
      "knownEscapeShape": false
    },
    {
      "id": "snapshot/corpus-seal-internal/$authoritativePaths (PS in-memory, CorpusSeal.ps1)",
      "stage": "snapshot",
      "contract": "corpus-seal-internal",
      "field": "$authoritativePaths (PS in-memory, CorpusSeal.ps1)",
      "kind": "psCollectionReturn",
      "required": "required: n/a; empty means a PR with 0 changed files (refused by census check if authoritativeChangedPathCount > 0)",
      "producer": "src/Agents/reviewer/CorpusSeal.ps1:1044 — $authoritativeList = [List[string]]::new(); line 1058 — $authoritativePaths = @($authoritativeList.ToArray())",
      "consumer": "src/Agents/reviewer/CorpusSeal.ps1:1059 — .Count comparison; line 1062 — for loop with $digestOrder; line 1073 — foreach; lines 1094,1095,1122,1207 — -cnotcontains membership; line 1438-1441 — foreach in source transport plan; line 1483 — Census.digestOrder =…",
      "collapseRisk": "@($authoritativeList.ToArray()) for empty List gives @(). For 1 path: ToArray() gives [string[]]{'path'}, @() wrapping is redundant but correct. The -cnotcontains membership test is O(n) for the PS array — for large change sets this could be slow, but correctness is maintained. Serialized as [string[]]@($authoritativePaths) in Census.digestOrder — preserves array shape.",
      "knownEscapeShape": false
    },
    {
      "id": "snapshot/corpus-seal-internal/$authoritativeRightHand (HashSet[string] in New-ReviewerCorpusSealPlan)",
      "stage": "snapshot",
      "contract": "corpus-seal-internal",
      "field": "$authoritativeRightHand (HashSet[string] in New-ReviewerCorpusSealPlan)",
      "kind": "hashSet",
      "required": "required: n/a; empty is valid for all-delete PRs",
      "producer": "src/Agents/reviewer/CorpusSeal.ps1:1072 — $authoritativeRightHand = [System.Collections.Generic.HashSet[string]]::new([StringComparer]::Ordinal); line 1078 — [void]$authoritativeRightHand.Add($path) for each right-hand-…",
      "consumer": "src/Agents/reviewer/CorpusSeal.ps1:1077 — Test-ReviewerSourceChangeCarriesRightHand gate; line 1100 — if (-not $authoritativeSpansByPath.Contains($path)): span evidence cross-check; line 1143 — if (-not $authoritativeRightHand.Contains($path)) refuse; line 12…",
      "collapseRisk": "HashSet.Contains() on an empty HashSet correctly returns $false — no collapse. The @($authoritativeRightHand | Where-Object {...}) at line 1239 iterates the HashSet's IEnumerable — produces [object[]] even for 0 elements. Key risk: if this HashSet were accidentally returned bare from a function, PowerShell would enumerate it in the pipeline, producing individual strings that would then be implicitly @()ed by a calle…",
      "knownEscapeShape": false
    },
    {
      "id": "snapshot/corpus-seal-internal/$authoritativeSeen / $requestKeys / $payloadFiles / $contestedPaths (HashSet[string] membership checks in CorpusSeal.ps1)",
      "stage": "snapshot",
      "contract": "corpus-seal-internal",
      "field": "$authoritativeSeen / $requestKeys / $payloadFiles / $contestedPaths (HashSet[string] membership checks in CorpusSeal.ps1)",
      "kind": "hashSet",
      "required": "required: n/a — all used as deduplication membership sets",
      "producer": "src/Agents/reviewer/CorpusSeal.ps1:1045 — $authoritativeSeen = [HashSet[string]] Ordinal; line 1272 — $requestKeys = [HashSet[string]] Ordinal; line 1273 — $payloadFiles = [HashSet[string]] OrdinalIgnoreCase; SourceTran…",
      "consumer": "$authoritativeSeen: line 1052 — .Add() returns false for duplicates; $requestKeys: line 1326 — if (-not $requestKeys.Add($key.Key)) duplicate detection; $payloadFiles: line 1296 — if (-not $payloadFiles.Add($payloadFile)) duplicate detection; $contestedPaths:…",
      "collapseRisk": "These sets are NEVER returned bare from functions — they are local to their function scope. No cross-boundary collapse risk. Key risk if any were ever refactored to return: bare HashSet enumeration in PS pipeline would unroll elements, losing set semantics.",
      "knownEscapeShape": false
    },
    {
      "id": "snapshot/corpus-seal-internal/$noRightHandPaths (PS in-memory, CorpusSeal.ps1)",
      "stage": "snapshot",
      "contract": "corpus-seal-internal",
      "field": "$noRightHandPaths (PS in-memory, CorpusSeal.ps1)",
      "kind": "psCollectionReturn",
      "required": "required: n/a; empty is legal (all paths have right-hand content)",
      "producer": "src/Agents/reviewer/CorpusSeal.ps1:1199 — $noRightHandPaths = @(Get-ReviewerCorpusSealProperty ... -Type array | ForEach-Object { [string]$_ })",
      "consumer": "src/Agents/reviewer/CorpusSeal.ps1:1207 — foreach ($path in $noRightHandPaths); line 1481 — Census.noRightHandPaths = [string[]]@($noRightHandPaths)",
      "collapseRisk": "The @() wrapping at line 1199 ensures a 0-element result gives @() and a 1-element JSON string gives a 1-element PS array. [string[]]@($noRightHandPaths) at line 1481 preserves the array type in the census hashtable. When later serialized in the sidecar, [string[]]@() serializes as [] in JSON — correct.",
      "knownEscapeShape": false
    },
    {
      "id": "snapshot/corpus-seal-internal/$Plan.Evidence.siblings / rules / threads / facts (arrays in plan hashtable)",
      "stage": "snapshot",
      "contract": "corpus-seal-internal",
      "field": "$Plan.Evidence.siblings / rules / threads / facts (arrays in plan hashtable)",
      "kind": "psCollectionReturn",
      "required": "required: n/a; empty lists are common (no siblings evidence for a simple PR)",
      "producer": "src/Agents/reviewer/CorpusSeal.ps1:1263 — $evidenceRecord[$class] = @($resolved.ToArray()) where $resolved is List[object]",
      "consumer": "src/Agents/reviewer/CorpusSeal.ps1:1931-1934 — [string[]]@($Plan.Evidence.siblings | ForEach-Object { [string]$_.Path }); sidecar evidence field for each class",
      "collapseRisk": "@($resolved.ToArray()) for empty List gives @(), serialized as [] — correct. The ForEach-Object on @() produces @() — correct [string[]] in sidecar. For 1 evidence payload: @($resolved.ToArray()) gives [object[]] with 1 element; ForEach-Object produces 1-element [string[]] — correct. No collapse risk.",
      "knownEscapeShape": false
    },
    {
      "id": "snapshot/corpus-seal-internal/$plannedResources (List[object] in New-ReviewerCorpusSealPlan)",
      "stage": "snapshot",
      "contract": "corpus-seal-internal",
      "field": "$plannedResources (List[object] in New-ReviewerCorpusSealPlan)",
      "kind": "list",
      "required": "required: n/a",
      "producer": "src/Agents/reviewer/CorpusSeal.ps1:1271 — $plannedResources = [System.Collections.Generic.List[object]]::new(); lines 1276-1344 — [void]$plannedResources.Add(...) per resource",
      "consumer": "src/Agents/reviewer/CorpusSeal.ps1:1465 — Resources = @($plannedResources.ToArray()); line 1813-1849 in Save-ReviewerCorpusSeal — foreach ($resource in $Plan.Resources)",
      "collapseRisk": ".ToArray() on a List always returns [object[]], never a scalar — no collapse. Plan.Resources = @($plannedResources.ToArray()) adds a second @() that is redundant but harmless. In Save-ReviewerCorpusSeal, foreach over $Plan.Resources (already [object[]]) is safe.",
      "knownEscapeShape": false
    },
    {
      "id": "snapshot/corpus-seal-internal/$script:ReviewerCorpusIndexKinds",
      "stage": "snapshot",
      "contract": "corpus-seal-internal",
      "field": "$script:ReviewerCorpusIndexKinds",
      "kind": "psCollectionReturn",
      "required": "required: n/a",
      "producer": "src/Agents/reviewer/CorpusSeal.ps1:41-43 — @('private-immutable-non-promotable-research-corpus','private-non-promotable-research-corpus')",
      "consumer": "src/Agents/reviewer/CorpusSeal.ps1:530 — if ($script:ReviewerCorpusIndexKinds -cnotcontains $kind) validation",
      "collapseRisk": "2-element constant; no collapse. -cnotcontains is correct.",
      "knownEscapeShape": false
    },
    {
      "id": "snapshot/corpus-seal-internal/$script:ReviewerCorpusSealEnvelopes / $script:ReviewerCorpusSealTransportModes / $script:ReviewerCorpusSealSourceKinds",
      "stage": "snapshot",
      "contract": "corpus-seal-internal",
      "field": "$script:ReviewerCorpusSealEnvelopes / $script:ReviewerCorpusSealTransportModes / $script:ReviewerCorpusSealSourceKinds",
      "kind": "psCollectionReturn",
      "required": "required: n/a",
      "producer": "src/Agents/reviewer/CorpusSeal.ps1:54-56 — @('jsonRpcResult','mcpTextContent','mcpResourceContent'); @('mcpFlat','azureDevOpsCliFallback','legacyMcp'); @('capturedArtifact','derivedFromCorpus')",
      "consumer": "src/Agents/reviewer/CorpusSeal.ps1:1289 — $script:ReviewerCorpusSealEnvelopes -cnotcontains $envelope; line 1627-1632 — kind/mode validation",
      "collapseRisk": "Small constants; no collapse. -cnotcontains is correct for arrays.",
      "knownEscapeShape": false
    },
    {
      "id": "snapshot/corpus-seal-internal/$script:ReviewerCorpusSealReservedFiles",
      "stage": "snapshot",
      "contract": "corpus-seal-internal",
      "field": "$script:ReviewerCorpusSealReservedFiles",
      "kind": "psCollectionReturn",
      "required": "required: n/a",
      "producer": "src/Agents/reviewer/CorpusSeal.ps1:64-66 — @('manifest.json','offline-corpus-seal.json')",
      "consumer": "src/Agents/reviewer/CorpusSeal.ps1:399 — foreach ($reserved in $script:ReviewerCorpusSealReservedFiles) in Assert-ReviewerCorpusSealOutputPaths; line 412 — foreach again to add reserved names to $byPath",
      "collapseRisk": "2-element constant; no collapse. foreach over it is always correct.",
      "knownEscapeShape": false
    },
    {
      "id": "snapshot/corpus-seal-internal/Get-ReviewerCorpusSealSpanEvidence return value (OrderedDictionary path→@spans)",
      "stage": "snapshot",
      "contract": "corpus-seal-internal",
      "field": "Get-ReviewerCorpusSealSpanEvidence return value (OrderedDictionary path→@spans)",
      "kind": "jsonObjectMap",
      "required": "required: n/a; empty map for all-delete change sets",
      "producer": "src/Agents/reviewer/CorpusSeal.ps1:313 — $byPath = [OrderedDictionary] Ordinal; line 369 — $byPath[$path] = @($spans.ToArray()) where $spans is List[object]; return $byPath",
      "consumer": "src/Agents/reviewer/CorpusSeal.ps1:1092 — $authoritativeSpansByPath = Get-ReviewerCorpusSealSpanEvidence; line 1094 — foreach ($evidencePath in @($authoritativeSpansByPath.Keys)); line 1100,1101 — -not $authoritativeSpansByPath.Contains($path); line 1160 — $d…",
      "collapseRisk": "Each value is @($spans.ToArray()) — correct for 0 or 1 spans. $derivedSpans = @($authoritativeSpansByPath[$path]) at line 1160 adds a second @() — safe. A path not in the map gives $null; the .Contains() check at lines 1100-1101 guards this.",
      "knownEscapeShape": false
    },
    {
      "id": "snapshot/corpus-seal-sidecar/$.evidence.siblings / $.evidence.rules / $.evidence.threads / $.evidence.facts (in sidecar)",
      "stage": "snapshot",
      "contract": "corpus-seal-sidecar",
      "field": "$.evidence.siblings / $.evidence.rules / $.evidence.threads / $.evidence.facts (in sidecar)",
      "kind": "jsonArray",
      "required": "required: sidecar evidence object is required; each list: empty [] is legal",
      "producer": "src/Agents/reviewer/CorpusSeal.ps1:1931-1934 — evidence = ordered @ { siblings = [string[]]@($Plan.Evidence.siblings | ForEach-Object {[string]$_.Path}); rules = ...; threads = ...; facts = ... }",
      "consumer": "Offline corpus seal loader (not in session scope) reads sidecar evidence for audit/verification of what was included",
      "collapseRisk": "[string[]]@() serializes as [] — correct. [string[]]@('path') serializes as ['path'] — correct. The canonical JSON renderer (ConvertTo-AgentReplayCanonicalJson) must preserve array shape for 0 and 1 element cases.",
      "knownEscapeShape": false
    },
    {
      "id": "snapshot/corpus-seal-sidecar/$.sourceTransport.gateReasonCodes (in offline-corpus-seal.json sidecar)",
      "stage": "snapshot",
      "contract": "corpus-seal-sidecar",
      "field": "$.sourceTransport.gateReasonCodes (in offline-corpus-seal.json sidecar)",
      "kind": "jsonArray",
      "required": "required: yes (in sourceTransport sidecar object); empty: YES — empty means gate passed",
      "producer": "src/Agents/reviewer/CorpusSeal.ps1:1926 — gateReasonCodes = [string[]]@($Plan.SourceTransport.GateReasonCodes); GateReasonCodes is from $actualReasons = [string[]]@(@($gate.reasonCodes) | ...)",
      "consumer": "Replay loader verification of non-promotability; also compared against expected.gateReasonCodes from recipe at CorpusSeal.ps1:1753-1754",
      "collapseRisk": "[string[]]@() for empty — ConvertTo-AgentReplayCanonicalJson renders as [] — correct. For a 1-reason code: [string[]]@('reason') renders as ['reason'] — correct. The comparison at line 1754 '($expectedReasons -join) -cne ($actualReasons -join)' is order-sensitive. If gate.reasonCodes came back as a bare string (not [string[]]) from Import-ReviewerSourceTransportReplayArtifact, the @(@($gate.reasonCodes) | ...) at li…",
      "knownEscapeShape": false
    },
    {
      "id": "snapshot/offline-corpus-seal-recipe.v1/$.bindings.models",
      "stage": "snapshot",
      "contract": "offline-corpus-seal-recipe.v1",
      "field": "$.bindings.models",
      "kind": "jsonArray",
      "required": "required: parent object is required; models field has no minItems — empty array is legal",
      "producer": "src/Agents/reviewer/schemas/reviewer.offline-corpus-seal-recipe.v1.json:93-97 (type:array, maxItems:8, items:string pattern; NOT required individually but required by parent bindings object)",
      "consumer": "src/Agents/reviewer/CorpusSeal.ps1:995 — $models = @(Get-ReviewerCorpusSealProperty ... -Type array); line 996-999 — foreach ($model in $models) validation; line 1006 — @($models | ForEach-Object { [string]$_ }); CorpusSeal.ps1:1881 — manifest: models = @($Pl…",
      "collapseRisk": "Get-ReviewerCorpusSealProperty at line 477-480 enforces '$value -isnot [System.Object[]]' fails. A 0-model binding is legal but semantically unusual. A 1-model array after ConvertFrom-Json without -AsHashtable: Get-ReviewerCorpusSealProperty -Type array checks '$value -isnot [System.Object[]]' — a single-element JSON array deserializes as a bare string in ConvertFrom-Json, so the type check would THROW, forcing the…",
      "knownEscapeShape": false
    },
    {
      "id": "snapshot/offline-corpus-seal-recipe.v1/$.changedFiles[*]",
      "stage": "snapshot",
      "contract": "offline-corpus-seal-recipe.v1",
      "field": "$.changedFiles[*]",
      "kind": "jsonArray",
      "required": "required: yes (the field is required per the schema required array at line 8-12); empty: YES",
      "producer": "src/Agents/reviewer/schemas/reviewer.offline-corpus-seal-recipe.v1.json:130-166 (type:array, items:object with required path/changeKinds/rightHand/spans)",
      "consumer": "src/Agents/reviewer/CorpusSeal.ps1:1108 — $changedFiles = @(Get-ReviewerCorpusSealProperty ... -Type array); line 1112 — foreach ($file in $changedFiles)",
      "collapseRisk": "A 1-file PR's changedFiles is a single JSON object after ConvertFrom-Json — Get-ReviewerCorpusSealProperty -Type array fails. Recipe authors must produce multi-element JSON arrays or use explicit array tagging. Empty changedFiles + non-zero census would be caught at line 1201.",
      "knownEscapeShape": false
    },
    {
      "id": "snapshot/offline-corpus-seal-recipe.v1/$.changedFiles[*].changeKinds",
      "stage": "snapshot",
      "contract": "offline-corpus-seal-recipe.v1",
      "field": "$.changedFiles[*].changeKinds",
      "kind": "jsonArray",
      "required": "required: yes (in changedFile item); empty: YES",
      "producer": "src/Agents/reviewer/schemas/reviewer.offline-corpus-seal-recipe.v1.json:143-146 (type:array, items:string pattern)",
      "consumer": "src/Agents/reviewer/CorpusSeal.ps1:1125 — $kinds = @(Get-ReviewerCorpusSealProperty ... -Type array) then foreach; line 1136 — $declaredKinds = [string[]]@(@($kinds | ForEach-Object {[string]$_}) | Sort-Object -CaseSensitive -Unique)",
      "collapseRisk": "A single change kind (e.g., ['edit']) collapses to a bare string after ConvertFrom-Json. Get-ReviewerCorpusSealProperty -Type array throws. Comparison at line 1139 '($declaredKinds -join)' would produce wrong results if it ever received a bare string instead of [string[]].",
      "knownEscapeShape": false
    },
    {
      "id": "snapshot/offline-corpus-seal-recipe.v1/$.changedFiles[*].spans",
      "stage": "snapshot",
      "contract": "offline-corpus-seal-recipe.v1",
      "field": "$.changedFiles[*].spans",
      "kind": "jsonArray",
      "required": "required: yes; empty: NO (minItems:1)",
      "producer": "src/Agents/reviewer/schemas/reviewer.offline-corpus-seal-recipe.v1.json:150-164 (type:array, minItems:1, required)",
      "consumer": "src/Agents/reviewer/CorpusSeal.ps1:1152 — $spans = @(Get-ReviewerCorpusSealProperty ... -Type array); line 1165 — $spans.Count compared to $derivedSpans.Count; line 1171 — for ($spanIndex = 0; $spanIndex -lt $spans.Count; $spanIndex++)",
      "collapseRisk": "A single-span file after ConvertFrom-Json: the spans array becomes a single PSCustomObject — Get-ReviewerCorpusSealProperty -Type array throws, so recipe validation fails for single-hunk files. The recipe schema enforces minItems:1, so empty [] is never legal — but the collapse to scalar for 1-item is the real risk.",
      "knownEscapeShape": false
    },
    {
      "id": "snapshot/offline-corpus-seal-recipe.v1/$.changeSet.digestOrder",
      "stage": "snapshot",
      "contract": "offline-corpus-seal-recipe.v1",
      "field": "$.changeSet.digestOrder",
      "kind": "jsonArray",
      "required": "required: parent changeSet object is required, field itself no explicit required; empty array legal",
      "producer": "src/Agents/reviewer/schemas/reviewer.offline-corpus-seal-recipe.v1.json:124-127 (type:array, items:repositoryPath; no minItems, no required)",
      "consumer": "src/Agents/reviewer/CorpusSeal.ps1:1036 — $digestOrder = @(Get-ReviewerCorpusSealProperty ... -Type array); line 1062-1065 — compared element-by-element with $authoritativePaths",
      "collapseRisk": "A 1-path change set after ConvertFrom-Json: digestOrder becomes a bare string, not [string[]]. Get-ReviewerCorpusSealProperty -Type array at line 476 checks '$value -isnot [System.Object[]]' and throws. This means a seal recipe for a single-file PR would fail schema enforcement unless the JSON was produced with ConvertTo-Json -AsArray or the round-trip preserved array shape.",
      "knownEscapeShape": false
    },
    {
      "id": "snapshot/offline-corpus-seal-recipe.v1/$.evidence.siblings / $.evidence.rules / $.evidence.threads / $.evidence.facts",
      "stage": "snapshot",
      "contract": "offline-corpus-seal-recipe.v1",
      "field": "$.evidence.siblings / $.evidence.rules / $.evidence.threads / $.evidence.facts",
      "kind": "jsonArray",
      "required": "required: parent evidence is required; each list: no minItems — empty [] is legal and common",
      "producer": "src/Agents/reviewer/schemas/reviewer.offline-corpus-seal-recipe.v1.json:184-191 — each is $ref corpusPayloadRefList (type:array, no minItems); required parent evidence object is required",
      "consumer": "src/Agents/reviewer/CorpusSeal.ps1:1253-1263 — foreach ($class in @('siblings','rules','threads','facts')): $items = @(Get-ReviewerCorpusSealProperty ... -Type array); foreach ($item in $items) to resolve each payload; line 1263: $evidenceRecord[$class] = @($…",
      "collapseRisk": "Empty evidence lists serialize correctly as [string[]]@() in the sidecar. A 1-item evidence list collapses to PSCustomObject from ConvertFrom-Json, causing Get-ReviewerCorpusSealProperty -Type array to throw. Recipe authors must ensure JSON array syntax even for singleton evidence. The sidecar ForEach-Object at line 1931-1934 produces empty [string[]] for empty lists — safe.",
      "knownEscapeShape": false
    },
    {
      "id": "snapshot/offline-corpus-seal-recipe.v1/$.resources[*]",
      "stage": "snapshot",
      "contract": "offline-corpus-seal-recipe.v1",
      "field": "$.resources[*]",
      "kind": "jsonArray",
      "required": "required: yes; empty: NO (minItems:1)",
      "producer": "src/Agents/reviewer/schemas/reviewer.offline-corpus-seal-recipe.v1.json:241-278 (type:array, minItems:1, maxItems:4096, required)",
      "consumer": "src/Agents/reviewer/CorpusSeal.ps1:1267 — $resources = @(Get-ReviewerCorpusSealProperty ... -Type array); line 1276-1344 — foreach ($resource in $resources) with per-entry validation and envelope materialization",
      "collapseRisk": "A 1-resource recipe (hypothetically): Get-ReviewerCorpusSealProperty -Type array throws. minItems:1 enforced by the schema BUT ConvertFrom-Json deserialization collapse still occurs before type check. A real corpus always has multiple resources (at minimum PR identity, change set, file reads), but a synthetic test with 1 resource would fail.",
      "knownEscapeShape": false
    },
    {
      "id": "snapshot/offline-corpus-seal-recipe.v1/$.sourceCensus.noRightHandPaths",
      "stage": "snapshot",
      "contract": "offline-corpus-seal-recipe.v1",
      "field": "$.sourceCensus.noRightHandPaths",
      "kind": "jsonArray",
      "required": "required: yes (in sourceCensus); empty: YES (common for all-source-bearing PRs)",
      "producer": "src/Agents/reviewer/schemas/reviewer.offline-corpus-seal-recipe.v1.json:174-178 (type:array, items:repositoryPath; required in sourceCensus)",
      "consumer": "src/Agents/reviewer/CorpusSeal.ps1:1199 — $noRightHandPaths = @(Get-ReviewerCorpusSealProperty ... -Type array | ForEach-Object { [string]$_ }); lines 1207-1223 — foreach to validate each excused path; line 1481 — Census.noRightHandPaths = [string[]]@($noRigh…",
      "collapseRisk": "Empty [] is the common case (all files have right-hand content). [string[]]@() is stable. A 1-path excusal: ConvertFrom-Json gives bare string → Get-ReviewerCorpusSealProperty -Type array THROWS. This is the same single-element collapse pattern as changedFiles. An all-delete PR would have all paths in noRightHandPaths; if there's exactly 1 path, the recipe would fail to seal. Recipe producers MUST use JSON array syn…",
      "knownEscapeShape": false
    },
    {
      "id": "snapshot/offline-corpus-seal-recipe.v1/$.sourceTransport.expected.gateReasonCodes",
      "stage": "snapshot",
      "contract": "offline-corpus-seal-recipe.v1",
      "field": "$.sourceTransport.expected.gateReasonCodes",
      "kind": "jsonArray",
      "required": "required: yes (in expected object); empty: YES — empty [] means gate passed with no reason codes (normal for covered PRs)",
      "producer": "src/Agents/reviewer/schemas/reviewer.offline-corpus-seal-recipe.v1.json:235-237 (type:array, items:string; no minItems — required in expected object)",
      "consumer": "src/Agents/reviewer/CorpusSeal.ps1:1751-1752 — $expectedReasons = [string[]]@(Get-ReviewerCorpusSealProperty ... -Type array | ForEach-Object { [string]$_ }); line 1753 — $actualReasons = [string[]]@(@($gate.reasonCodes) | ForEach-Object { [string]$_ }); line…",
      "collapseRisk": "Empty [] serializes to [string[]]@() — join gives '' — comparison correct. A 1-reason array from recipe after ConvertFrom-Json: Get-ReviewerCorpusSealProperty -Type array throws. The comparison at line 1754 joins both sides — if either side were a bare string instead of [string[]], the join would still work but order-sensitive comparison could give false mismatch for multi-reason cases.",
      "knownEscapeShape": false
    },
    {
      "id": "snapshot/qualification-internal/$arguments (List[string] in New-ReviewerQualificationSlotArgument)",
      "stage": "snapshot",
      "contract": "qualification-internal",
      "field": "$arguments (List[string] in New-ReviewerQualificationSlotArgument)",
      "kind": "list",
      "required": "required: n/a",
      "producer": "src/Agents/reviewer/ReplayQualification.ps1:185 — $arguments = [System.Collections.Generic.List[string]]::new(); lines 186-201 — AddRange / Add calls per argument",
      "consumer": "src/Agents/reviewer/ReplayQualification.ps1:124 — foreach ($argument in [string[]]@($Arguments)) in Invoke-ReviewerQualificationPrelaunch; passes to $startInfo.ArgumentList.Add()",
      "collapseRisk": "List[string] — no collapse from List itself. The conversion to [string[]]@($arguments) for passing to ArgumentList.Add() is explicit. AddRange at line 186 takes [string[]]@('-RepoPath', $RepoPath) — if any parameter is $null, AddRange would add $null as a string element, causing command-line parsing issues.",
      "knownEscapeShape": false
    },
    {
      "id": "snapshot/qualification-internal/$marked (filtered prelaunch output lines)",
      "stage": "snapshot",
      "contract": "qualification-internal",
      "field": "$marked (filtered prelaunch output lines)",
      "kind": "psCollectionReturn",
      "required": "required: n/a; expected exactly 1 element on success",
      "producer": "src/Agents/reviewer/ReplayQualification.ps1:139 — $marked = @(@($stdout -split '`n') | Where-Object { $_.StartsWith($script:ReviewerQualificationPrelaunchMarker) })",
      "consumer": "src/Agents/reviewer/ReplayQualification.ps1:140 — if ($process.ExitCode -ne 0 -or $marked.Count -ne 1); line 145 — $payload = $marked[0].Substring(...)",
      "collapseRisk": "Double @() wrap: @(@($stdout -split ...) | Where-Object {...}). The outer @() ensures $marked is always an array, even if Where-Object returns 0 or 1 elements. $marked.Count -ne 1 correctly detects 0 (no marker found) or >1 (multiple markers, unexpected). $marked[0] on a 1-element array gives the sole string — correct. If $stdout were $null, @($null -split ...) gives @('') — Where-Object filters it out — $marked = @…",
      "knownEscapeShape": false
    },
    {
      "id": "snapshot/qualification-internal/$script:ReviewerQualificationRefusedSwitches",
      "stage": "snapshot",
      "contract": "qualification-internal",
      "field": "$script:ReviewerQualificationRefusedSwitches",
      "kind": "psCollectionReturn",
      "required": "required: n/a",
      "producer": "src/Agents/reviewer/ReplayQualification.ps1:41-46 — @('-EnableFindingComments','-EnableSummaryComment', ...)",
      "consumer": "ReplayQualification.ps1 — New-ReviewerQualificationSlotArgument validates that none of these switches are in the caller-supplied arguments (not read in detail in this session)",
      "collapseRisk": "Script-level constant; no collapse. The -ccontains membership test against this array is correct.",
      "knownEscapeShape": false
    },
    {
      "id": "snapshot/snapshot-manifest.v2/$.bindings.models",
      "stage": "snapshot",
      "contract": "snapshot-manifest.v2",
      "field": "$.bindings.models",
      "kind": "jsonArray",
      "required": "required: parent bindings required; models no minItems",
      "producer": "src/Agents/reviewer/testdata/replay-v1/synthetic-pr/manifest.json:20-22 — [\"claude-opus-5\"] (1 element); CorpusSeal.ps1:1878-1881 — models = @($Plan.Bindings.models)",
      "consumer": "Replay loader: validates that the requested model is in bindings.models before serving the snapshot",
      "collapseRisk": "Testdata has exactly 1 model — [\"claude-opus-5\"]. When this manifest is loaded via ConvertFrom-Json, $.bindings.models becomes a bare string 'claude-opus-5', NOT a 1-element array. Any consumer doing '$models -contains $requestedModel' would need @($models) to avoid string-containment vs. array-membership test. This is a REAL collapse risk in the testdata. The production loader must guard with @().",
      "knownEscapeShape": false
    },
    {
      "id": "snapshot/snapshot-manifest.v2/$.resources[*]",
      "stage": "snapshot",
      "contract": "snapshot-manifest.v2",
      "field": "$.resources[*]",
      "kind": "jsonArray",
      "required": "required: yes; empty: NO — must have ≥1 resource for a usable snapshot",
      "producer": "src/Agents/reviewer/testdata/replay-v1/synthetic-pr/manifest.json:24-139 — 8 resource entries; also CorpusSeal.ps1:1975 — resources = @($resources.ToArray())",
      "consumer": "Replay loader (New-AgentReplaySnapshot, not in this session's scope) reads manifest.resources to index tool→SHA for each recorded read; testdata manifest is the canonical example",
      "collapseRisk": "Testdata has 8 resources — no collapse risk in practice. CorpusSeal.ps1 builds the manifest resources via @($resources.ToArray()) from a List[object], so empty List gives @(), multi-entry gives [object[]], 1-entry gives [object[]] correctly (List.ToArray() always returns [object[]] not a scalar).",
      "knownEscapeShape": false
    },
    {
      "id": "snapshot/testdata-recipe/$.[] (root is JSON array)",
      "stage": "snapshot",
      "contract": "testdata-recipe",
      "field": "$.[] (root is JSON array)",
      "kind": "jsonArray",
      "required": "required: n/a (whole-file); empty: NO in practice",
      "producer": "src/Agents/reviewer/testdata/replay-v1/synthetic-pr/recipe.json:1-93 — root is a JSON array of resource entry objects (8 entries)",
      "consumer": "The legacy replay loader reads this as a bare resource array (not the offline-corpus-seal-recipe.v1 format); each entry has tool/arguments/payloadFile fields",
      "collapseRisk": "The recipe.json root being a JSON array (not an object) means ConvertFrom-Json returns [object[]]. A 1-entry recipe would produce [PSCustomObject] without @() wrap. The testdata has 8 entries — safe. Note this format is DIFFERENT from the offline-corpus-seal-recipe.v1 schema (which is an object); a consumer that expects an object and gets an array (or vice versa) would fail silently or throw on property access.",
      "knownEscapeShape": false
    },
    {
      "id": "source/source-transport-internal/$script:ReviewerSourceNonContentKinds",
      "stage": "source",
      "contract": "source-transport-internal",
      "field": "$script:ReviewerSourceNonContentKinds",
      "kind": "psCollectionReturn",
      "required": "required: n/a; empty: NO",
      "producer": "src/Agents/reviewer/SourceTransport.ps1:1400-1402 — @('delete','rename','sourcerename','targetrename','encoding','lock','property')",
      "consumer": "src/Agents/reviewer/SourceTransport.ps1:1420 — Test-ReviewerSourceChangeCarriesRightHand: $rightHand = @(@($kinds) | Where-Object { $_ -cnotin $script:ReviewerSourceNonContentKinds }); count determines whether path has right-hand content",
      "collapseRisk": "'-cnotin' operator against this constant array works correctly for any element count. Key semantic risk: 'none' is explicitly absent — a 0 changeType produces no tokens from Get-ReviewerSourceChangeKinds, which makes the path content-bearing (deliberate fail-closed).",
      "knownEscapeShape": false
    },
    {
      "id": "source/source-transport-internal/$script:ReviewerSourceNonTextExtensions",
      "stage": "source",
      "contract": "source-transport-internal",
      "field": "$script:ReviewerSourceNonTextExtensions",
      "kind": "psCollectionReturn",
      "required": "required: n/a; empty: NO",
      "producer": "src/Agents/reviewer/SourceTransport.ps1:139-147 — large @(...) constant",
      "consumer": "src/Agents/reviewer/SourceTransport.ps1:163 — Test-ReviewerSourcePathLooksNonText: ($script:ReviewerSourceNonTextExtensions -ccontains ...) membership test; also called from New-ReviewerSourceTransportReport lines 2909-2910 to decide corroboration",
      "collapseRisk": "Large constant; -ccontains is fine. A single-element array would still work. Key risk: this set is the corroboration check — a missing extension from this list means reader excusal is UNcorroborated and charged against the allowance ceiling.",
      "knownEscapeShape": false
    },
    {
      "id": "source/source-transport-internal/$script:ReviewerSourceNoSourceReasons",
      "stage": "source",
      "contract": "source-transport-internal",
      "field": "$script:ReviewerSourceNoSourceReasons",
      "kind": "psCollectionReturn",
      "required": "required: n/a; empty: NO",
      "producer": "src/Agents/reviewer/SourceTransport.ps1:75-78 — script-scope PS array @('noChangedSpans','binaryNoText','readerReportedNonTextUncorroborated','emptyFile','authoritativeDeletionOnly')",
      "consumer": "src/Agents/reviewer/SourceTransport.ps1:3057 — New-ReviewerSourceFileEntry: if (-not $CarriesSource -and $script:ReviewerSourceNoSourceReasons -cnotcontains $Reason) gate-side validation",
      "collapseRisk": "5-element constant; no collapse. The deliberately LARGER size vs $script:ReviewerSourceNothingToReadReasons means any drift (e.g. accidental single-element assignment) would silently permit reasons the gate was never supposed to admit.",
      "knownEscapeShape": false
    },
    {
      "id": "source/source-transport-internal/$script:ReviewerSourceNothingToReadReasons",
      "stage": "source",
      "contract": "source-transport-internal",
      "field": "$script:ReviewerSourceNothingToReadReasons",
      "kind": "psCollectionReturn",
      "required": "required: n/a; empty: NO",
      "producer": "src/Agents/reviewer/SourceTransport.ps1:86 — @('noChangedSpans','authoritativeDeletionOnly')",
      "consumer": "src/Agents/reviewer/SourceTransport.ps1 — used to generate the model-facing binding sentence in Format-ReviewerSealedSourceBlock (later in file, not read in this session but referenced in comments at line 85-86)",
      "collapseRisk": "Explicitly stated as 'the sealed block's binding sentence is GENERATED from this array'. A drift to 1-element would change the generated prose; a collapse to string (e.g. via $var = $script:ReviewerSourceNothingToReadReasons[0]) would silently break the generation loop.",
      "knownEscapeShape": false
    },
    {
      "id": "source/source-transport-internal/$script:ReviewerSourceOmissionReasons",
      "stage": "source",
      "contract": "source-transport-internal",
      "field": "$script:ReviewerSourceOmissionReasons",
      "kind": "psCollectionReturn",
      "required": "required: n/a (hardcoded constant); empty: NO",
      "producer": "src/Agents/reviewer/SourceTransport.ps1:60-68 — script-scope PS array literal @(...)",
      "consumer": "src/Agents/reviewer/SourceTransport.ps1:3050-3052 — New-ReviewerSourceFileEntry: if ($Reason -and $script:ReviewerSourceOmissionReasons -cnotcontains $Reason) validation; also used in Format-ReviewerSealedSourceBlock for the model-facing binding sentence",
      "collapseRisk": "Script-scoped PS array; -ccontains membership test is correct for arrays. No collapse risk, but a single-element array constant read as a string (e.g., via string interpolation) would silently change -cnotcontains semantics to substring test.",
      "knownEscapeShape": false
    },
    {
      "id": "source/source-transport-internal/$script:ReviewerSourceReaderAuthoredRejections",
      "stage": "source",
      "contract": "source-transport-internal",
      "field": "$script:ReviewerSourceReaderAuthoredRejections",
      "kind": "psCollectionReturn",
      "required": "required: n/a; empty: NO",
      "producer": "src/Agents/reviewer/SourceTransport.ps1:93-95 — @('notTextual','emptyFile','decodeRejected','fileTooLarge','transportFailed')",
      "consumer": "src/Agents/reviewer/SourceTransport.ps1:2755,2805 — New-ReviewerSourceTransportReport: if ($script:ReviewerSourceReaderAuthoredRejections -cnotcontains $spanlessRejected) and if ($script:ReviewerSourceReaderAuthoredRejections -cnotcontains $rejection) — gate…",
      "collapseRisk": "Closed set enforcement; if -cnotcontains test ran on a string scalar instead of this array, it would perform substring test. Adding a 6th reason would still work; removing all but 1 element would not collapse the PS array literal.",
      "knownEscapeShape": false
    },
    {
      "id": "source/source-transport-internal/Get-ReviewerSourceAzChangePage return value — .Changes",
      "stage": "source",
      "contract": "source-transport-internal",
      "field": "Get-ReviewerSourceAzChangePage return value — .Changes",
      "kind": "psCollectionReturn",
      "required": "required: n/a; can be empty (terminal page)",
      "producer": "src/Agents/reviewer/SourceTransport.ps1:408-455 — .Changes = @($changes)",
      "consumer": "src/Agents/reviewer/SourceTransport.ps1:498 — foreach ($change in @($page.Changes)) in Get-ReviewerSourceAzIdentityCapture",
      "collapseRisk": "@($changes) on an empty $changes gives @(), not $null. Safe. But AzChangePage's terminal condition at line 424 returns HasMoreChanges:$false only for a 'short page' (count < $ExpectedTop). A full-page with no cursors at line 426 returns $null rather than a 0-element result, which is a different escape shape.",
      "knownEscapeShape": false
    },
    {
      "id": "source/source-transport-internal/Get-ReviewerSourceAzIdentityCapture return value — .Response.changes",
      "stage": "source",
      "contract": "source-transport-internal",
      "field": "Get-ReviewerSourceAzIdentityCapture return value — .Response.changes",
      "kind": "psCollectionReturn",
      "required": "required: n/a",
      "producer": "src/Agents/reviewer/SourceTransport.ps1:526 — $response = [pscustomobject]@{ changes = $all.ToArray() }",
      "consumer": "src/Agents/reviewer/SourceTransport.ps1:531 — $response | ConvertTo-Json -Depth 30 -Compress for ChangeSetSha256 computation; later passed as $aggregateResponse to Get-ReviewerSourceChangedSpans etc.",
      "collapseRisk": "$all.ToArray() on an empty List gives @(), which ConvertTo-Json renders as []. A 1-element List gives a 1-element array, which ConvertTo-Json renders as [...]. No collapse. But the ConvertTo-Json call for ChangeSetSha256 at line 531 uses -Compress and -Depth 30 — if a change entry contains nested arrays, depth matters. Default depth 2 would silently truncate.",
      "knownEscapeShape": false
    },
    {
      "id": "source/source-transport-internal/Get-ReviewerSourceAzIterationBinding return value — .ReasonNames",
      "stage": "source",
      "contract": "source-transport-internal",
      "field": "Get-ReviewerSourceAzIterationBinding return value — .ReasonNames",
      "kind": "psCollectionReturn",
      "required": "required: n/a",
      "producer": "src/Agents/reviewer/SourceTransport.ps1:400-401 — ReasonNames = [string[]]@($reasonName)",
      "consumer": "src/Agents/reviewer/SourceTransport.ps1:316 — @($Before.ReasonNames) -join",
      "collapseRisk": "[string[]]@($reasonName) where $reasonName is a single string gives a 1-element array — safe for join. AzIterationBinding always produces exactly 1 name (line 381 $reasonName = $reason.Trim().ToLowerInvariant()). The PageBinding variant may have multiple names from the API's enum flags; the [string[]]$reasonNames cast at line 296 handles that.",
      "knownEscapeShape": false
    },
    {
      "id": "source/source-transport-internal/Get-ReviewerSourceChangedSpans return value (OrderedDictionary path→@spans)",
      "stage": "source",
      "contract": "source-transport-internal",
      "field": "Get-ReviewerSourceChangedSpans return value (OrderedDictionary path→@spans)",
      "kind": "jsonObjectMap",
      "required": "required: n/a; empty map is valid (all-delete PR)",
      "producer": "src/Agents/reviewer/SourceTransport.ps1:1519-1553 — $spansByPath OrderedDictionary[string, List[object]] built per path from lineDiffBlocks; returned as fresh OrderedDictionary with @($spansByPath[$path]) per key",
      "consumer": "src/Agents/reviewer/SourceTransport.ps1:974 — $spans = Get-ReviewerSourceChangedSpans; line 983 — foreach ($spanPath in @($spans.Keys)) with @($SpansByPath[$spanPath]).Count; CorpusSeal.ps1:1092 — $authoritativeSpansByPath = Get-ReviewerCorpusSealSpanEvidence…",
      "collapseRisk": "Each value is already wrapped with @() at line 1552: $result[$path] = @($spansByPath[$path]). However, if the stored List has 1 span, it is @() of a 1-element List — still an array. The key collapse risk is in consumers: any consumer that does $spans[$path] without @() gets either an array or $null (path not in map). CorpusSeal.ps1 line 1439 uses @($spansByPath[$path]) correctly.",
      "knownEscapeShape": false
    },
    {
      "id": "source/source-transport-internal/Get-ReviewerSourceChangeKinds return value",
      "stage": "source",
      "contract": "source-transport-internal",
      "field": "Get-ReviewerSourceChangeKinds return value",
      "kind": "psCollectionReturn",
      "required": "required: n/a; empty list means unknown/0 change type (treated as content-bearing at line 1419)",
      "producer": "src/Agents/reviewer/SourceTransport.ps1:1333-1388 — $kinds = New-Object List[string]; various .Add() calls; return , $kinds.ToArray() (unary comma at exit)",
      "consumer": "src/Agents/reviewer/SourceTransport.ps1:1418 — $kinds = Get-ReviewerSourceChangeKinds -Value $ChangeTypeValue (direct assign, unary comma ensures array wrapper); line 1451 — $declaredKinds = Get-ReviewerSourceChangeKinds ...; CorpusSeal.ps1:1591-1593 — foreac…",
      "collapseRisk": "Unary comma return (', $kinds.ToArray()') prevents a 1-element array from collapsing to a scalar in the pipeline. HOWEVER: the caller at line 1418 does '$kinds = Get-ReviewerSourceChangeKinds ...' — PowerShell assignment unwraps one level of the unary comma, leaving $kinds as the inner [string[]]. A subsequent '$kinds.Count -eq 0' is correct. The risk is at line 1419: 'if ($null -eq $kinds -or @($kinds).Count -eq 0)…",
      "knownEscapeShape": false
    },
    {
      "id": "source/source-transport-internal/Get-ReviewerSourceChangeKindsByPath return value (OrderedDictionary path→[string[]])",
      "stage": "source",
      "contract": "source-transport-internal",
      "field": "Get-ReviewerSourceChangeKindsByPath return value (OrderedDictionary path→[string[]])",
      "kind": "jsonObjectMap",
      "required": "required: n/a; missing key means unknown kinds (treated as content-bearing)",
      "producer": "src/Agents/reviewer/SourceTransport.ps1:1424-1459 — OrderedDictionary, accumulated per change entry; line 1453: $kindsByPath[$path] = @(@($kindsByPath[$path]) + @($declaredKinds) | Select-Object -Unique)",
      "consumer": "src/Agents/reviewer/SourceTransport.ps1:975 — $kindsByPath = Get-ReviewerSourceChangeKindsByPath; line 1004 — $actualKinds = if ($kindsByPath.Contains($path)) { @($kindsByPath[$path]) }; CorpusSeal.ps1:1071 — $authoritativeKindsByPath = Get-ReviewerSourceChan…",
      "collapseRisk": "Values are re-wrapped with @() at line 1453 and at each consumer. A path absent from the map gives $null; the consumer at line 1004 guards with if ($kindsByPath.Contains($path)). The combination-via-Select-Object-Unique at line 1453 could silently discard duplicates when a rename produces two entries.",
      "knownEscapeShape": false
    },
    {
      "id": "source/source-transport-internal/Get-ReviewerSourceDegenerateChanges return value (OrderedDictionary path→obj)",
      "stage": "source",
      "contract": "source-transport-internal",
      "field": "Get-ReviewerSourceDegenerateChanges return value (OrderedDictionary path→obj)",
      "kind": "jsonObjectMap",
      "required": "required: n/a; empty map means no degenerate changes",
      "producer": "src/Agents/reviewer/SourceTransport.ps1:1563-1638 — OrderedDictionary path→{ChangeKinds, EvidenceBlockCount}",
      "consumer": "src/Agents/reviewer/SourceTransport.ps1:1948 — $candidates = Get-ReviewerSourceDegenerateChanges; line 1950-1951 — foreach ($candidatePath in @($candidates.Keys)): EvidenceBlockCount summed",
      "collapseRisk": "OrderedDictionary with empty Keys returns @() for @($candidates.Keys) — safe. Single-entry: @($candidates.Keys) gives a 1-element array — safe for foreach. The state.ChangeKinds is a List[string] internally — ToArray() at line 1627 gives [string[]].",
      "knownEscapeShape": false
    },
    {
      "id": "source/source-transport-internal/Get-ReviewerSourceDeterministicDiffResult return value — .Spans",
      "stage": "source",
      "contract": "source-transport-internal",
      "field": "Get-ReviewerSourceDeterministicDiffResult return value — .Spans",
      "kind": "psCollectionReturn",
      "required": "required: n/a; empty means no changed lines (fully deleted file)",
      "producer": "src/Agents/reviewer/SourceTransport.ps1:1855 — Spans = @($spans.ToArray()) where $spans is a List[object]",
      "consumer": "src/Agents/reviewer/SourceTransport.ps1:1876 — return , @($result.Spans); consumed by Get-ReviewerSourceDeterministicDiffSpans then by Get-ReviewerSourceRecoveredSpans",
      "collapseRisk": "@($spans.ToArray()) for empty List gives @(). Return via ', @($result.Spans)' wraps in unary comma — the caller at line 1875-1876 does 'if (-not [bool]$result.Success) { return $null } return , @($result.Spans)' — the unary comma means callers get an array-within-array wrapper that PowerShell assignment unwraps once. Consumers iterate result of Get-ReviewerSourceDeterministicDiffSpans using @() at lines like 1921. A…",
      "knownEscapeShape": false
    },
    {
      "id": "source/source-transport-internal/Get-ReviewerSourceIterationPageBinding return value — .Changes, .ReasonNames",
      "stage": "source",
      "contract": "source-transport-internal",
      "field": "Get-ReviewerSourceIterationPageBinding return value — .Changes, .ReasonNames",
      "kind": "psCollectionReturn",
      "required": "required: n/a",
      "producer": "src/Agents/reviewer/SourceTransport.ps1:290-305 — .Changes = @($changes); .ReasonNames = [string[]]$reasonNames",
      "consumer": "src/Agents/reviewer/SourceTransport.ps1:915 — foreach ($change in @($binding.Changes)); line 316 — @($Before.ReasonNames) -join '`0'",
      "collapseRisk": ".Changes = @($changes) ensures an array even if $changes is a single object. .ReasonNames = [string[]]$reasonNames — if $reasonNames is empty, [string[]] cast produces @(), not $null. The join at line 316 handles empty correctly.",
      "knownEscapeShape": false
    },
    {
      "id": "source/source-transport-internal/Get-ReviewerSourceRawChangedPaths return value",
      "stage": "source",
      "contract": "source-transport-internal",
      "field": "Get-ReviewerSourceRawChangedPaths return value",
      "kind": "psCollectionReturn",
      "required": "required: n/a; empty allowed (all-delete PR)",
      "producer": "src/Agents/reviewer/SourceTransport.ps1:1462-1478 — $paths = [List[string]]::new(); ...Add($path); return $paths.ToArray()",
      "consumer": "src/Agents/reviewer/SourceTransport.ps1:976 — $paths = [string[]]@(Get-ReviewerSourceRawChangedPaths -Response $aggregateResponse); line 978 — @($paths | Where-Object {...}).Count; line 983 — foreach ($spanPath in @($spans.Keys)); CorpusSeal.ps1:1043 — $rawAu…",
      "collapseRisk": "Returns a plain [string[]] via .ToArray() — no unary comma. If the PR has exactly 1 changed file, .ToArray() returns a 1-element [string[]], which is fine. The [string[]]@() wrapping at call sites is the guard. An empty return for a 0-change PR is intentional and safe.",
      "knownEscapeShape": false
    },
    {
      "id": "source/source-transport-internal/Get-ReviewerSourceRecoveredSpans return value — .RecoveredPaths, .SpansByPath, .SpanBasisByPath, .ExpectedSpanCountByPath, .AuthoritativeNoSourceByPath, .RecoveryFailureByPath",
      "stage": "source",
      "contract": "source-transport-internal",
      "field": "Get-ReviewerSourceRecoveredSpans return value — .RecoveredPaths, .SpansByPath, .SpanBasisByPath, .ExpectedSpanCountByPath, .AuthoritativeNoSourceByPath, .RecoveryFailureByPath",
      "kind": "psCollectionReturn",
      "required": "required: n/a",
      "producer": "src/Agents/reviewer/SourceTransport.ps1:1937-1947 — [pscustomobject]@{ SpansByPath = $result; RecoveredPaths = $recoveredPaths.ToArray(); ... }",
      "consumer": "src/Agents/reviewer/SourceTransport.ps1:1063-1075 — New-ReviewerSourceTransportReport called with -SpansByPath $recovery.SpansByPath; line 1073 — @($recovery.RecoveredPaths).Count",
      "collapseRisk": ".RecoveredPaths = $recoveredPaths.ToArray() — empty List gives @() not $null. But .SpansByPath is an OrderedDictionary that is a REFERENCE to $result which was mutated during the recovery loop. @($recovery.RecoveredPaths).Count at line 1073 wraps with @() — safe. The early-return path at line 1937 emits .RecoveredPaths = $recoveredPaths.ToArray() where the List is still empty — @() is correct.",
      "knownEscapeShape": false
    },
    {
      "id": "source/source-transport-internal/Get-ReviewerSourceSiblingSpans return value",
      "stage": "source",
      "contract": "source-transport-internal",
      "field": "Get-ReviewerSourceSiblingSpans return value",
      "kind": "psCollectionReturn",
      "required": "required: n/a; empty is common when MaxSlices=0 or no gaps",
      "producer": "src/Agents/reviewer/SourceTransport.ps1:2122-2152 — $slices = [List[object]]::new(); Add per gap; return @($slices)",
      "consumer": "src/Agents/reviewer/SourceTransport.ps1:2222-2223 — $siblingSpans = @(Get-ReviewerSourceSiblingSpans ...) then foreach ($span in $siblingSpans)",
      "collapseRisk": "Returns @($slices) — plain array, no unary comma. Empty list → @() → safe. 1 slice → @([hashtable]) — safe for foreach. The function is called inside 'if (@($slices).Count -gt 0)' so the outer Slices list being empty gates the call.",
      "knownEscapeShape": false
    },
    {
      "id": "source/source-transport-internal/New-ReviewerSourceFileEntry return value — .Slices, .SiblingSlices, .RawSpans",
      "stage": "source",
      "contract": "source-transport-internal",
      "field": "New-ReviewerSourceFileEntry return value — .Slices, .SiblingSlices, .RawSpans",
      "kind": "psCollectionReturn",
      "required": "required: n/a; empty arrays for omitted files",
      "producer": "src/Agents/reviewer/SourceTransport.ps1:3083-3085 — Slices = @($Slices); SiblingSlices = @($SiblingSlices); RawSpans = @($RawSpans)",
      "consumer": "src/Agents/reviewer/SourceTransport.ps1:3064 — foreach ($slice in @($Slices)) for DeliveredBytes count; New-ReviewerSourceTransportReport line 3010 — Files = @($files); Format-ReviewerSealedSourceBlock consumes .Slices and .SiblingSlices per file entry",
      "collapseRisk": "All three fields are @()-wrapped at return time. Omitted-file entries always have @() for Slices and SiblingSlices. The key collapse scenario is in Format-ReviewerSealedSourceBlock (not read here): iterating .Slices without @() would work for multi-slice files but give a hashtable iteration (PS default) for a 1-slice file if the List became a bare hashtable.",
      "knownEscapeShape": false
    },
    {
      "id": "source/source-transport-internal/New-ReviewerSourceFileSlices return value — .Slices, .SiblingSlices",
      "stage": "source",
      "contract": "source-transport-internal",
      "field": "New-ReviewerSourceFileSlices return value — .Slices, .SiblingSlices",
      "kind": "psCollectionReturn",
      "required": "required: n/a; empty is legal and common",
      "producer": "src/Agents/reviewer/SourceTransport.ps1:2248-2267 — .Slices = @($slices); .SiblingSlices = @($siblingSlices)",
      "consumer": "src/Agents/reviewer/SourceTransport.ps1:2833 — $deliveredSpanCount = @($cut.Slices).Count; line 2873 — New-ReviewerSourceFileEntry -Slices @($cut.Slices) -SiblingSlices @($cut.SiblingSlices)",
      "collapseRisk": "@($slices) on a List gives an array even when empty. The extra @() at call sites (line 2873) is redundant but harmless. True collapse risk: line 2221 — 'if (@($slices).Count -gt 0)' guards sibling computation — if this were '$slices.Count -gt 0' on the underlying List before assignment, it would be fine, but after @($slices) the .Count is always defined.",
      "knownEscapeShape": false
    },
    {
      "id": "source/source-transport-internal/New-ReviewerSourceTransportReport return value — .Files",
      "stage": "source",
      "contract": "source-transport-internal",
      "field": "New-ReviewerSourceTransportReport return value — .Files",
      "kind": "psCollectionReturn",
      "required": "required: n/a; empty is valid (no changed files)",
      "producer": "src/Agents/reviewer/SourceTransport.ps1:3010 — Files = @($files) where $files is a [List[object]]",
      "consumer": "src/Agents/reviewer/SourceTransport.ps1:1087 — @($report.Files).Count -gt 0 gate for block rendering; line 2899-2979 — multiple @(@($files) | Where-Object {...}).Count metrics; CorpusSeal.ps1:1713 — $report = New-ReviewerSourceTransportReport; $report's .File…",
      "collapseRisk": "@($files) on a 0-element List gives @(). @($files) on a 1-file List gives a 1-element array. Multiple consumers wrap again with @() — safe. CRITICAL: at line 1087, 'if (@($report.Files).Count -gt 0)' gates block rendering. If .Files were $null (missing from hashtable), @($null) gives @($null) (1 element!), making .Count -gt 0 true and passing a $null file list to the block renderer. The @() wrapping at source is the…",
      "knownEscapeShape": false
    },
    {
      "id": "source/source-transport-internal/Resolve-ReviewerSourceChangeEntries return value",
      "stage": "source",
      "contract": "source-transport-internal",
      "field": "Resolve-ReviewerSourceChangeEntries return value",
      "kind": "psCollectionReturn",
      "required": "required: n/a",
      "producer": "src/Agents/reviewer/SourceTransport.ps1:1306-1331 — unwraps changeEntries/changes/value nesting; return , (@($node)) (unary comma wrapping @($node))",
      "consumer": "src/Agents/reviewer/SourceTransport.ps1:1437 — $changeEntries = Resolve-ReviewerSourceChangeEntries; line 1438 — foreach ($change in @($changeEntries)); line 1487 — $changes = Resolve-ReviewerSourceChangeEntries; line 1488 — foreach ($change in @($changes))",
      "collapseRisk": "Unary comma return wraps the entire unwrapped collection in a 1-element array. Callers do '$changes = Resolve-...' which unrolls one level — $changes becomes the actual collection. Then 'foreach ($change in @($changes))' adds @() to handle the case where $changes is itself a PSCustomObject (single item). The comment at line 1433-1435 explicitly documents: '@() around the unary-comma return NESTSs instead of flatteni…",
      "knownEscapeShape": true
    },
    {
      "id": "source/source-transport-internal/Split-ReviewerSourceLines return value",
      "stage": "source",
      "contract": "source-transport-internal",
      "field": "Split-ReviewerSourceLines return value",
      "kind": "psCollectionReturn",
      "required": "required: n/a",
      "producer": "src/Agents/reviewer/SourceTransport.ps1:1147-1151 — $lines = @($Text -split ...) with trailing-newline trimming; return , $lines (unary comma)",
      "consumer": "src/Agents/reviewer/SourceTransport.ps1:2174 — $lines = Split-ReviewerSourceLines -Text $Text in New-ReviewerSourceFileSlices; line 2180 — $lines.Count; line 2191 — $lines[($startLine-1)..($endLine-1)]",
      "collapseRisk": "Unary comma return. Assignment unwraps: $lines = the inner array. For an empty file, @('') -split on an empty string gives @(''), then the trailing-newline trim at line 1148-1150 removes a final empty element, leaving @() — correct 0-line count. For a 1-line file with a trailing newline: @('line','') → trim last empty → @('line') — correct. Key collapse risk: slicing at line 2191 '$lines[($startLine-1)..($endLine-1)…",
      "knownEscapeShape": false
    },
    {
      "id": "source/source-transport-policy.v1/$.allowedMimeTypes",
      "stage": "source",
      "contract": "source-transport-policy.v1",
      "field": "$.allowedMimeTypes",
      "kind": "jsonArray",
      "required": "required: yes; empty array: NO (minItems:1)",
      "producer": "src/Agents/reviewer/source/v1/policy.json:17-23 (5 MIME type strings); src/Agents/reviewer/source/v1/policy.schema.json:70-75 (type:array, minItems:1, maxItems:16, required)",
      "consumer": "src/Agents/reviewer/SourceTransport.ps1:1296-1302 — New-ReviewerSourceTransportPolicy: each item validated as non-empty string, stored as @($mimeTypes | ForEach-Object { [string]$_ }); line 2816 — $Policy.allowedMimeTypes -cnotcontains $mimeType membership te…",
      "collapseRisk": "A policy with 1 allowed MIME type collapses to a bare string after ConvertFrom-Json. New-ReviewerSourceTransportPolicy at line ~1291 iterates with foreach ($mime in @($Policy.allowedMimeTypes.Value)) — the @() is critical. The -cnotcontains membership test on a string scalar (not array) would test substring containment in some PS versions, silently admitting all MIME types.",
      "knownEscapeShape": false
    },
    {
      "id": "specialistPlan/ChangedConstructs/$Constructs (psCollectionReturn from Get-ReviewerChangedInvocations / Get-ReviewerChangedDeclarations)",
      "stage": "specialistPlan",
      "contract": "ChangedConstructs",
      "field": "$Constructs (psCollectionReturn from Get-ReviewerChangedInvocations / Get-ReviewerChangedDeclarations)",
      "kind": "psCollectionReturn",
      "required": "False",
      "producer": "src/Agents/reviewer/ChangedConstructs.ps1 ~500 (@{ Constructs = @($found.ToArray()); Truncated = $truncated }) and ~686 (same shape)",
      "consumer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~1276 (foreach ($construct in @($Constructs))); ~2101 (-Constructs $Constructs)",
      "collapseRisk": "0 constructs is valid (a change set with no multi-line calls or no declarations). @($found.ToArray()) double-wrap guarantees array even for 0 or 1 item.",
      "knownEscapeShape": false
    },
    {
      "id": "specialistPlan/ChangedConstructs/AttributeFrequency/$.attributeFrequency (from Get-ReviewerConstructAttributeFrequency)",
      "stage": "specialistPlan",
      "contract": "ChangedConstructs/AttributeFrequency",
      "field": "$.attributeFrequency (from Get-ReviewerConstructAttributeFrequency)",
      "kind": "psCollectionReturn",
      "required": "False",
      "producer": "src/Agents/reviewer/ChangedConstructs.ps1 ~732 (@{ … Attributes = @($rows.ToArray()); … })",
      "consumer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~212-213 (@((Get-ReviewerConventionSpecialistValue $Evidence 'attributeFrequency' @())))",
      "collapseRisk": "Can be empty (no attributes on any declaration). Default @() fallback at consumer.",
      "knownEscapeShape": false
    },
    {
      "id": "specialistPlan/ChangedConstructs/Declaration/$.construct.absentHere",
      "stage": "specialistPlan",
      "contract": "ChangedConstructs/Declaration",
      "field": "$.construct.absentHere",
      "kind": "jsonArray",
      "required": "False",
      "producer": "src/Agents/reviewer/ChangedConstructs.ps1 ~666 (@(@($unchangedAttributes) | Where-Object { @($declaration.Attributes) -cnotcontains $_ })) then sorted",
      "consumer": "Specialist input serialization",
      "collapseRisk": "KNOWN ESCAPE SHAPE: empty when every unchanged attribute is also on this declaration. Empty [] → $null on some JSON boundaries. The difference-set computation (@($unchangedAttributes) | Where-Object …) produces $null, not @(), when the pipeline returns 0 items — if the result is not forced with @() before Sort it may fail.",
      "knownEscapeShape": true
    },
    {
      "id": "specialistPlan/ChangedConstructs/Declaration/$.construct.attributes",
      "stage": "specialistPlan",
      "contract": "ChangedConstructs/Declaration",
      "field": "$.construct.attributes",
      "kind": "jsonArray",
      "required": "False",
      "producer": "src/Agents/reviewer/ChangedConstructs.ps1 ~675 (attributes = @($declaration.Attributes))",
      "consumer": "src/Agents/reviewer/ConventionSpecialist.ps1 (when constructs are serialized into specialist input); ChangedConstructs.ps1 ~620 (foreach ($attribute in @($neighbour.Attributes)))",
      "collapseRisk": "0 attributes is valid (a declaration with no attributes). @() wrap at every consumer. Sorted string[] so array-ness is preserved in PS.",
      "knownEscapeShape": false
    },
    {
      "id": "specialistPlan/ChangedConstructs/Declaration/$.construct.siblingAttributes",
      "stage": "specialistPlan",
      "contract": "ChangedConstructs/Declaration",
      "field": "$.construct.siblingAttributes",
      "kind": "jsonArray",
      "required": "False",
      "producer": "src/Agents/reviewer/ChangedConstructs.ps1 ~676 (@($sortedSiblings))",
      "consumer": "Specialist input serialization",
      "collapseRisk": "Can be empty when no neighbour declarations exist. @() wrap.",
      "knownEscapeShape": false
    },
    {
      "id": "specialistPlan/ChangedConstructs/MaskedLines/$.Lines / $.CommentLines (from Get-ReviewerConstructMaskedLines)",
      "stage": "specialistPlan",
      "contract": "ChangedConstructs/MaskedLines",
      "field": "$.Lines / $.CommentLines (from Get-ReviewerConstructMaskedLines)",
      "kind": "psCollectionReturn",
      "required": "True",
      "producer": "src/Agents/reviewer/ChangedConstructs.ps1 ~262-266 (@{ Lines = $masked.ToArray(); CommentLines = $commentLines; Truncated = … }) — CommentLines is a HashSet[int]",
      "consumer": "src/Agents/reviewer/ChangedConstructs.ps1 Get-ReviewerChangedInvocations, Get-ReviewerChangedDeclarations (iterate MaskedLines array)",
      "collapseRisk": "CommentLines is a HashSet[int] — if the return is unwrapped through PowerShell pipeline (not using -NoEnumerate or ,) the HashSet enumerates to individual ints. The caller uses $result.CommentLines directly (named property), so this is safe here.",
      "knownEscapeShape": false
    },
    {
      "id": "specialistPlan/ChangedFileIndex/$changedFileIndex (returned from Get-ReviewerConventionSpecialistChangedFileIndex)",
      "stage": "specialistPlan",
      "contract": "ChangedFileIndex",
      "field": "$changedFileIndex (returned from Get-ReviewerConventionSpecialistChangedFileIndex)",
      "kind": "psCollectionReturn",
      "required": "True",
      "producer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~1171 (return , $index.ToArray()) — comma-prefix ensures array-ness",
      "consumer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~2099-2102 (-ChangedFileAnchors $changedFileIndex); ~2132 (-ChangedFileAnchors $changedFileIndex)",
      "collapseRisk": "Comma-unary prevents PowerShell from unwrapping a 1-element array to a scalar. 0-element index is possible (no changed files that match 'current' role) — callers use @(...) wrapping.",
      "knownEscapeShape": false
    },
    {
      "id": "specialistPlan/ChangedFileIndex/Anchor/$.changedFileIndex[*].rightHandRanges",
      "stage": "specialistPlan",
      "contract": "ChangedFileIndex/Anchor",
      "field": "$.changedFileIndex[*].rightHandRanges",
      "kind": "jsonArray",
      "required": "False",
      "producer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~1156-1163 ($ranges = @(…); then stored in index entry)",
      "consumer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~124 (foreach ($range in @(Get-ReviewerConventionSpecialistValue $anchor 'rightHandRanges' @()))); ~1297",
      "collapseRisk": "Can be empty @() when no right-hand range info is available (commit-only mode). Default fallback @() at every consumer.",
      "knownEscapeShape": false
    },
    {
      "id": "specialistPlan/ConventionContextPlan/$.selectedPacks",
      "stage": "specialistPlan",
      "contract": "ConventionContextPlan",
      "field": "$.selectedPacks",
      "kind": "jsonArray",
      "required": "True",
      "producer": "src/Agents/reviewer/ConventionPacks.ps1 ~806 (New-ReviewerConventionContextPlan, $selectedPlans.ToArray() → withheld serialized)",
      "consumer": "src/Agents/reviewer/ConventionPacks.ps1 ~806 (plan written with ConvertTo-Json -Depth 32); src/Agents/reviewer/Start-ReviewerAgent.ps1 ~15618 (@($conventionPlan.selectedPacks).Count); ConventionSpecialist.ps1 (Read-ReviewerConventionPlanFile + iteration over…",
      "collapseRisk": "ConvertTo-Json/ConvertFrom-Json round-trip: a single-pack plan serializes $.selectedPacks as a 1-element JSON array, but ConvertFrom-Json without -AsHashtable can deserialize it as a bare PSCustomObject rather than an array; all consumers wrap in @(...) but a consumer that indexes directly would fail silently.",
      "knownEscapeShape": false
    },
    {
      "id": "specialistPlan/ConventionContextPlan/$.selectedPacks[*].matchedPaths",
      "stage": "specialistPlan",
      "contract": "ConventionContextPlan",
      "field": "$.selectedPacks[*].matchedPaths",
      "kind": "jsonArray",
      "required": "False",
      "producer": "src/Agents/reviewer/ConventionPacks.ps1 ~613-619 (Select-ReviewerConventionPacks, $matches.ToArray())",
      "consumer": "src/Agents/reviewer/Start-ReviewerAgent.ps1 ~15618 (count display only); no direct iteration found in scope files",
      "collapseRisk": "1-element matchedPaths collapses to a scalar PSCustomObject on ConvertFrom-Json without @(). The producing code guarantees Count>0 (a pack with 0 matches goes to withheld), but a single-match pack would be singleton on round-trip.",
      "knownEscapeShape": false
    },
    {
      "id": "specialistPlan/ConventionContextPlan/$.selectedPacks[*].sources",
      "stage": "specialistPlan",
      "contract": "ConventionContextPlan",
      "field": "$.selectedPacks[*].sources",
      "kind": "jsonArray",
      "required": "True",
      "producer": "src/Agents/reviewer/ConventionPacks.ps1 ~712-730 ($resolved.ToArray() inside New-ReviewerConventionContextPlan)",
      "consumer": "src/Agents/reviewer/ConventionSpecialist.ps1 Get-ReviewerConventionSpecialistRuleRequest: @($ResolvedSources) iteration; New-ReviewerConventionSpecialistInput line ~2243",
      "collapseRisk": "An empty $resolved list (AllowDegradedSources but all sources missing) produces an empty sources array. A pack with 0 sources is withheld, so this should never be empty in selectedPacks — but if it slips through, ConvertFrom-Json singleton collapse would give a bare object.",
      "knownEscapeShape": false
    },
    {
      "id": "specialistPlan/ConventionContextPlan/$.withheldPacks",
      "stage": "specialistPlan",
      "contract": "ConventionContextPlan",
      "field": "$.withheldPacks",
      "kind": "jsonArray",
      "required": "False",
      "producer": "src/Agents/reviewer/ConventionPacks.ps1 ~806 (@(@($Selection.Withheld) + $degradedPacks.ToArray()))",
      "consumer": "src/Agents/reviewer/Start-ReviewerAgent.ps1 ~15619 (@($conventionPlan.withheldPacks).Count); ~17803 (@(...'withheldPacks' @()) Where-Object)",
      "collapseRisk": "Explicitly initialized to @() when plan fails (line ~15646 'withheldPacks = @()'). 0-element array is legal and expected. On ConvertFrom-Json without @() guard, an empty array round-trips as $null in some PowerShell versions; consumers use @(...) wrap.",
      "knownEscapeShape": false
    },
    {
      "id": "specialistPlan/ConventionContextPlan/PackMatchedPath/$.selectedPacks[*].matchedPaths[*].changeTypes",
      "stage": "specialistPlan",
      "contract": "ConventionContextPlan/PackMatchedPath",
      "field": "$.selectedPacks[*].matchedPaths[*].changeTypes",
      "kind": "jsonArray",
      "required": "False",
      "producer": "src/Agents/reviewer/ConventionPacks.ps1 ~616 (@($entry.ChangeTypes)) inside Select-ReviewerConventionPacks",
      "consumer": "Not directly iterated downstream in scope files; part of sealed plan passed to specialist prompt text",
      "collapseRisk": "An unknown change type produces @('unknown') — 1 element; singleton arrays collapse to scalar strings on ConvertFrom-Json. Any downstream `foreach ($t in $entry.changeTypes)` would still work in PowerShell, but `.Count` on a string would give character count not item count.",
      "knownEscapeShape": false
    },
    {
      "id": "specialistPlan/ConventionContextPlan/PackMatchedPath/$.selectedPacks[*].matchedPaths[*].globs",
      "stage": "specialistPlan",
      "contract": "ConventionContextPlan/PackMatchedPath",
      "field": "$.selectedPacks[*].matchedPaths[*].globs",
      "kind": "jsonArray",
      "required": "False",
      "producer": "src/Agents/reviewer/ConventionPacks.ps1 ~617 (@($matchedGlobs)) inside Select-ReviewerConventionPacks",
      "consumer": "Not directly iterated in scope files",
      "collapseRisk": "1 matched glob → singleton → scalar string on ConvertFrom-Json. Safe only while wrapped in @(...).",
      "knownEscapeShape": false
    },
    {
      "id": "specialistPlan/ConventionPackPolicy/$.Packs[*].AuthoritativeSourceRefs",
      "stage": "specialistPlan",
      "contract": "ConventionPackPolicy",
      "field": "$.Packs[*].AuthoritativeSourceRefs",
      "kind": "list",
      "required": "True",
      "producer": "src/Agents/reviewer/ConventionPacks.ps1 ~569 (@($sourceRefs)) inside ConvertTo-ReviewerConventionPackPolicy",
      "consumer": "src/Agents/reviewer/ConventionPacks.ps1 ~644 (foreach … @($selectionItem.Pack.AuthoritativeSourceRefs)); ~700 (@($pack.AuthoritativeSourceRefs))",
      "collapseRisk": "Can be empty (MinCount=0), in which case the pack must have RepositorySources. Empty @() in PS is safe. No JSON boundary in this path.",
      "knownEscapeShape": false
    },
    {
      "id": "specialistPlan/ConventionPackPolicy/$.Packs[*].ChangedPathGlobs",
      "stage": "specialistPlan",
      "contract": "ConventionPackPolicy",
      "field": "$.Packs[*].ChangedPathGlobs",
      "kind": "list",
      "required": "True",
      "producer": "src/Agents/reviewer/ConventionPacks.ps1 ~568 (@($globs)) inside ConvertTo-ReviewerConventionPackPolicy, stored in pack hashtable field ChangedPathGlobs",
      "consumer": "src/Agents/reviewer/ConventionPacks.ps1 ~609 (foreach ($entry) … $pack.ChangedPathGlobs | Where-Object …)",
      "collapseRisk": "Validated to MinCount=1 so never empty; array stays as PS object[] inside hashtable — no JSON round-trip risk in this in-memory path.",
      "knownEscapeShape": false
    },
    {
      "id": "specialistPlan/ConventionPackPolicy/$.Packs[*].RepositorySources",
      "stage": "specialistPlan",
      "contract": "ConventionPackPolicy",
      "field": "$.Packs[*].RepositorySources",
      "kind": "list",
      "required": "False",
      "producer": "src/Agents/reviewer/ConventionPacks.ps1 ~570 ($localSources.ToArray()) inside ConvertTo-ReviewerConventionPackPolicy",
      "consumer": "src/Agents/reviewer/ConventionPacks.ps1 ~647 (foreach … @($selectionItem.Pack.RepositorySources)); ~701 (@($pack.RepositorySources))",
      "collapseRisk": "Can be empty (0 localSources, covered by sourceRefs). No JSON boundary.",
      "knownEscapeShape": false
    },
    {
      "id": "specialistPlan/FactPlan/$.domains",
      "stage": "specialistPlan",
      "contract": "FactPlan",
      "field": "$.domains",
      "kind": "jsonArray",
      "required": "True",
      "producer": "src/Agents/reviewer/ReviewFacts.ps1 (New-ReviewerFactDomainResult accumulation → array of 5 domain objects in Build-ReviewerFactPlan or equivalent)",
      "consumer": "src/Agents/reviewer/ReviewFacts.ps1:1213 (domains = $body.domains) and :1248 (domains = Get-ReviewerFactValue $Plan \"domains\"); facts schema requires minItems:5 maxItems:5",
      "collapseRisk": "Schema enforces exactly 5 items; collapse to scalar impossible (5 elements never singleton). Still, ConvertFrom-Json without -Depth guard on older PowerShell could truncate nested objects.",
      "knownEscapeShape": false
    },
    {
      "id": "specialistPlan/FactPlan/$.facts",
      "stage": "specialistPlan",
      "contract": "FactPlan",
      "field": "$.facts",
      "kind": "jsonArray",
      "required": "True",
      "producer": "src/Agents/reviewer/ReviewFacts.ps1 New-ReviewerFact accumulation, $facts List → $facts.ToArray() via New-ReviewerFactDomainResult and plan assembly",
      "consumer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~271 (@(Get-ReviewerConventionSpecialistValue $FactPlan 'facts' @())); iterated with foreach to build $factMap",
      "collapseRisk": "0 facts is legal (schema has no minItems). ConvertFrom-Json on 0-element serializes as $null in some PS versions. Consumer uses @(…default @()) guard.",
      "knownEscapeShape": false
    },
    {
      "id": "specialistPlan/FactPlan/$.hashes.scriptClosure",
      "stage": "specialistPlan",
      "contract": "FactPlan",
      "field": "$.hashes.scriptClosure",
      "kind": "jsonArray",
      "required": "True",
      "producer": "src/Agents/reviewer/ReviewFacts.ps1 (scriptClosure array built during plan construction)",
      "consumer": "src/Agents/reviewer/ConventionSpecialist.ps1 ~1116 ($closure = @(Get-ReviewerConventionSpecialistValue $hashes 'scriptClosure' @())); ~1117 ($wrapper = @($closure | Where-Object {…}))",
      "collapseRisk": "minItems:3 in schema. @() wrap on both reads guards against singleton collapse. A 1-item closure would collapse without @().",
      "knownEscapeShape": false
    },
    {
      "id": "specialistPlan/FactPlan policy (facts/v1/policy.json)/$.cloudTest.manifestFileNames",
      "stage": "specialistPlan",
      "contract": "FactPlan policy (facts/v1/policy.json)",
      "field": "$.cloudTest.manifestFileNames",
      "kind": "jsonArray",
      "required": "False",
      "producer": "src/Agents/reviewer/facts/v1/policy.json ~34 (static config array)",
      "consumer": "src/Agents/reviewer/ReviewFacts.ps1 (manifest file discovery)",
      "collapseRisk": "Static 2-item array.",
      "knownEscapeShape": false
    },
    {
      "id": "specialistPlan/FactPlan policy (facts/v1/policy.json)/$.cloudTest.projectExtensions",
      "stage": "specialistPlan",
      "contract": "FactPlan policy (facts/v1/policy.json)",
      "field": "$.cloudTest.projectExtensions",
      "kind": "jsonArray",
      "required": "False",
      "producer": "src/Agents/reviewer/facts/v1/policy.json ~30 (static config array)",
      "consumer": "src/Agents/reviewer/ReviewFacts.ps1 ~497-499 (@($Policy.projectExtensions | Where-Object {…}))",
      "collapseRisk": "Static 3-item array. Singleton would collapse to string on ConvertFrom-Json.",
      "knownEscapeShape": false
    },
    {
      "id": "specialistPlan/FactPlan policy (facts/v1/policy.json)/$.cloudTest.testPathGlobs",
      "stage": "specialistPlan",
      "contract": "FactPlan policy (facts/v1/policy.json)",
      "field": "$.cloudTest.testPathGlobs",
      "kind": "jsonArray",
      "required": "False",
      "producer": "src/Agents/reviewer/facts/v1/policy.json ~26 (static config array)",
      "consumer": "src/Agents/reviewer/ReviewFacts.ps1 ~492-495 (@($Policy.testPathGlobs | Where-Object {…}))",
      "collapseRisk": "Static 3-item array. If reduced to 1 item, singleton collapse on ConvertFrom-Json would give a string not array — consumer uses @() wrap but Where-Object on a string would test the string itself.",
      "knownEscapeShape": false
    },
    {
      "id": "specialistPlan/FactPlan policy (facts/v1/policy.json)/$.fanOut.companionRules",
      "stage": "specialistPlan",
      "contract": "FactPlan policy (facts/v1/policy.json)",
      "field": "$.fanOut.companionRules",
      "kind": "jsonArray",
      "required": "False",
      "producer": "src/Agents/reviewer/facts/v1/policy.json ~52 (static empty array [])",
      "consumer": "src/Agents/reviewer/ReviewFacts.ps1 fanOut domain",
      "collapseRisk": "KNOWN ESCAPE SHAPE: configured as [] (empty). ConvertFrom-Json of [] is $null on PS 5.x but @() on PS 7.x. All code #requires 7.0, but this is the exact empty-array concern: any consumer without @() guard would get $null and fail .Count.",
      "knownEscapeShape": true
    },
    {
      "id": "specialistPlan/FactPlan policy (facts/v1/policy.json)/$.fanOut.filePathGlobs",
      "stage": "specialistPlan",
      "contract": "FactPlan policy (facts/v1/policy.json)",
      "field": "$.fanOut.filePathGlobs",
      "kind": "jsonArray",
      "required": "False",
      "producer": "src/Agents/reviewer/facts/v1/policy.json ~46 (static config array)",
      "consumer": "src/Agents/reviewer/ReviewFacts.ps1 fanOut domain",
      "collapseRisk": "5 items — safe. @() wrap at consumer.",
      "knownEscapeShape": false
    },
    {
      "id": "specialistPlan/FactPlan policy (facts/v1/policy.json)/$.metadata.requiredSections",
      "stage": "specialistPlan",
      "contract": "FactPlan policy (facts/v1/policy.json)",
      "field": "$.metadata.requiredSections",
      "kind": "jsonArray",
      "required": "True",
      "producer": "src/Agents/reviewer/facts/v1/policy.json ~7 (static config array)",
      "consumer": "src/Agents/reviewer/ReviewFacts.ps1 ~268 (foreach ($name in @($Policy.requiredSections))); ~284 (foreach ($nameValue in @($Policy.requiredSections)))",
      "collapseRisk": "Static config, always 3 items. @() wrap at consumer prevents scalar collapse.",
      "knownEscapeShape": false
    },
    {
      "id": "specialistPlan/FactPlan policy (facts/v1/policy.json)/$.metadata.tagNames",
      "stage": "specialistPlan",
      "contract": "FactPlan policy (facts/v1/policy.json)",
      "field": "$.metadata.tagNames",
      "kind": "jsonArray",
      "required": "False",
      "producer": "src/Agents/reviewer/facts/v1/policy.json ~12 (static config array)",
      "consumer": "src/Agents/reviewer/ReviewFacts.ps1 ~297 (foreach ($tagNameValue in @($Policy.tagNames)))",
      "collapseRisk": "Static 2-item array. @() wrap at consumer.",
      "knownEscapeShape": false
    },
    {
      "id": "specialistPlan/FactPlan/Fact/$.facts[*].evidence",
      "stage": "specialistPlan",
      "contract": "FactPlan/Fact",
      "field": "$.facts[*].evidence",
      "kind": "jsonArray",
      "required": "True",
      "producer": "src/Agents/reviewer/ReviewFacts.ps1 ~191 (evidence = $orderedEvidence.ToArray())",
      "consumer": "facts schema (additionalProperties:false, array of evidence objects); facts consumed by specialist for factIds validation",
      "collapseRisk": "KNOWN ESCAPE SHAPE: empty evidence array [] is legal and common (e.g., unknownReason facts). ConvertTo-Json of an empty PS array @() produces [], but ConvertFrom-Json of [] is $null in PowerShell 5.x (not 7.x). All code is #requires -Version 7.0 so this is contained, but still a boundary risk if facts are passed through ConvertTo-Json / ConvertFrom-Json without -AsHashtable.",
      "knownEscapeShape": true
    },
    {
      "id": "specialistPlan/FactPlan/Fact (cloudTest executionEntry)/$.facts[*].value.categories",
      "stage": "specialistPlan",
      "contract": "FactPlan/Fact (cloudTest executionEntry)",
      "field": "$.facts[*].value.categories",
      "kind": "jsonArray",
      "required": "False",
      "producer": "src/Agents/reviewer/ReviewFacts.ps1 ~533 (@($categoryObservations | ForEach-Object { $_.text }))",
      "consumer": "Not iterated by specialist directly; included in canonical JSON for identity SHA computation",
      "collapseRisk": "0 categories is legal. Empty array [] round-trips to $null on ConvertFrom-Json without @() guard. Canonical-JSON serializer handles IEnumerable correctly.",
      "knownEscapeShape": false
    },
    {
      "id": "specialistPlan/FactPlan/Fact (cloudTest executionEntry)/$.facts[*].value.categorySha256",
      "stage": "specialistPlan",
      "contract": "FactPlan/Fact (cloudTest executionEntry)",
      "field": "$.facts[*].value.categorySha256",
      "kind": "jsonArray",
      "required": "False",
      "producer": "src/Agents/reviewer/ReviewFacts.ps1 ~534-536 (@($execution.categories | ForEach-Object { Get-ReviewerFactSha256 … }))",
      "consumer": "Identity / auditing only",
      "collapseRisk": "Same as categories — 0-element collapses on JSON boundary.",
      "knownEscapeShape": false
    },
    {
      "id": "verdict/Get-ReviewerVerificationAcceptedConventionCandidateDecisions (in-memory HashSet)/$seenOriginIds (HashSet[string] — per-origin duplicate suppression)",
      "stage": "verdict",
      "contract": "Get-ReviewerVerificationAcceptedConventionCandidateDecisions (in-memory HashSet)",
      "field": "$seenOriginIds (HashSet[string] — per-origin duplicate suppression)",
      "kind": "hashSet",
      "required": "internal; always starts empty, may stay empty if no accepted decisions",
      "producer": "CrossVerification.ps1:1332 `$seenOriginIds = [System.Collections.Generic.HashSet[string]]::new([StringComparer]::Ordinal)`",
      "consumer": "CrossVerification.ps1:1341 `$seenOriginIds.Add($originCandidateId)` — prevents same originCandidateId appearing twice",
      "collapseRisk": "In-memory only; never serialized. If it stays empty, result array is empty.",
      "knownEscapeShape": false
    },
    {
      "id": "verdict/Get-ReviewerVerificationAcceptedConventionCandidates return/$result (→ $result.ToArray())",
      "stage": "verdict",
      "contract": "Get-ReviewerVerificationAcceptedConventionCandidates return",
      "field": "$result (→ $result.ToArray())",
      "kind": "psCollectionReturn",
      "required": "no; LEGALLY EMPTY when no convention decisions were accepted",
      "producer": "CrossVerification.ps1:1395 `return $result.ToArray()`",
      "consumer": "(wrapper for specialist candidate forwarding)",
      "collapseRisk": "HIGH: bare ToArray() without `, `. Empty result collapses to $null at a bare assignment caller. Wrapper must wrap in @().",
      "knownEscapeShape": false
    },
    {
      "id": "verdict/Get-ReviewerVerificationAcceptedReconciliationCandidates return/$result (→ $result.ToArray())",
      "stage": "verdict",
      "contract": "Get-ReviewerVerificationAcceptedReconciliationCandidates return",
      "field": "$result (→ $result.ToArray())",
      "kind": "psCollectionReturn",
      "required": "no; LEGALLY EMPTY when no convention-bound verified candidates",
      "producer": "CrossVerification.ps1:1502 `return $result.ToArray()`",
      "consumer": "(wrapper for reconciliation candidate assembly)",
      "collapseRisk": "HIGH: bare ToArray() without `, `. Caller must wrap in @().",
      "knownEscapeShape": false
    },
    {
      "id": "verdict/Limit-ReviewerVerificationToPhaseDeadline return/return.eligible / return.withheld / return.decisions",
      "stage": "verdict",
      "contract": "Limit-ReviewerVerificationToPhaseDeadline return",
      "field": "return.eligible / return.withheld / return.decisions",
      "kind": "jsonArray",
      "required": "yes; all three fields always present",
      "producer": "CrossVerification.ps1:2043-2048 `eligible = @(); withheld = @($withheld.ToArray()); decisions = @($Replay.decisions)`",
      "consumer": "wrapper (Start-ReviewerAgent.ps1) reads back these three arrays to build the preview manifest",
      "collapseRisk": "ESCAPE SHAPE: when deadline exceeded, eligible is ALWAYS @() — the function unconditionally empties it. All previously eligible candidates are moved to withheld under \"phaseDeadline\" reason.",
      "knownEscapeShape": false
    },
    {
      "id": "verdict/thread fact (in-memory from Get-ReviewerVerificationThreadFacts)/threadFact.authorClasses",
      "stage": "verdict",
      "contract": "thread fact (in-memory from Get-ReviewerVerificationThreadFacts)",
      "field": "threadFact.authorClasses",
      "kind": "jsonArray",
      "required": "no; default @()",
      "producer": "CrossVerification.ps1:1553 `authorClasses = @((Get-ReviewerVerificationValue $value \"authorClasses\" @()))`",
      "consumer": "CrossVerification.ps1:1594 `$authorClasses -ccontains \"agent\"` (membership test to classify as duplicatePriorAgent vs duplicateExistingThread)",
      "collapseRisk": "ESCAPE SHAPE: empty authorClasses → reason=\"duplicateExistingThread\" (not agent). Single-element array with \"agent\" → reason=\"duplicatePriorAgent\". `-ccontains` works correctly on both empty and singleton.",
      "knownEscapeShape": false
    },
    {
      "id": "verdict/thread facts (from Get-ReviewerVerificationThreadFacts)/$records (→ $records.ToArray())",
      "stage": "verdict",
      "contract": "thread facts (from Get-ReviewerVerificationThreadFacts)",
      "field": "$records (→ $records.ToArray())",
      "kind": "psCollectionReturn",
      "required": "no; 0 threads = no existing review threads",
      "producer": "CrossVerification.ps1:1564 `return $records.ToArray()`",
      "consumer": "CrossVerification.ps1:1579 `foreach ($thread in @($ThreadFacts))`; DeliveryGates.ps1:770 `foreach ($thread in @($ThreadFacts))`",
      "collapseRisk": "Bare ToArray() without `, `. Both callers wrap in @() at call sites. The function itself correctly returns empty typed array when no thread facts exist.",
      "knownEscapeShape": false
    },
    {
      "id": "verdict/verification-decision-preview//decisions",
      "stage": "verdict",
      "contract": "verification-decision-preview",
      "field": "/decisions",
      "kind": "jsonArray",
      "required": "no required in schema; LEGALLY EMPTY (empty run condition checked at RunReconciliation.ps1:406)",
      "producer": "CrossVerification.ps1 (wrapper writes verifier decision list into preview artifact)",
      "consumer": "RunReconciliation.ps1:404 `@(Get-ReviewerRunReconciliationValue $VerificationManifest \"decisions\" @()).Count`; CrossVerification.ps1:1323 `foreach ($decision in @($Decisions))`",
      "collapseRisk": "ESCAPE SHAPE: decisions=[] signals an empty run. If ConvertFrom-Json returns a PSCustomObject for an empty decisions array and a consumer does `$manifest.decisions` without @(), they get $null in PS 7 for an absent field (but present-empty gives @()). Read with @() guard everywhere.",
      "knownEscapeShape": false
    },
    {
      "id": "verdict/verification-decision-preview//eligiblePreviewCandidates",
      "stage": "verdict",
      "contract": "verification-decision-preview",
      "field": "/eligiblePreviewCandidates",
      "kind": "jsonArray",
      "required": "no; LEGALLY EMPTY (no verified candidates = eligible=[])",
      "producer": "src/Agents/reviewer/Start-ReviewerAgent.ps1:10794 (eligiblePreviewCandidates = @($Eligible))",
      "consumer": "src/Agents/reviewer/RunReconciliation.ps1:405 `@(Get-ReviewerRunReconciliationValue $VerificationManifest \"eligiblePreviewCandidates\" @()).Count`",
      "collapseRisk": "ESCAPE SHAPE: 0-element eligible list triggers empty-run path. Wrapped in @() at every read.",
      "knownEscapeShape": false
    },
    {
      "id": "verdict/verification-input (candidates normalized for verifier input)//candidates",
      "stage": "verdict",
      "contract": "verification-input (candidates normalized for verifier input)",
      "field": "/candidates",
      "kind": "jsonArray",
      "required": "no; LEGALLY EMPTY (all candidates withheld via candidateLimit etc.)",
      "producer": "CrossVerification.ps1:1127 `candidates = $bounded.ToArray()`",
      "consumer": "DeliveryGates.ps1:653 `@(Get-ReviewerVerificationValue $InputManifest \"candidates\" @())`; RunReconciliation.ps1:974 `@(Get-ReviewerRunReconciliationValue $manifest \"candidates\" @()).Count`",
      "collapseRisk": "ESCAPE SHAPE: can be 0 elements. Stored as typed empty array in pscustomobject, serialized via canonical JSON. ConvertFrom-Json -Depth 32 restores it correctly. Callers wrap in @().",
      "knownEscapeShape": false
    },
    {
      "id": "verdict/verification-input-preview / verification-decision-preview//withheld",
      "stage": "verdict",
      "contract": "verification-input-preview / verification-decision-preview",
      "field": "/withheld",
      "kind": "jsonArray",
      "required": "no; LEGALLY EMPTY (all candidates within cap = nothing withheld)",
      "producer": "CrossVerification.ps1:1116-1125 ($withheld.ToArray() via Get-ReviewerVerificationCandidatePlan); Limit-ReviewerVerificationToPhaseDeadline:2045 @($withheld.ToArray())",
      "consumer": "RunReconciliation.ps1:419 `$withheldRows = @(Get-ReviewerRunReconciliationValue … \"withheld\" @())`; DeliveryGates.ps1:813 `foreach ($item in @($Withheld))`",
      "collapseRisk": "ESCAPE SHAPE: 0-withheld is the normal success case. Stored as List.ToArray() → empty typed array in pscustomobject. ConvertFrom-Json at Depth 32 re-reads as empty array, not $null. Callers use @() wrapper.",
      "knownEscapeShape": false
    },
    {
      "id": "verifierAssignment/$script:ReviewerVerificationOutcomes (code constant)/$script:ReviewerVerificationOutcomes",
      "stage": "verifierAssignment",
      "contract": "$script:ReviewerVerificationOutcomes (code constant)",
      "field": "$script:ReviewerVerificationOutcomes",
      "kind": "jsonArray",
      "required": "code-defined; always non-empty",
      "producer": "CrossVerification.ps1:59-61 `@(\"verified\",\"duplicate\",\"unsupported\",\"wrongSeverity\",\"needsHuman\")`",
      "consumer": "(wrapper validates verifier outcome strings against this set)",
      "collapseRisk": "None; code constant.",
      "knownEscapeShape": false
    },
    {
      "id": "verifierAssignment/$script:ReviewerVerificationWithheldReasons (code constant)/$script:ReviewerVerificationWithheldReasons",
      "stage": "verifierAssignment",
      "contract": "$script:ReviewerVerificationWithheldReasons (code constant)",
      "field": "$script:ReviewerVerificationWithheldReasons",
      "kind": "jsonArray",
      "required": "code-defined; always non-empty",
      "producer": "CrossVerification.ps1:62-79 (closed vocabulary array)",
      "consumer": "DeliveryGates.ps1:817 `$script:ReviewerVerificationWithheldReasons -cnotcontains $reason` (membership test)",
      "collapseRisk": "None; code constant used only for membership tests.",
      "knownEscapeShape": false
    },
    {
      "id": "verifierAssignment/convention-plan (ConventionPlan JSON)//selectedPacks",
      "stage": "verifierAssignment",
      "contract": "convention-plan (ConventionPlan JSON)",
      "field": "/selectedPacks",
      "kind": "jsonArray",
      "required": "not required in target schema files; default @() → empty pack list is legal",
      "producer": "convention plan artifact (ConventionPacks.ps1, not a target file)",
      "consumer": "CrossVerification.ps1:672 `@(Get-ReviewerVerificationValue $ConventionPlan \"selectedPacks\" @())`; also DeliveryGates.ps1:658 `@(Get-ReviewerVerificationValue $ConventionPlan \"selectedPacks\" @())`",
      "collapseRisk": "Both sites wrap in @(). An empty selectedPacks list means no convention bindings are produced, which silently degrades convention coverage.",
      "knownEscapeShape": false
    },
    {
      "id": "verifierAssignment/convention-plan /selectedPacks[*]//selectedPacks[*]/matchedPaths",
      "stage": "verifierAssignment",
      "contract": "convention-plan /selectedPacks[*]",
      "field": "/selectedPacks[*]/matchedPaths",
      "kind": "jsonArray",
      "required": "not required; default @()",
      "producer": "convention plan artifact",
      "consumer": "CrossVerification.ps1:674 `@((Get-ReviewerVerificationValue $pack \"matchedPaths\" @()) | Where-Object {…})`",
      "collapseRisk": "Wrapped in @(). An empty matchedPaths means the pack never matches any candidate path → no convention bindings produced.",
      "knownEscapeShape": false
    },
    {
      "id": "verifierAssignment/convention-plan /selectedPacks[*]//selectedPacks[*]/sources",
      "stage": "verifierAssignment",
      "contract": "convention-plan /selectedPacks[*]",
      "field": "/selectedPacks[*]/sources",
      "kind": "jsonArray",
      "required": "not required; default @()",
      "producer": "convention plan artifact",
      "consumer": "CrossVerification.ps1:680 `@(Get-ReviewerVerificationValue $pack \"sources\" @())`; DeliveryGates.ps1:660 `@(Get-ReviewerVerificationValue $pack \"sources\" @())`",
      "collapseRisk": "Wrapped in @(). Empty sources → no rule bindings found for this pack.",
      "knownEscapeShape": false
    },
    {
      "id": "verifierAssignment/Get-ReviewerVerificationAssignments return value/$assignments (→ $assignments.ToArray())",
      "stage": "verifierAssignment",
      "contract": "Get-ReviewerVerificationAssignments return value",
      "field": "$assignments (→ $assignments.ToArray())",
      "kind": "psCollectionReturn",
      "required": "no; 0 assignments is legal when no ready cluster exists",
      "producer": "src/Agents/reviewer/CrossVerification.ps1:1697 `return $assignments.ToArray()`",
      "consumer": "CrossVerification.ps1:1727 `@($Assignments).Count` in Assert-ReviewerVerificationAssignmentCoverage; 1732 `@($Assignments | Where-Object {…})`",
      "collapseRisk": "HIGH: bare `ToArray()` without `, ` prefix. An empty List returns `@()` → `.ToArray()` returns a zero-length typed array, and PowerShell 7 unwraps a typed empty array to `$null` in a bare assignment. Assert-ReviewerVerificationAssignmentCoverage wraps in @() at line 1727, but any caller that does `$assignments = Get-ReviewerVerificationAssignments …` without @() gets $null when no assignments exist.",
      "knownEscapeShape": false
    },
    {
      "id": "verifierAssignment/Get-ReviewerVerificationConventionBindings return value/$bindings (→ $bindings.ToArray())",
      "stage": "verifierAssignment",
      "contract": "Get-ReviewerVerificationConventionBindings return value",
      "field": "$bindings (→ $bindings.ToArray())",
      "kind": "psCollectionReturn",
      "required": "no; 0 bindings = no convention binding found for this candidate",
      "producer": "src/Agents/reviewer/CrossVerification.ps1:736 `return $bindings.ToArray()`",
      "consumer": "CrossVerification.ps1:924 `$bindings = @(Get-ReviewerVerificationConventionBindings …)` — caller wraps in @()",
      "collapseRisk": "Caller wraps in @() at line 924, so safe at that site. Other hypothetical callers must do the same.",
      "knownEscapeShape": false
    },
    {
      "id": "verifierAssignment/Get-ReviewerVerificationTokens return/$list (→ $list.ToArray())",
      "stage": "verifierAssignment",
      "contract": "Get-ReviewerVerificationTokens return",
      "field": "$list (→ $list.ToArray())",
      "kind": "psCollectionReturn",
      "required": "no; 0 tokens = no meaningful text",
      "producer": "CrossVerification.ps1:513 `return $list.ToArray()`",
      "consumer": "CrossVerification.ps1:628 `@(Get-ReviewerVerificationTokens …)` (wrapped); 636 (wrapped); 1578 `@(Get-ReviewerVerificationTokens …)` (wrapped); 1586 (wrapped)",
      "collapseRisk": "Bare ToArray() without `, `. ALL callers in these files wrap in @(), so safe at those sites. Any external caller that does not wrap gets $null for an empty token set.",
      "knownEscapeShape": false
    },
    {
      "id": "verifierAssignment/in-memory cluster/$cluster.memberHashes",
      "stage": "verifierAssignment",
      "contract": "in-memory cluster",
      "field": "$cluster.memberHashes",
      "kind": "jsonArray",
      "required": "not schema-validated; ≥1 member → ≥1 hash by construction",
      "producer": "src/Agents/reviewer/CrossVerification.ps1:1278-1280 `$hashes = @($members | ForEach-Object { ... })` then assigned to memberHashes",
      "consumer": "src/Agents/reviewer/CrossVerification.ps1:1281 passed to Get-ReviewerVerificationObjectSha256 for clusterHash",
      "collapseRisk": "One-element array: if only one candidate is in the cluster, `$hashes` is a one-element array; ConvertTo-ReviewerVerificationCanonicalArray handles it correctly. But if serialized by ConvertTo-Json (not canonical), a one-element array survives only at depth ≥1.",
      "knownEscapeShape": false
    },
    {
      "id": "verifierAssignment/in-memory cluster/$cluster.origins (from HashSet[string] → List → ToArray())",
      "stage": "verifierAssignment",
      "contract": "in-memory cluster",
      "field": "$cluster.origins (from HashSet[string] → List → ToArray())",
      "kind": "hashSet",
      "required": "no; can be 1-element when all members share same originModel",
      "producer": "src/Agents/reviewer/CrossVerification.ps1:1282-1288 HashSet $origins populated per member, then $originList.ToArray() stored as origins",
      "consumer": "CrossVerification.ps1 (wrapper reads for reporting)",
      "collapseRisk": "Singleton: a 1-element ToArray() is a proper array. No collapse risk in PS 7.",
      "knownEscapeShape": false
    },
    {
      "id": "verifierAssignment/in-memory cluster (Get-ReviewerVerificationClusters)/$cluster.members  (→ cluster[].members array)",
      "stage": "verifierAssignment",
      "contract": "in-memory cluster (Get-ReviewerVerificationClusters)",
      "field": "$cluster.members  (→ cluster[].members array)",
      "kind": "psCollectionReturn",
      "required": "no schema constraint; at least 1 member per cluster by construction; overflow cluster may have many",
      "producer": "src/Agents/reviewer/CrossVerification.ps1:1305 `members = $members.ToArray()`",
      "consumer": "src/Agents/reviewer/CrossVerification.ps1:1656 foreach cluster.members; 1716 Assert-ReviewerVerificationAssignmentCoverage foreach; 1334 Get-ReviewerVerificationAcceptedConventionCandidateDecisions foreach; 1420 Get-ReviewerVerificationAcceptedReconciliationC…",
      "collapseRisk": "Get-ReviewerVerificationClusters returns `$clusters.ToArray()` bare (no leading `, `). If the candidate list is empty, this collapses to `$null` at the caller unless it wraps in @(). Callers in the wrapper (Start-ReviewerAgent.ps1) must wrap.",
      "knownEscapeShape": false
    },
    {
      "id": "verifierAssignment/ruleCoverage (specialist manifest)//ruleCoverage/changedFileAnchors",
      "stage": "verifierAssignment",
      "contract": "ruleCoverage (specialist manifest)",
      "field": "/ruleCoverage/changedFileAnchors",
      "kind": "jsonArray",
      "required": "yes in empty-run schema (explicitly set to @()); otherwise no minItems",
      "producer": "convention specialist manifest (not in target files)",
      "consumer": "CrossVerification.ps1:560 `@(Get-ReviewerVerificationValue $anchor \"rightHandRanges\" @())` (inner); 627 `@(Get-ReviewerRunReconciliationValue $coverage \"changedFileAnchors\" @())`; RunReconciliation.ps1:336,627,657,661,764",
      "collapseRisk": "Always wrapped in @() at all read sites. An empty list means no changed-file anchor lookup succeeds → no anchor match → candidate withheld.",
      "knownEscapeShape": false
    },
    {
      "id": "verifierAssignment/ruleCoverage /changedFileAnchors[*]//ruleCoverage/changedFileAnchors[*]/rightHandRanges",
      "stage": "verifierAssignment",
      "contract": "ruleCoverage /changedFileAnchors[*]",
      "field": "/ruleCoverage/changedFileAnchors[*]/rightHandRanges",
      "kind": "jsonArray",
      "required": "not required; default @()",
      "producer": "specialist manifest; also RunReconciliation.ps1:342-348 (binding projection)",
      "consumer": "CrossVerification.ps1:560 `@(Get-ReviewerVerificationValue $anchor \"rightHandRanges\" @())`; RunReconciliation.ps1:661",
      "collapseRisk": "Empty rightHandRanges on an anchor → no line-in-range match → Get-ReviewerVerificationChangedFileAnchor returns null → candidate withheld as sourceInvalid.",
      "knownEscapeShape": false
    },
    {
      "id": "verifierAssignment/verification-candidate.v1 (schema)//existingDebtEvidence/attributeFrequency",
      "stage": "verifierAssignment",
      "contract": "verification-candidate.v1 (schema)",
      "field": "/existingDebtEvidence/attributeFrequency",
      "kind": "jsonArray",
      "required": "not required at candidate level (existingDebtEvidence is nullable); no minItems in schema",
      "producer": "specialist manifest (generalist or convention candidate)",
      "consumer": "CrossVerification.ps1:847 `existingDebtEvidence = Get-ReviewerVerificationValue $RawCandidate \"existingDebtEvidence\" $null` — passed through opaquely",
      "collapseRisk": "If serialized via ConvertTo-ReviewerVerificationCanonicalJson the array is preserved correctly. If serialized via default ConvertTo-Json without -Depth ≥3, elements inside the nullable object would be truncated. The canonical path uses Depth 32, so safe.",
      "knownEscapeShape": false
    },
    {
      "id": "verifierAssignment/verification-input-preview (generalist marker)//findings",
      "stage": "verifierAssignment",
      "contract": "verification-input-preview (generalist marker)",
      "field": "/findings",
      "kind": "jsonArray",
      "required": "not required; default @() means missing field is treated as empty; empty is legal",
      "producer": "src/Agents/reviewer/CrossVerification.ps1:919 (generalist pass marker read via `@(Get-ReviewerVerificationValue $marker \"findings\" @())`)",
      "consumer": "src/Agents/reviewer/CrossVerification.ps1:919-933 — foreach loop, each element becomes a New-ReviewerVerificationCandidate call; also ~1586 candidateTokens built from thread facts",
      "collapseRisk": "Low at this site: wrapped in @() at call site. If serialized then re-read via ConvertFrom-Json -Depth <2 (default depth), nested objects inside findings would be truncated to strings — but Read-ReviewerVerificationArtifact uses -Depth 32, so safe.",
      "knownEscapeShape": false
    }
  ]
}
