name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  hygiene:
    name: Generic-toolkit invariant
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      # The premise of this repo is that it is generic. That claim erodes one
      # commit at a time unless it is checked mechanically.
      - name: No employer-specific values outside samples/
        shell: pwsh
        run: ./tools/Test-NoEmployerSpecifics.ps1

  lint:
    name: PSScriptAnalyzer
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install PSScriptAnalyzer
        shell: pwsh
        run: Install-Module PSScriptAnalyzer -Force -Scope CurrentUser -ErrorAction Stop

      - name: Analyze
        shell: pwsh
        run: |
          $results = Invoke-ScriptAnalyzer -Path ./src -Recurse -Severity Error
          if ($results) {
              $results | Format-Table -AutoSize
              throw "PSScriptAnalyzer reported $($results.Count) error-severity finding(s)."
          }
          Write-Host "PSScriptAnalyzer: clean at Error severity."

  manifest:
    name: Module manifest
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate manifest and import module
        shell: pwsh
        run: |
          $manifest = './src/DevPilot.AgentHarness/DevPilot.AgentHarness.psd1'
          $info = Test-ModuleManifest -Path $manifest
          Write-Host "Manifest OK: $($info.Name) $($info.Version), $($info.ExportedFunctions.Count) exported function(s)."
          Import-Module $manifest -Force
          # The asset resolver is what lets a consumer find prompts and fixtures
          # after installing the module; if it breaks, agents cannot start.
          $agents = Get-DevPilotAgentPath
          Write-Host "Agent assets resolved at: $agents"

  tests:
    name: Targeted Pester tests
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Pester
        shell: pwsh
        run: Install-Module Pester -RequiredVersion 5.7.1 -Force -Scope CurrentUser -ErrorAction Stop

      - name: Run targeted tests
        shell: pwsh
        run: |
          $result = Invoke-Pester -Path ./tests -Output Detailed -PassThru
          if ($result.FailedCount -gt 0) {
              throw "Pester reported $($result.FailedCount) failed test(s)."
          }

  dashboard:
    name: Operations dashboard
    runs-on: windows-latest
    defaults:
      run:
        working-directory: src/DevPilot.Dashboard
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
          cache-dependency-path: src/DevPilot.Dashboard/package-lock.json

      - name: Restore locked dependencies
        shell: pwsh
        run: npm ci

      - name: Build and run logic tests
        shell: pwsh
        run: npm test

      - name: Run native renderer smoke test
        shell: pwsh
        run: npm run test:renderer

  selfcheck:
    name: Agent self-checks (-DryRun)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      # Offline by design: no network, no Azure DevOps, no Copilot process.
      - name: review-handler -DryRun
        shell: pwsh
        run: |
          ./src/Agents/review-handler/Start-ReviewHandlerAgent.ps1 -DryRun `
              -ConfigFile ./samples/handler-ado.config.json

      - name: reviewer -DryRun
        shell: pwsh
        run: |
          ./src/Agents/reviewer/Start-ReviewerAgent.ps1 -DryRun `
              -ConfigFile ./samples/reviewer-ado.config.json

      # Provider normalization, asserted against a fixture captured from a real
      # GitHub API response. Offline: no network, no host, no model.
      - name: provider self-checks
        shell: pwsh
        run: ./tools/Test-Provider.ps1

  platform-safety:
    name: Platform safety (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Pester
        shell: pwsh
        run: Install-Module Pester -RequiredVersion 5.7.1 -Force -Scope CurrentUser -ErrorAction Stop

      - name: Run platform-sensitive suites
        shell: pwsh
        run: |
          $result = Invoke-Pester -Path @(
            './tests/DispatchProtocol.Tests.ps1',
            './tests/DurableState.Tests.ps1',
            './tests/DurableStateMigration.Tests.ps1',
            './tests/Invoke-TimedProcess.Tests.ps1',
            './tests/ManualRedispatch.Tests.ps1',
            './tests/ReviewHandler.Resume.Tests.ps1',
            './tests/ReviewerOutput.Tests.ps1'
          ) -Output Detailed -PassThru
          if ($result.FailedCount -gt 0) {
            throw "Platform Pester failures: $($result.FailedCount)"
          }

  dashboard-dispatch:
    name: Dashboard dispatch groundwork (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        working-directory: src/DevPilot.Dashboard
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
          cache-dependency-path: src/DevPilot.Dashboard/package-lock.json

      - name: Restore and test dashboard
        shell: pwsh
        run: |
          npm ci
          npm test
          $bun = './node_modules/bun/bin/bun.exe'
          & $bun --conditions=browser test ./dist/test/dispatch.test.js
